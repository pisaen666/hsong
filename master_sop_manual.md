# 📘 คู่มือปฏิบัติงานและผังความสัมพันธ์ระบบ 5 บทบาท (Master SOP & Ecosystem Manual)

**โปรเจกต์:** แพลตฟอร์มตลาดสดออนไลน์บ้านบึง (TalatHub / Hsong)  
**เวอร์ชันเอกสาร:** 1.0 (Master Standard Operating Procedure)  
**คลังซอร์สโค้ด:** https://github.com/pisaen666/hsong  

---

## 🗺️ ส่วนที่ 1: แผนผังภาพรวมความสัมพันธ์และการไหลของระบบ (Ecosystem Flow)

ระบบ TalatHub ทำงานร่วมกัน 5 บทบาท (5-Roles Multi-Sided Marketplace) โดยมีกระแสการไหล 3 สายหลัก:

```
[ Role 1: ลูกค้า (Customer) ]
       │  ▲
 1.สั่งซื้อ│  │ 5.จัดส่งของสดถึงบ้าน
       ▼  │
[ Role 2: ฮับตลาดกลาง (Hub Operations) ] ── ( 5. ส่งพิกัด/งาน ) ──► [ Role 4: ไรเดอร์ (Rider) ]
       │  ▲                                                             │
 2.ใบจัด│  │ 3.ส่งมอบของสด                                               │ 4.รับของสด
       ▼  │                                                             ▼
[ Role 3: แผงค้าแม่ค้า (Merchant Stalls) ] ◄────────────────────────────┘ (กรณีส่งด่วน Express)

                         [ Role 5: ผู้ดูแลระบบ (Admin) ]
                       ( กำกับดูแล, อนุมัติ, ดูเรดาร์ภาพรวม )
```

### 1. การไหลของสินค้า (Goods Flow)
* **การสั่งซื้อปกติ (Hub Consolidated):** สินค้าสดจากแผงค้า (Role 3) ➡️ รวมถุงคัดแยกที่ฮับ (Role 2) ➡️ ไรเดอร์รับถุงรวม (Role 4) ➡️ จัดส่งถึงบ้านลูกค้า (Role 1)
* **การสั่งซื้อส่งด่วน (Merchant Express):** สินค้าสดจากแผงค้า (Role 3) ➡️ ไรเดอร์รับตรงที่หน้าแผง ➡️ จัดส่งตรงถึงบ้านลูกค้า (Role 1)

### 2. การไหลของเงิน (Money Flow)
* **กรณีชำระโอน (PromptPay/QR):** ลูกค้าโอนเข้าบัญชีฮับกลาง ➡️ ฮับเคลียร์ค่างวด หัก GP 5% ➡️ โอนเข้าบัญชีธนาคารแม่ค้า (Role 3) และ จ่ายค่ารอบ ฿40 ให้ไรเดอร์ (Role 4)
* **กรณีชำระปลายทาง (COD Cash):** ลูกค้าจ่ายเงินสดให้ไรเดอร์ (Role 4) ➡️ ไรเดอร์ถือเงินสดไว้ ➡️ ไรเดอร์โอนเคลียร์ยอดสุทธิคืนฮับกลางช่วงเย็น

---

## 📖 ส่วนที่ 2: ขั้นตอนการปฏิบัติงานอย่างละเอียดแยกตามบทบาท (Role SOPs)

---

### 🛒 2.1 Role 1: ลูกค้าผู้สั่งซื้อของสด (Customer SOP)

| ขั้นตอน | การปฏิบัติงาน | ฟังก์ชันระบบในแอป |
| :---: | :--- | :--- |
| **1. เลือกสินค้า** | เลือกของสดจากแผงค้าต่างๆ ในตลาดสดบ้านบึง เพิ่มลงตะกร้า | `addToCart()`, `updateCartUI()` |
| **2. ตรวจสอบตะกร้า** | ระบุจำนวน ปรับแต่งหมายเหตุพิเศษ (เช่น บดละเอียด, ไม่เอาหัว) | `changeCartQty()`, `renderCheckoutPage()` |
| **3. ชำระเงิน & ที่อยู่** | ปักหมุดพิกัด GPS เลือกชำระผ่าน PromptPay QR หรือ COD เงินสด | `processOrderCheckout()`, `selectPaymentMethod()` |
| **4. ติดตามสถานะ** | เปิดดูหน้าจอติดตามพิกัด GPS ของไรเดอร์แบบเรียลไทม์ | `renderTrackingScreen()`, `initRiderTrackingMap()` |
| **5. รับของ & ให้ดาว** | ตรวจรับของสด กดให้คะแนนดาวสะสมแต้มส่วนลด | `openRatingModal()`, `submitRating()` |

---

### 📦 2.2 Role 2: เจ้าหน้าที่ศูนย์จัดส่งฮับ (Hub Operations SOP)

| ขั้นตอน | การปฏิบัติงาน | ฟังก์ชันระบบในแอป |
| :---: | :--- | :--- |
| **1. รับออเดอร์เข้า** | ตรวจสอบรายการออเดอร์รวมสดใหม่ที่เข้ามายังฮับ | `renderHubPickingList()` |
| **2. คัดแยก & รวบรวม** | พิมพ์/ดูใบจัดของสด ตรวจรับของสดจากแม่ค้าตามแผง | `viewHubOrderDetails()`, `markItemPicked()` |
| **3. กระจายงานไรเดอร์** | มอบหมายคิวจัดส่งใหักับไรเดอร์ประจำชุมชน | `assignOrderToRider()`, `assignExpressOrderToRider()` |
| **4. ตรวจสลิปค่าส่ง** | กดดูรูปสลิปค่าส่งด่วนของแผงค้า และอนุมัติจ่ายงาน | `viewHubMerchantExpressSlip()`, `approveHubMerchantExpressSlip()` |
| **5. เคลียร์ค่างวดประจำวัน** | ตรวจสอบสรุปรายงานประจำวัน กดโอนเงินค่างวดให้แม่ค้า/ไรเดอร์ | `aggregateDailyOperations()`, `markVendorSettled()` |

---

### 🏪 2.3 Role 3: แผงค้าแม่ค้าในตลาด (Merchant Stall SOP)

| ขั้นตอน | การปฏิบัติงาน | ฟังก์ชันระบบในแอป |
| :---: | :--- | :--- |
| **1. บริหารร้านค้า** | กดสลับปุ่ม `🟢 เปิดรับออเดอร์` หรือ `🔴 พักร้านชั่วคราว` | `toggleMerchantStoreStatus()` |
| **2. ตั้งค่าการรับเงิน** | ระบุชื่อธนาคาร เลขที่บัญชี หรือพร้อมเพย์ สำหรับรับค่างวด | `openMerchantPortalModal()`, `saveMerchantStallData()` |
| **3. จัดเตรียมวัตถุดิบ** | จัดเตรียมของสดตามรายการออเดอร์ที่ฮับหรือลูกค้าส่งมา | `renderMerchantOrdersList()` |
| **4. เรียกรถส่งด่วน** | กดปุ่มเรียกรถไรเดอร์ส่งด่วนกรณีลูกค้าสั่งตรง | `submitMerchantExpressRequest()`, `cancelMerchantExpressOrder()` |
| **5. ดูสรุปเงินโอน** | เปิดแท็บ `3. สรุปยอดเงินโอนค่างวด` เพื่อดูยอดขายสุทธิหลังหัก GP 5% | `renderMerchantSettlementTab()` |

---

### 🛵 2.4 Role 4: ไรเดอร์ผู้จัดส่งชุมชน (Rider SOP)

| ขั้นตอน | การปฏิบัติงาน | ฟังก์ชันระบบในแอป |
| :---: | :--- | :--- |
| **1. เลือกรับงาน** | สลับไปแท็บ `2. บอร์ดรับงานด่วน` แล้วกด `🛵 กดรับงานนี้` | `switchRiderMainTab('pool')`, `claimOrderForRider()` |
| **2. ส่งสัญญาณพิกัด** | เมื่อถึงจุดรับของให้กด `📍 ถึงหน้าแผงค้าแล้ว` | `markRiderArrivedAtStall()` |
| **3. ออกเดินทางส่ง** | กดปุ่ม `1. รับของแล้ว ออกส่ง` เพื่อเปิดระบบ GPS ให้ลูกค้าตามดูได้ | `handleRiderStartDelivery()` |
| **4. ส่งมอบ & ถ่ายรูป** | เมื่อถึงบ้านลูกค้าให้กด `🏠 ถึงหน้าบ้านลูกค้าแล้ว` และกดอัปโหลดรูปหลักฐาน | `markRiderArrivedAtCustomer()`, `confirmRiderDeliveryWithProof()` |
| **5. สรุปเงินกระเป๋า** | เปิดแท็บ `3. กระเป๋าเงินไรเดอร์` ดูสรุปค่ารอบ (฿40/รอบ) และยอด COD ต้องส่งฮับ | `switchRiderMainTab('wallet')`, `renderRiderWallet()` |

---

### 🔑 2.5 Role 5: ผู้ดูแลระบบ (Admin SOP)

| ขั้นตอน | การปฏิบัติงาน | ฟังก์ชันระบบในแอป |
| :---: | :--- | :--- |
| **1. อนุมัติแผงค้า** | ตรวจสอบคำขอสมัครแผงค้าใหม่และออกรหัสผ่าน | `renderAdminStallsTable()`, `approveMerchantApplication()` |
| **2. เรดาร์กองยาน** | เปิดดูแผนที่เรดาร์พิกัดไรเดอร์เรียลไทม์ทั่วพื้นที่บ้านบึง | `initAdminRiderRadarMap()` |
| **3. ดูรายงานสรุป** | ดูสรุปยอดขายรวมทั้งตลาด ยอด GP สะสม และจำนวนออเดอร์รวม | `renderAdminDashboard()`, `renderDailyReportSummary()` |

---

## 🛠️ ส่วนที่ 3: ขั้นตอนการรับมือกรณีพิเศษ (Special Scenarios)

1. **กรณีของสดหมดบางรายการ (Out of Stock & Refund):**
   * แม่ค้า/ฮับ แจ้งว่าสินค้าหมด ➡️ ระบบบันทึกยอดเงินสดต้องทอนคืน (`refundCashTotal`) ➡️ ไรเดอร์ถือเงินสดไปทอนคืนลูกค้าหน้าบ้านพร้อมแจ้งเตือนในแอป
2. **กรณีแผงค้าขอยกเลิกเรียกรถด่วน (Express Cancellation):**
   * แม่ค้ากด `✕ ขอยกเลิกคำขอเรียกไรเดอร์` ➡️ สถานะเปลี่ยนเป็นยกเลิก ➡️ คืนยอดเข้ากระเป๋าและปลดล็อกไรเดอร์
3. **กรณีสลับบทบาทจำลอง (Cross-Role Testing):**
   * ผู้ทดสอบสามารถกดปุ่มสลับมุมมอง (`switchRole('customer' | 'hub' | 'merchant' | 'rider' | 'admin')`) บริเวณแถบ Top Bar ได้ตลอดเวลาโดยไม่หลุดจากระบบ

---
*บันทึกสำเร็จโดย Antigravity AI - พร้อมนำไปพิมพ์การ์ด หรือเปิดบน Google Docs*
