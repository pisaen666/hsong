# 📋 เอกสารส่งต่องาน (Hand-off) — TalatHub / Hsong

**อัปเดตล่าสุด:** 21 กันยายน 2026 (ปิดงานที่ออฟฟิศ ส่งต่อไปทำที่บ้าน)
**คลังโค้ด:** https://github.com/pisaen666/hsong  |  **เว็บจริง:** https://pisaen666.github.io/hsong/  |  **เว็บทดสอบ:** https://hsong-test.web.app
**ไฟล์กติกาของ Claude:** `CLAUDE.md` (อ่านอัตโนมัติ) — ไฟล์นี้บอก "งานค้างและจุดที่ทำถึง"

> สำหรับ Claude ที่บ้าน: เจ้าของเป็นมือใหม่ สายตาไม่ดี ให้ตอบเป็นภาษาไทย คำง่าย และ **อธิบายทุกขั้นตอนที่ให้เจ้าของทำแบบละเอียดเป็นข้อ ๆ** (บอกชื่อปุ่มตามหน้าจอ บอกว่าควรเห็นอะไร และถ้าเห็นไม่ตรงให้ทำอย่างไร) เจ้าของขอเรื่องนี้ไว้ชัดเจน (ความจำใน Claude เครื่องออฟฟิศไม่ตามมาที่บ้าน จึงจดไว้ที่นี่และใน CLAUDE.md ข้อ 4)

---

## 0. เริ่มที่บ้านต้องทำอะไรก่อน (สำคัญ)

งานที่ทำวันนี้ **ยังไม่ได้ขึ้น main** (main คือเว็บจริง) เพราะขั้นตอนขึ้นเว็บจริงต้องรอเจ้าของสั่ง จึงเก็บไว้ใน **กิ่ง (branch) ชื่อ `handoff-2026-09-21`** ที่ GitHub

1. เปิดเทอร์มินัลในโฟลเดอร์โปรเจกต์ที่บ้าน แล้วพิมพ์:
   ```bash
   git fetch origin
   git checkout handoff-2026-09-21
   ```
   (ถ้า `git pull origin main` เฉย ๆ จะไม่เห็นงานวันนี้)
2. เปิด Claude Code แล้ววางข้อความนี้ให้ Claude:
   > *"อ่าน CLAUDE.md กับ HANDOFF_TO_CLAUDE.md แล้วสรุปให้ฟังว่าตอนนี้ทำถึงไหน แล้วรอผมสั่งขั้นตอนต่อไป"*
3. ทดสอบโค้ดในกิ่งนี้ก่อนเริ่มงาน (ทุกชุดต้องขึ้น ALL PASSED):
   ```bash
   node -c app.js
   node firebase-rules/tests/untrusted-guard.test.js
   node firebase-rules/tests/owner-logic.test.js
   node firebase-rules/tests/admin-gp-revoke.test.js
   node firebase-rules/tests/rider-login.test.js
   node firebase-rules/tests/merchant-login.test.js
   ```
4. ตอนเจ้าของสั่งขึ้นเว็บจริง (พิมพ์ `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ`) ให้รวมกิ่งนี้เข้า main ก่อน: `git checkout main && git merge --ff-only handoff-2026-09-21` แล้วทำตามขั้นตอนในหัวข้อ 4 ของ CLAUDE.md (รัน `node -c app.js` ตรวจ tag เวอร์ชัน push ตรวจไฟล์เว็บจริงเทียบกับในเครื่อง)

---

## 1. สถานะตอนนี้ (สรุปสั้น)

| เรื่อง | สถานะ |
|---|---|
| เว็บจริง (main = `4dd1c21`, `app.js?v=10.31_login_error_msg`) | ✅ ใช้งานอยู่ มีระบบกันข้อความอันตราย + ไรเดอร์ล็อกอินด้วยรหัสลับ + กล่องแดงตอนใส่รหัสผิด |
| กฎฐานข้อมูลจริง | ✅ **v2.2 เผยแพร่แล้ว** (เจ้าของอนุญาต ตรวจแล้วด้วยการยิงคำขอที่ควรถูกปฏิเสธ 8 แบบ ถูกปฏิเสธครบ) |
| **รหัสลับของแผงค้า (Role 3)** | ⏳ เขียนและทดสอบเสร็จ อยู่ในกิ่ง `handoff-2026-09-21` และเว็บทดสอบ (`app.js?v=10.32b_merchant_secret_login`) **ยังไม่ขึ้นเว็บจริง** |
| เว็บทดสอบ https://hsong-test.web.app | ✅ เป็นเวอร์ชันล่าสุด (10.32b) กฎฐานข้อมูลทดสอบ = v2.2 ปลอดภัยแล้ว |

**คำเตือนจังหวะการขึ้น:** ทันทีที่โค้ดแผงค้าขึ้นเว็บจริง รหัส 6 หลักเดิมของร้านค้าจะใช้ไม่ได้ (ตั้งใจ เพราะเคยรั่วผ่านหน้าตรวจสถานะ) จนกว่าเจ้าของจะกด 🔑 สร้างรหัสผ่านให้แต่ละร้าน

---

## 2. งานที่ต้องทำต่อ ตามลำดับ

### ขั้นที่ 2: ส่งโค้ดแผงค้าขึ้นเว็บจริง
รอเจ้าของพิมพ์ `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` (ก่อนหน้านั้นห้าม push main) ทำตามข้อ 0 ข้อ 4 แล้วบอกเจ้าของว่า "ทำขั้นที่ 3 ได้แล้ว"

### ขั้นที่ 3: เจ้าของสร้างรหัสผ่านให้ร้านค้าจริง (เจ้าของทำเอง ต้องอธิบายละเอียด)
1. เปิดเว็บจริง กด Ctrl + Shift + R ล็อกอินเจ้าของ เข้าหน้าแอดมิน แท็บ "แผงค้า"
2. หาการ์ดร้าน มีป้ายเขียว "🏪 รหัสร้าน: APP-SHOP-xxxx" และปุ่ม "🔑 สร้างรหัสผ่าน" กดปุ่ม กด ตกลง
3. หน้าต่างรหัสขึ้นมา **แสดงครั้งเดียว** ต้องกด "คัดลอกรหัส & ข้อความ" แล้วส่งให้เจ้าของร้านทาง LINE/SMS ด้วยตัวเอง แล้วค่อยปิดหน้าต่าง
4. ทดสอบเข้าเมนู "3. แผงค้า" ใส่รหัสร้าน + รหัสผ่าน (ถ้าผิดต้องเห็นกล่องแดง)

### ข้อมูลทดสอบบนฐานข้อมูลจริงที่ต้องถามเจ้าของก่อนลบ (ผมยังไม่ยืนยัน)
- ร้าน `APP-SHOP-6758` (ชื่อ "ร้าน ส.ไก่สด") และ `stall_test_chicken` ("ร้านทดสอบ พร้อมลบออก") — **ยังไม่แน่ใจว่าร้าน ส.ไก่สดเป็นร้านจริงหรือร้านที่เจ้าของสร้างทดสอบ ให้ถามเจ้าของ** ห้ามลบเองโดยไม่ถาม
- ไรเดอร์ `RD3562` (เจ้าของตั้งรหัสผ่านแล้ว) และ `RD1987` (ใบสมัครทดสอบ)
- ไรเดอร์เก่าที่อนุมัติไว้ก่อนมีระบบรหัสผ่าน ต้องกด "🔑 รหัสเข้าระบบ" เพื่อสร้างรหัสก่อนจึงล็อกอินได้
- การลบข้อมูลบนฐานข้อมูลจริงต้องขออนุญาตเจ้าของก่อนเสมอ (กติกา CLAUDE.md ข้อ 4)

---

## 3. ทำอะไรไปบ้างในวันที่ 21 ก.ย. 2026 (เรียงตามเวลา)

1. ตรวจ Role 5 (แอดมิน): ตั้ง GP 10% ให้ค่าเดียวทั้งระบบ, แก้ข้อความ PIN เก่า, ถอนสิทธิ์ไรเดอร์เมื่อปฏิเสธ/ย้อนสถานะ
2. **กฎฐานข้อมูล v2** (ใครก็สมัครได้ เฉพาะเจ้าของอนุมัติ/สร้าง/ลบ) + เปลี่ยนการเขียนขึ้นคลาวด์เป็นทีละรายการ + ลบโค้ดที่เคยล้างออเดอร์/ไรเดอร์ทั้งระบบทุกครั้งที่มีเครื่องใหม่เข้าเว็บ
3. **ไรเดอร์ล็อกอินด้วยเลขไรเดอร์ + รหัสลับ 8 ตัว** (เก็บเฉพาะค่าเข้ารหัส) ปิดทางเข้าที่ไม่ใช้รหัสทุกทาง กฎ v2.1
4. **ป้องกัน Stored XSS:** ทดสอบกับโค้ดเก่าบน hsong-test พบว่าข้อความอันตรายในฐานข้อมูลรันโค้ดได้ 10 จุด แก้แล้วเหลือ 0 (ตัวกรองที่ประตูเดียว + ป้องกันที่ปลายทางกว่า 700 จุด) รายละเอียด CLAUDE.md หัวข้อ 5e
5. แก้ข้อความผิดพลาดตอนล็อกอินไรเดอร์ให้เป็นกล่องแดงใต้ปุ่ม (เดิมเป็นแถบเล็กที่หายเร็ว เจ้าของไม่เห็น)
6. **รหัสลับแผงค้า** (ข้อ 2 ตามแผน) เขียน ทดสอบ และเผยแพร่กฎ v2.2 บนเว็บจริง รายละเอียด CLAUDE.md หัวข้อ 5f
7. ซ่อนปุ่มลัดของแอดมิน (อนุมัติทันที/ไปหน้าแอดมิน/ศูนย์ฮับ) จากผู้สมัครร้านค้าทั่วไป

---

## 4. งานค้างต่อจากนี้ (เรียงตามที่แนะนำ)

1. **ระบบล็อกอินลูกค้า / ป้องกันข้อมูลลูกค้า (ใหญ่ที่สุด ควรทำก่อนมีลูกค้าจริง):** `orders` และ `carts` ยังเปิดอ่าน-เขียนได้ทุกคน (มีชื่อ เบอร์ ที่อยู่ลูกค้า) ต้องมีตัวตนลูกค้า (Firebase Auth เช่นเบอร์โทร/OTP) แล้วปรับกฎ
2. รหัสผ่านไรเดอร์/แผงค้าตรวจในเบราว์เซอร์เท่านั้น (ไม่ใช่ความปลอดภัยฝั่งเซิร์ฟเวอร์) ทางแก้ระยะยาวคือ Firebase Auth ให้ไรเดอร์/แผงค้า
3. ข้อสงสัยบั๊ก: `aggregateDailyOperations` อาจเขียนทับ `daily_reports/<วัน>` แล้วลบสถานะ "โอนแล้ว" (ยังไม่ยืนยัน ต้องตรวจก่อนพึ่งประวัติการโอนเงิน)
4. หน้า "ตั้งค่า" ของแอดมิน: มีผลจริงแค่ GP ที่เหลือบันทึกแต่ไม่มีที่ใช้ และเก็บในเครื่องเดียว
5. `promptPay` ของไรเดอร์อาจเป็นเลขบัตรประชาชน และเบอร์/LINE ยังเปิดสาธารณะ
6. ค่าส่งไรเดอร์ 40 บาท/เที่ยว ยังฮาร์ดโค้ดในรายงาน
7. ไรเดอร์ยังอนุมัติอัตโนมัติในฟอร์มเก่าบางทาง และไม่มีคนตรวจเอกสารก่อนรับงาน (ดู CLAUDE.md ข้อ 6)

---

## 5. เครื่องมือและวิธีทดสอบ (ไฟล์ชั่วคราวของเครื่องออฟฟิศไม่ตามมา ใช้สคริปต์ในโปรเจกต์แทน)

| ต้องการ | ใช้ |
|---|---|
| สร้างโฟลเดอร์เว็บทดสอบ (ชี้ hsong-test เท่านั้น) | `node firebase-rules/tools/build-test-site.js <โฟลเดอร์>` แล้วเข้าโฟลเดอร์นั้น `firebase deploy --only hosting --project hsong-test` |
| ส่งกฎไป hsong-test | สร้างโฟลเดอร์ชั่วคราวที่มี `firebase.json` = `{"database":{"rules":"rules.json"}}`, `.firebaserc` ชี้ hsong-test, `rules.json` = ไฟล์ `firebase-rules/hsong-test.rules.v2.json` แล้ว `firebase deploy --only database --project hsong-test` |
| ทดสอบกฎแบบคนแปลกหน้า (55 ข้อ) | ใส่ข้อมูลตั้งต้นก่อน `MSYS_NO_PATHCONV=1 firebase database:update / firebase-rules/tests/rules-anon.seed.json --project hsong-test --force` แล้ว `node firebase-rules/tests/rules-anon.test.js` |
| ตรวจข้อความที่ยังไม่ผ่าน escapeHtml ในโค้ดใหม่ | `firebase-rules/tools/scan-unescaped.js` (ต้องมี `acorn` `acorn-walk` ติดตั้งในโฟลเดอร์ชั่วคราว) |
| ตรวจว่าปุ่ม onclick ทุกปุ่มยังคอมไพล์ได้ | `firebase-rules/tools/check-handlers.js app.js` |
| จำลองการโจมตี XSS บน hsong-test | `firebase-rules/tools/seed-poison.js` (ต้องเปิดกฎทดสอบชั่วคราว และคืนกฎเดิมทุกครั้ง ขั้นตอนเต็มใน CLAUDE.md 5e) |
| สร้างกฎใหม่ | แก้ `firebase-rules/build-rules.js` เท่านั้น แล้ว `node firebase-rules/build-rules.js` (ห้ามแก้ไฟล์ .json ตรง ๆ) |

**ข้อควรระวังที่เจอจริง:**
- เจ้าของเห็น "ปุ่มกดแล้วไม่ได้ผล" เพราะเบราว์เซอร์จำหน้าเก่า → ให้กด **Ctrl + Shift + R** (ตอนขึ้นเว็บจริงหน้าเก่าอาจค้างได้ ~10 นาที)
- ทดสอบล็อกอินไรเดอร์/แผงค้า **ต้องออกจากระบบเจ้าของก่อน** และสมัครเป็นคนทั่วไป ถ้าสมัครตอนล็อกอินเจ้าของ ระบบสร้างไรเดอร์ให้ทันทีโดยไม่มีรหัสผ่าน
- ใส่รหัสผิด 5 ครั้งถูกล็อก 5 นาที (ไรเดอร์และแผงค้าแยกกัน)
- กฎทดสอบเปิดชั่วคราวทุกครั้งต้องคืนเป็น v2.2 แล้วรัน `rules-anon.test.js` ให้ผ่านครบ
- ใน Windows: ใช้ `MSYS_NO_PATHCONV=1` กับคำสั่ง firebase ที่มี `/`, ไฟล์ `CLAUDE.md` และ `index.html` เป็น CRLF (สคริปต์แก้ไฟล์ต้องรักษาบรรทัดใหม่), เครื่องมือแก้ไฟล์อาจใส่อักขระควบคุมจริงแทน ` ` ให้เขียนสคริปต์ Python แก้แทน
- ห้ามเปิดหน้าเว็บจริงในเบราว์เซอร์ของ Claude เพราะเปิดแล้วเชื่อมฐานข้อมูลจริงทันที (ใช้เว็บทดสอบแทน)
- ผมล็อกอินเจ้าของแทนไม่ได้ (ไม่มีรหัสผ่านและไม่ควรมี) ทางที่ได้ผล: ให้เจ้าของล็อกอินเองในหน้าต่างเบราว์เซอร์ของ Claude แล้วให้ Claude กดทดสอบต่อ

---

## 6. กฎเหล็ก (ย่อ ฉบับเต็มอยู่ CLAUDE.md)
1. `DEFAULT_COMMUNITY_RIDERS` ต้องเป็น `[]` เสมอ ห้ามมีไรเดอร์จำลอง
2. แก้ `app.js` ทุกครั้งต้องรัน `node -c app.js`
3. ฟังก์ชันที่เรียกจาก HTML ต้องผูก `window.*`
4. ข้อความจากฐานข้อมูล/ผู้ใช้ที่แสดงใน HTML ต้องผ่าน `escapeHtml(x)`; อาร์กิวเมนต์สตริงใน `onclick` ต้องใช้ `jsArg(x)` (ห้ามเขียน `'${x}'`)
5. โค้ดเขียนไรเดอร์/ร้านขึ้นคลาวด์ต้องเขียนทีละรายการ (`syncKeyedToCloud`) ห้าม `set()` ทั้งโหนด และวัตถุร้านที่สร้างจากใบสมัครต้องผ่าน `stallFromApp(app)`
6. เขียน/ลบ/เผยแพร่กฎบนฐานข้อมูลจริง (`hsong-1f342`) และ `git push` เข้า main ต้องขออนุญาตเจ้าของทุกครั้ง (ยกเว้นประโยคที่เจ้าของกำหนดไว้ใน CLAUDE.md)
