# 📋 เอกสารส่งต่องานสำหรับ Claude Code (TalatHub / Hsong)

**อัปเดตล่าสุด:** คืนวันที่ 20 กันยายน 2569 (ก่อนย้ายจากเครื่องที่บ้านไปเครื่องที่ออฟฟิศ)
**คลังซอร์สโค้ด:** https://github.com/pisaen666/hsong (branch `main` = เว็บจริงบน GitHub Pages: https://pisaen666.github.io/hsong/)
**อ่านคู่กัน:** [`CLAUDE.md`](CLAUDE.md) (กติกาโปรเจกต์ + ขอบเขตที่ Claude ทำเองได้) และ [`คู่มือสื่อสารกับ_Claude.md`](คู่มือสื่อสารกับ_Claude.md) (ประโยคสั่งงานของเจ้าของ)

> เจ้าของเป็นมือใหม่ สายตาไม่ดี: ตอบเป็นภาษาไทย ประโยคสั้น อธิบายศัพท์เทคนิค ทำงานสลับ 2 เครื่อง (บ้าน/ออฟฟิศ) ผ่าน GitHub เท่านั้น
> Claude จำบทสนทนาข้ามเครื่องไม่ได้ ข้อมูลทั้งหมดต้องอยู่ในไฟล์เอกสารนี้และ CLAUDE.md

---

## 1. สถานะล่าสุด (main = `d122f11` + commit เอกสารนี้)

| เรื่อง | สถานะ |
|---|---|
| รายงานการเงินแอดมิน | ✅ แก้แล้ว: ตัดออเดอร์ยกเลิก, ไม่นับค่าส่งด่วนซ้ำ |
| ความปลอดภัยฝั่งหน้าเว็บ | ✅ escape ข้อมูลผู้สมัครในหน้าอนุมัติ (แก้ XSS), แก้บั๊กปุ่ม "สมัครไรเดอร์" (modal ซ้อนผิดที่) |
| ฟอร์มสมัครไรเดอร์ | ✅ เพิ่ม: ผู้ติดต่อฉุกเฉิน, รูปเอกสาร (ตัวเอง/ใบขับขี่/เล่มทะเบียน/รูปรถ), บัญชีรับเงิน 2-3 บัญชี, ยินยอม PDPA (ทั้งฟอร์มหน้าหลักและ modal) |
| ล็อกอินเจ้าของ (Firebase Auth) | ✅ ขึ้นเว็บจริงแล้ว: บัญชีอีเมล/รหัสผ่านบัญชีเดียวใช้ทั้ง Admin (Role 5) และ Hub (Role 2) แทน PIN `admin6305`/`hb6305` (ลบแล้ว รวมปุ่มโหมดทดสอบ) |
| ข้อมูลส่วนตัวไรเดอร์ | ✅ เก็บที่ `rider_private/<id>` + รูปที่ `rider_documents/<id>` เจ้าของอ่านได้คนเดียว ไม่เก็บใน localStorage ไม่ขึ้นโหนดสาธารณะ |
| กฎ Firebase ของจริง | ✅ วางแล้ว (ไฟล์ `firebase-rules/hsong-1f342.rules.json`) โหนดทั่วไปยังเปิดสาธารณะ **จนหมดอายุ 2 พ.ย. 2569 (`1793552400000`)** |
| ทดสอบล็อกอินบนเว็บจริงด้วยบัญชีจริง | ⏳ **เจ้าของยังไม่ได้ยืนยันผล** (ล็อกอินบนโปรเจกต์ทดสอบผ่านแล้ว บนเว็บจริงยังไม่ทดสอบ) |

## 2. Firebase 2 โปรเจกต์

- **จริง `hsong-1f342`**: Authentication เปิด Email/Password, มีบัญชีเจ้าของ 2 บัญชี (หลัก UID `hloZD0Flzyd0X8CCq6RuDV3GYjQ2`, สำรอง UID `DIhgzYpqR8c3m8SIBXvQ6h8PqsL2`) ปิด sign-up แล้ว UID เหล่านี้อยู่ใน `firebase-config.js` (`OWNER_UIDS`) และในกฎ ถ้าเจ้าของเสียบัญชีทั้งคู่ จะเข้า Admin/Hub ไม่ได้ ต้องสร้างผู้ใช้ใหม่ใน Console แล้วเพิ่ม UID ทั้งใน `OWNER_UIDS` และกฎ
- **ทดสอบ `hsong-test`**: Spark plan, กฎ = `firebase-rules/hsong-test.rules.json`, UID ทดสอบอยู่ในไฟล์นั้น มีข้อมูลขยะจากการทดสอบของ Claude (ลบได้ตามสะดวก)
- วิธีทดสอบด้วยตัวล็อกอินจริง: ดูหัวข้อ 5b ใน `CLAUDE.md` Claude **ล็อกอินแทนเจ้าของไม่ได้** (ไม่กรอกรหัสผ่านให้) ให้เจ้าของทดสอบเอง
- ข้อมูลจริงมีไรเดอร์ตัวอย่าง `RD3562` ที่เจ้าของสร้างจากปุ่มทดสอบ (ลบเองได้ผ่าน Console)

## 3. งานค้างตามลำดับความสำคัญ

1. **ยืนยันล็อกอินบนเว็บจริง** (เจ้าของ): เปิดเว็บ กด Ctrl+F5 กดปุ่ม Admin ใส่อีเมลกับรหัสผ่านของโปรเจกต์จริง เข้าหน้าแอดมินและฮับ แล้วออกจากระบบ (ห้ามสมัครไรเดอร์/สั่งของ เพื่อไม่ให้ข้อมูลทดสอบเข้าระบบจริง) ถ้าเจอรหัสข้อผิดพลาดเช่น `auth/unauthorized-domain` ให้เพิ่ม `pisaen666.github.io` ใน Authentication → Settings → Authorized domains
2. **ก่อน 2 พ.ย. 2569 (ด่วนที่สุดหลังข้อ 1)**: กฎแบบเปิดสาธารณะจะหมดอายุและเว็บจะอ่าน/เขียน DB ไม่ได้ ต้องเลือกทางใดทางหนึ่ง: (ก) ต่อวันหมดอายุอีกรอบ (ซื้อเวลา) หรือ (ข) ทำขั้นถัดไปของแผนความปลอดภัย
3. **ขั้นถัดไปของแผนความปลอดภัย**: ข้อมูลที่ยังเปิดสาธารณะ = ใบสมัครแผงค้า `merchant_applications` (มีบัญชีธนาคาร), `orders` (ชื่อ ที่อยู่ เบอร์ลูกค้า), `carts` (ใช้เบอร์โทรเป็นชื่อโหนด), และในโหนดไรเดอร์สาธารณะยังมี เบอร์โทร, LINE ID, `promptPay` (ถ้าไรเดอร์ใช้เลขบัตรเป็นพร้อมเพย์ เลขนั้นหลุด) แนวทาง: แผงค้าย้ายข้อมูลลับไปโหนดส่วนตัวแบบเดียวกับ `rider_private`, ออเดอร์/ตะกร้าใช้ล็อกอินแบบไม่ระบุตัวตน (ขั้น 2), ระบบตรวจ PIN ไรเดอร์/แผงค้าที่ฝั่งเซิร์ฟเวอร์ต้องใช้ Cloud Functions + แพลน Blaze (ผูกบัตร ต้องให้เจ้าของตัดสินใจ) ตอนนี้ **PIN ของไรเดอร์ (`accessCode`) ยังอ่านได้ในโหนดสาธารณะ** ใครก็ล็อกอินแทนไรเดอร์ได้
4. **คำถามธุรกิจที่รอเจ้าของตัดสินใจ**: (ก) GP 5% (เอกสารเก่า) หรือ 10% (โค้ด) และตั้ง 0% ไม่ได้ผล (รายงานเปลี่ยนเป็น 10% เงียบ ๆ) (ข) ค่ารอบไรเดอร์ในรายงานตายตัว ฿40 ไม่ตามค่าที่ตั้งในหน้าจัดการไรเดอร์ (ค) ฟอร์มสมัครไรเดอร์ทั้งสองแบบ **อนุมัติอัตโนมัติ** ไรเดอร์รับงานได้ทันทีโดยไม่มีใครตรวจรูปเอกสาร ถ้าต้องการให้เป็น "รอตรวจ" ต้องแก้
5. ปุ่มแก้ไขใบสมัครของแอดมินยังแก้ข้อมูลใหม่ (บัญชี ผู้ติดต่อฉุกเฉิน) ไม่ได้
6. อัปเดตความจำ: `database.rules.json` ในเครื่อง (อยู่ใน `.gitignore`) ล้าสมัย ใช้ไฟล์ใน `firebase-rules/` เป็นหลัก

## 4. บทเรียนที่ต้องจำ (ทำพลาดมาแล้ว)

- **แอปมีกลไก "ถ้าคลาวด์ว่าง ให้ส่งข้อมูลในเครื่องกลับขึ้นคลาวด์"** โหลดหน้าซ้ำโดยมีข้อมูลทดสอบใน localStorage → ข้อมูลรั่วขึ้น DB จริง และลบใน Console แล้วข้อมูลกลับมาถ้ามีแท็บเปิดค้าง (ทั้งกรณี DB จริง และ hsong-test) ก่อนลบข้อมูลใน Console ให้ปิดทุกแท็บของเว็บก่อน
- ห้ามเขียน/ลบ Firebase จริงโดยไม่ถามเจ้าของ ทดสอบเขียนข้อมูลกับ `hsong-test` หรือสำเนาที่ชี้ host ปลอม (`127.0.0.1:9`) เท่านั้น
- `await db.ref().set()` ของ Firebase SDK ค้างเงียบ ๆ เมื่อออฟไลน์ ต้องมี timeout (`_withTimeout`)
- โค้ดใหม่ที่เขียนข้อมูลไรเดอร์/ใบสมัครลง storage หรือ Firebase ต้องผ่าน `stripRiderPrivate*` เสมอ
- หน้า GitHub Pages ถูกเก็บในเบราว์เซอร์ 10 นาที ต้องเลื่อนเลขเวอร์ชัน `app.js?v=` และ `firebase-config.js?v=` ใน `index.html` ทุกครั้งที่ขึ้นเว็บ
- ลำดับขึ้นเว็บที่ใช้ได้: วางกฎก่อน → merge/push โค้ด → ตรวจว่าเว็บเสิร์ฟไฟล์ตรงกับในเครื่อง

## 5. เริ่มงานที่เครื่องใหม่ (ออฟฟิศ)

1. ถ้ายังไม่มีโฟลเดอร์โปรเจกต์: `git clone https://github.com/pisaen666/hsong.git` (เครื่องต้องมี Node.js และ Git) แล้วเปิด Claude Code ในโฟลเดอร์ `hsong`
2. ถ้ามีอยู่แล้ว ให้ Claude ดึงโค้ดล่าสุด (`git pull origin main`)
3. เจ้าของพิมพ์: `ดึงโค้ดล่าสุดจาก GitHub แล้วอ่าน CLAUDE.md กับ HANDOFF_TO_CLAUDE.md แล้วสรุปสถานะให้ฟัง`
4. ไม่ตามมากับ GitHub: ประวัติแชท ความจำของ Claude, `firebase-rtdb-export.json` (สั่ง `ซิงค์ Firebase ด้วย` เพื่อดึงใหม่ สคริปต์ดึงทีละโหนดและข้ามข้อมูลลับ), `database.rules.json`
5. รหัสผ่านบัญชีเจ้าของอยู่กับเจ้าของเท่านั้น Claude ไม่เห็นและไม่ขอ

## 6. กฎเหล็กของโปรเจกต์ (ย่อ) — รายละเอียดใน `CLAUDE.md`

1. `DEFAULT_COMMUNITY_RIDERS` ต้องเป็น `[]` เสมอ
2. แก้ `app.js` ทุกครั้งต้องรัน `node -c app.js` ก่อนจบงาน
3. ฟังก์ชันที่เรียกจาก HTML ต้องผูก `window.*`
4. รักษา session ข้ามบทบาท (ยกเว้นแอดมิน/ฮับ ที่มาจาก Firebase Auth เท่านั้น ไม่เก็บใน localStorage)
5. modal หลักใช้ `z-[999999]`
6. push `main` เฉพาะเมื่อเจ้าของสั่ง (ประโยค `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` หรือสั่งตรง ๆ)
