# 📋 เอกสารส่งต่องาน (Hand-off) — TalatHub / Hsong

**อัปเดตล่าสุด:** 26 กันยายน 2026 — **เว็บจริงใช้ `app.js?v=10.64_merchant_doc_verify` + `styles.css?v=10.05_toast_fix` (commit `7803be4`) + กฎฐานข้อมูล v6** ไฟล์บนเว็บจริงตรงกับในเครื่อง (เช็กแล้ว 26 ก.ย., GitHub deploy = success) ไม่มีโค้ดค้าง
**คลังโค้ด:** https://github.com/pisaen666/hsong  |  **เว็บจริง:** https://pisaen666.github.io/hsong/  |  **เว็บทดสอบ:** https://hsong-test.web.app
**ไฟล์กติกาของ Claude:** `CLAUDE.md` (อ่านอัตโนมัติ) — รายละเอียดทุกฟีเจอร์อยู่ในหัวข้อ 5a-5z4 เอกสารนี้เป็นแค่สรุปย่อ + สิ่งที่เปลี่ยนล่าสุด

> สำหรับ Claude เครื่องถัดไป: เจ้าของเป็นมือใหม่ สายตาไม่ดี ให้ตอบเป็นภาษาไทย คำง่าย และอธิบายทุกขั้นตอนที่ให้เจ้าของทำแบบละเอียดเป็นข้อ ๆ (ดู CLAUDE.md ข้อ 4)

---

## 0. เริ่มงานรอบหน้าต้องทำอะไรก่อน

1. เปิดเทอร์มินัลในโฟลเดอร์โปรเจกต์ แล้วพิมพ์:
   ```bash
   git checkout main
   git pull origin main
   ```
2. เปิด Claude Code แล้ววางข้อความนี้ให้ Claude:
   > *"ดึงโค้ดล่าสุดจาก GitHub แล้วอ่าน CLAUDE.md กับ HANDOFF_TO_CLAUDE.md แล้วสรุปสถานะให้ฟัง"*
3. ทดสอบโค้ดก่อนเริ่มงาน (ตอนนี้มี **34 ไฟล์ทดสอบ** ทุกไฟล์ต้องขึ้น ALL PASSED หรือ SKIPPED):
   ```bash
   node -c app.js
   for f in firebase-rules/tests/*.test.js; do node "$f" >/dev/null 2>&1 || echo "FAILED: $f"; done
   ```
   - `rules-anon.test.js` ต่อเน็ตไป hsong-test (ต้อง seed ก่อน ดูหัวไฟล์) และ `rules-v3/v4/v5/v6-live.test.js` ข้ามเอง ถ้าไม่ใส่ `LIVE=1` (กฎที่ใช้บน hsong-test ตอนนี้ = v6 ดังนั้น v3-live จะล้มถ้ารัน — ใช้กับ v3 เท่านั้น)
   - เครื่อง Windows ที่ `core.autocrlf=true`: `checkout-gate` และ `untrusted-guard` ล้มเพราะ CRLF ไม่ใช่เพราะโค้ดผิด — ลองรันบนสำเนาที่แปลงเป็น LF ก่อนสรุป (`merchant-login` แก้ให้แปลง CRLF เองแล้ว 26 ก.ย.; ใช้วิธีเดียวกันกับ 2 ไฟล์นั้นได้ถ้าเจ้าของสั่ง)
   - 26 ก.ย.: `rules-anon.test.js` ผ่าน 75 ล้ม 1 ข้อ ("ใบสมัครที่มีรหัสผ่านแล้ว: แก้ฟิลด์อื่นโดย loginHash เดิม ได้" ได้ 401) — น่าจะเพราะข้อมูล seed บน hsong-test ไม่ครบ/ถูกล้างไป ยังไม่ได้ตรวจยืนยัน ลอง seed ใหม่ตามหัวไฟล์ก่อนสรุปว่ากฎผิด
   - ล้างข้อมูลทดสอบ "ZZ*" บน hsong-test: `node firebase-rules/tools/clean-zz-test-data.js`
4. **ไม่มีงานโค้ดค้าง** — ทุกอย่างขึ้นเว็บจริงและตรวจแล้ว (ค่ารอบไรเดอร์ / ตรวจรูปก่อนอนุมัติร้าน / กล่องแจ้งเตือน toast = CLAUDE.md 5z5-5z7 ขึ้นเว็บจริง 25 ก.ย. เย็น ตรวจไฟล์ตรงกันแล้ว 26 ก.ย.)
5. **เรื่องที่พักไว้ / รอเจ้าของตอบ** (ถามเจ้าของก่อน ไม่ต้องทำเอง):
   - **เบอร์ฮับ / พร้อมเพย์ฮับ / บัญชีรับเงิน** — เจ้าของขอพักไว้ เพราะจะเปลี่ยนเบอร์เร็ว ๆ นี้ รอเจ้าของบอกเบอร์ใหม่ (รายละเอียดในหัวข้อ 2a บรรทัด "พักไว้ตามที่เจ้าของขอ")
   - ข้อเสนอเล็ก (ไม่ต้องรอเบอร์): เวลาร้าน/ไรเดอร์ไม่มีเลขพร้อมเพย์ ให้ขึ้นเตือนแทนการใส่เบอร์สมมติ 089-123-4567 ในหน้าโอนเงิน — เจ้าของยังไม่ได้ตอบ
   - ลบตะกร้าเก่า 8 รายการบนเว็บจริงที่ใช้เบอร์โทรเป็นชื่อหัวข้อ (ตอนนี้เห็นได้เฉพาะเจ้าของแล้ว) — ต้องขออนุญาตก่อนลบ
   - "เตรียมออเดอร์ทดสอบระบบชั่งน้ำหนัก" บน hsong-test ให้เจ้าของลองด้วยมือ — ยังไม่ได้ตอบ
   - ย้ายกติกาไป `AGENTS.md` — เจ้าของตัดสินใจแล้วว่ายังไม่ทำ
   - หน้าตั้งค่าค่ารอบมี 2 หน้า: หน้าเก่า "ตั้งค่าระบบ > 4. ไรเดอร์" (`hsong_hub_settings.riderBaseFare`) ไม่มีโค้ดไหนอ่านเลย — จะลบไหม ยังไม่ได้ตอบ (CLAUDE.md 5z5)
   - กล่องแจ้งเตือน (toast) ที่เพิ่งแก้ ยังไม่มีใครดูบนมือถือจริงว่าตำแหน่ง/ขนาดโอเค (CLAUDE.md 5z7)
6. **Claude ส่งขึ้นเว็บจริงเองไม่ได้:** ระบบความปลอดภัยของ Claude Code บล็อก `git push` และ `firebase deploy` ไปโปรเจกต์จริง → ให้เจ้าของกด Run เอง
   - ใน PowerShell ของเจ้าของต้องพิมพ์ `firebase.cmd` (พิมพ์ `firebase` เฉย ๆ ติด Execution Policy ของ Windows)
   - ไฟล์กฎสำหรับ deploy ต้องอยู่ในโฟลเดอร์ชั่วคราวที่มี `firebase.json` = `{"database":{"rules":"rules.json"}}` แล้วสั่ง `firebase.cmd deploy --only database --project hsong-1f342 --config "<โฟลเดอร์>irebase.json"` (โฟลเดอร์ scratchpad ของเครื่องออฟฟิศใช้ที่บ้านไม่ได้ ต้องสร้างใหม่)
   - ขั้นตอนขึ้นเว็บที่ใช้ได้ทุกรอบวันนี้: เจ้าของ push โค้ด → Claude ตรวจไฟล์บนเว็บจริงตรงกับในเครื่อง → รอ 10 นาที → เจ้าของ deploy กฎ → เจ้าของล็อกอินเจ้าของบนเว็บจริงเพื่อให้ระบบสร้างข้อมูลสาธารณะ → Claude ตรวจด้วย GET แบบอ่านอย่างเดียว

---

## 1. สถานะตอนนี้ (สรุปสั้น)

| เรื่อง | สถานะ |
|---|---|
| เว็บจริง (`main`, commit `7803be4`) | ✅ `app.js?v=10.64_merchant_doc_verify` ตรวจแล้วตรงกับในเครื่อง (26 ก.ย.) |
| กฎฐานข้อมูลจริง | ✅ **v6** — ออเดอร์/ตะกร้าปิดจากคนแปลกหน้า, ไรเดอร์/แม่ค้าเห็นเฉพาะงานของตัวเอง, ข้อมูลส่วนตัวไรเดอร์และร้านค้าซ่อนแล้ว, คนแปลกหน้าแก้ข้อมูลร้านไม่ได้; ถอยกลับ = `firebase-rules/hsong-1f342.rules.v5.json` |
| กฎบน hsong-test | v6 (เหมือนเว็บจริง) |
| บัตรผ่าน (Anonymous + sign-up) | ✅ เปิดแล้วทั้ง `hsong-test` และ `hsong-1f342` |
| ไรเดอร์/ร้านจริง | `RD3562` และ `APP-SHOP-6758` (ร้านทดสอบของเจ้าของ ห้ามลบ) ล็อกอินด้วยรหัสผ่านที่เจ้าของสร้างวันนี้ได้ |
| คู่มือผู้สมัคร | ✅ `guide-rider.html` และ `guide-merchant.html` (CLAUDE.md 5v, 5w) |

**บทเรียนสำคัญวันนี้:** หลังปิดสิทธิ์ "อ่าน" ของโหนดไหน ต้องทดสอบทุกทางที่คนทั่วไป "เขียน" ผ่าน `syncKeyedToCloud` ด้วย (v5 เคยทำให้ใบสมัครไรเดอร์ใหม่ไม่ถูกบันทึกเงียบ ๆ ~1 ชั่วโมง — แก้แล้ว ไม่มีข้อมูลหาย)

---

## 2a. งานวันที่ 25 ก.ย. 2026 — "บัตรผ่าน" ปิดข้อมูลลูกค้า (ขึ้นเว็บจริงแล้ว, CLAUDE.md 5z)

- ทุกเครื่องได้บัตรผ่านนิรนามอัตโนมัติ ออเดอร์ใหม่ผูกกับเครื่องที่สั่ง ลูกค้าเห็นเฉพาะของตัวเอง ลิงก์ติดตามตรวจรหัส 6 ตัวที่ฐานข้อมูล
- ไรเดอร์/แม่ค้าได้สิทธิ์อ่านออเดอร์เมื่อฐานข้อมูลตรวจรหัสผ่านแล้ว (`staff_keys` / `staff_sessions`) เจ้าของสร้างรหัสใหม่ให้ `APP-SHOP-6758` และ `RD3562` แล้ว
- ทดสอบ: `order-access.test.js` (ออฟไลน์), `LIVE=1 node firebase-rules/tests/rules-v3-live.test.js` (40 ข้อกับ hsong-test), `rules-anon.test.js` 60 ข้อ
- **ระบบบล็อก Claude ไม่ให้ `git push` และ `firebase deploy` ไปเว็บจริงเอง** → เจ้าของกด Run เอง; ใน PowerShell ของเจ้าของต้องใช้ `firebase.cmd` (พิมพ์ `firebase` เฉย ๆ ติด Execution Policy)
- ลบข้อมูลขยะ "ZZ*" ใน hsong-test แล้ว ถ้าลบข้อมูลทดสอบอีก ต้องให้เจ้าของล้างข้อมูลเว็บทดสอบใน Chrome ก่อน (Site settings > Delete data) ไม่งั้นเบราว์เซอร์ส่งของเก่ากลับขึ้นไป
- **รอบ 2 (v4, CLAUDE.md 5z2):** ไรเดอร์เห็นใบงานย่อ กดรับ/คืนงานได้, ฮับจ่ายงาน + เตือนงานค้างเกิน 10 นาที, แม่ค้าเห็นแค่ของที่ต้องเตรียม, ลบไรเดอร์/ออเดอร์ปลอมในโค้ด; ล้างข้อมูลทดสอบด้วย `node firebase-rules/tools/clean-zz-test-data.js`
- **รอบ 3 (v5, CLAUDE.md 5z3):** รายชื่อไรเดอร์/ใบสมัครอ่านได้เฉพาะเจ้าของ, ล็อกอินไรเดอร์ตรวจรหัสที่ฐานข้อมูล (`rider_public`), เช็กสถานะด้วยเบอร์ผ่านค่าแฮช, ปิดโหนดเก่า 4 โหนด (ระบบไม่เคยเก็บ GPS จริงของไรเดอร์ — ตำแหน่งบนแผนที่เป็นแบบจำลอง)
- **รอบ 4:** แก้บั๊กใบสมัครไรเดอร์ไม่ถูกบันทึกหลัง v5 (`c07689f`, CLAUDE.md 5z3) + **v6 ซ่อนข้อมูลร้านค้า** (CLAUDE.md 5z4): เบอร์ร้านเห็นเฉพาะไรเดอร์ที่ล็อกอิน, การ์ดร้านแสดงรูป+ชื่อเล่น, คนแปลกหน้าแก้ข้อมูล/บัญชีร้านไม่ได้แล้ว
- **พักไว้ตามที่เจ้าของขอ (25 ก.ย.):** เบอร์ฮับ/พร้อมเพย์ฮับ/บัญชีรับเงิน — เจ้าของจะเปลี่ยนเบอร์เร็ว ๆ นี้ จึงยังไม่อยากแก้ ไว้เจ้าของบอกเบอร์ใหม่ค่อยทำทีเดียว. เลขที่ต้องแก้: 089-123-4567 (ติดต่อฮับบนใบเสร็จ/สลิป/ประกาศ ~20 จุด), 081-999-8888 (พร้อมเพย์ฮับรับค่าส่งงานด่วน + ปุ่ม "โทรติดต่อฮับกลาง" ~10 จุด), บัญชี SCB 4111305737 (ลูกค้าโอน, ยังไม่ยืนยันว่าเป็นของเจ้าของ), รูป `promptpay_qr.png`. แผน: รวมไว้ที่เดียวในโค้ด. กลุ่มที่แก้ได้โดยไม่ต้องรอเบอร์: โค้ดใส่ 089-123-4567 แทนเลขพร้อมเพย์ของร้าน/ไรเดอร์ที่ว่าง (`openVendorPayoutModal` ~7852, `openSingleRiderPayoutModal` ~25376) -> ควรเตือนแทน (เสนอเจ้าของแล้ว ยังไม่ได้ตอบ); (ช่อง "-" ในผังแผง / คอลัมน์เลขแผง / หัวคอลัมน์ "รหัสผ่าน 6 หลัก" แก้แล้วใน 10.63); ตะกร้าเก่าที่ใช้เบอร์เป็นชื่อหัวข้อยังค้างในฐานข้อมูลจริง (เจ้าของเห็นคนเดียวแล้ว — ลบได้ถ้าเจ้าของอนุญาต)

## 2. งานที่ทำวันที่ 24 ก.ย. 2026 (เครื่องออฟฟิศ) — ขึ้นเว็บจริงทั้งหมดแล้ว

1. **คู่มือไรเดอร์** หน้าเว็บตัวหนังสือใหญ่ + รูป 20 รูป + ปุ่ม 📖 ในหน้าสมัคร/ล็อกอิน/หน้ารับงาน ปุ่มสมัครเปลี่ยนเป็น "ส่งใบสมัคร (รอเจ้าของอนุมัติ)" ลบคำโฆษณาโบนัสที่ไม่มีจริง — CLAUDE.md 5v
2. **คู่มือร้านค้า** แบบเดียวกัน + แก้ของเหลือจากการเอาเลขแผงออก ("รหัส 6 หลัก", ขีด "-" ลอย) — CLAUDE.md 5w
3. **บั๊กเงิน (สำคัญ):** หลายจุดคิดเงินแค่ราคาต่อหน่วย ไม่คูณจำนวน — ฮับโอนเงินให้ร้านขาด, เงินคืนลูกค้าเมื่อของหมดขาด, ใบเสร็จผิด ตอนนี้ใช้ `orderItemLineTotal` / `merchantStallItemsTotal` ทุกจุด — CLAUDE.md 5x
4. **บังคับกรอกบัญชีรับเงินของร้าน** (สมัครใหม่และแก้ไข) และลบปุ่ม "ไรเดอร์ (Role 4)" ในหน้าส่งใบสมัครร้าน — CLAUDE.md 5x
5. **ของชั่งน้ำหนัก** (เจ้าของเลือก: แม่ค้าพิมพ์น้ำหนัก / ชั่งได้น้อยคืนส่วนต่างใส่ซอง / ชั่งได้มากไม่เก็บเพิ่ม) + **แก้บั๊กปุ่ม "แจ้งหมด" และ "เตรียมของเสร็จแล้ว" ของแม่ค้าที่บนมือถือจริงไม่บันทึกอะไรเลย** ตอนนี้ส่งขึ้นคลาวด์เฉพาะช่องที่เปลี่ยน — CLAUDE.md 5y

**สิ่งที่ต้องรู้:** บนเครื่องออฟฟิศ (Windows, `core.autocrlf=true`) ตัวทดสอบ 3 ไฟล์ (`checkout-gate`, `merchant-login`, `untrusted-guard`) ล้มเพราะไฟล์เป็น CRLF ไม่ใช่เพราะโค้ดผิด — รันบนสำเนาที่แปลงเป็น LF แล้วผ่านทั้งหมด (ยังไม่ได้แก้ตัวทดสอบ เจ้าของยังไม่ได้สั่ง)

**รูปในคู่มือถ่ายด้วยสคริปต์:** `puppeteer-core` + Edge แบบไม่มีหน้าจอ จากสำเนาทดสอบที่ชี้ host ปลอม (`127.0.0.1:9`) ข้อมูลปลอมทั้งหมด สคริปต์อยู่ในโฟลเดอร์ชั่วคราวของ session ไม่ได้อยู่ใน repo — วิธีทำอยู่ใน CLAUDE.md 5v/5w (หน้าต่างสมัครร้านต้องใช้จอสูง 390x4200)

---

## 3. บทเรียนที่ต้องจำ (ทำพลาด/เจอปัญหามาแล้ว)

1. **GitHub Pages deploy ล้มเหลวได้แม้ push สำเร็จและ build ผ่าน** (เจอครั้งแรก 23 ก.ย. คืน) — วิธีตรวจสอบแบบไม่ต้องล็อกอิน: `curl -s "https://api.github.com/repos/pisaen666/hsong/actions/runs?per_page=1"` ดูฟิลด์ `conclusion` ของ job "deploy" (ไม่ใช่แค่ "build") ถ้าเจอ `"conclusion": "failure"` ที่ job deploy ให้ `git commit --allow-empty -m "..."` แล้ว push ใหม่เพื่อกระตุ้น deploy รอบใหม่ (ไม่ต้องแก้โค้ด เพราะโค้ดไม่ได้ผิด)
2. **แอปมีกลไก "ถ้าคลาวด์ว่าง ให้ส่งข้อมูลในเครื่องกลับขึ้นคลาวด์"** โหลดหน้าซ้ำโดยมีข้อมูลทดสอบใน localStorage → ข้อมูลรั่วขึ้น DB จริง ก่อนลบข้อมูลใน Console ให้ปิดทุกแท็บของเว็บก่อน
3. ห้ามเขียน/ลบ Firebase จริงโดยไม่ถามเจ้าของ ทดสอบเขียนข้อมูลกับ `hsong-test` หรือสำเนาที่ชี้ host ปลอม (`127.0.0.1:9`) เท่านั้น
4. หน้า GitHub Pages ถูกเก็บในเบราว์เซอร์ 10 นาที ต้องเลื่อนเลขเวอร์ชัน `app.js?v=` ใน `index.html` ทุกครั้งที่ขึ้นเว็บ (ถ้า `app.js` ไม่เปลี่ยน ไม่ต้องเลื่อนเลข)
5. ลำดับขึ้นเว็บที่ใช้ได้: วางกฎก่อน (ถ้ามีเปลี่ยน) → merge/push โค้ด → ตรวจว่าเว็บเสิร์ฟไฟล์ตรงกับในเครื่องด้วย `curl`/`diff` (ระวังเรื่อง CRLF ในเครื่อง Windows ใช้ `diff --strip-trailing-cr`)

---

## 4. งานค้างต่อจากนี้ (ไม่เปลี่ยนจากที่ CLAUDE.md หัวข้อ 6 บอกไว้ — สรุปย่อเฉพาะที่สำคัญ)

1. ~~ป้องกันข้อมูลลูกค้า `orders`/`carts`~~ ✅ และ ~~ไรเดอร์/แม่ค้าเห็นเฉพาะงานของตัวเอง~~ ✅ เสร็จ 25 ก.ย. (ข้อ 2a)
2. ~~ค่ารอบไรเดอร์ในรายงานยังฮาร์ดโค้ด 40 บาท ไม่ตามค่าที่ตั้งจริง~~ ✅ แก้แล้ว 25 ก.ย. (commit `a2f1593`, `getRiderTripFee()`) — ขึ้นเว็บจริงแล้ว
3. ~~เอกสารร้านค้ายังไม่มีคนตรวจก่อนอนุมัติ (ต่างจากไรเดอร์ที่ตรวจแล้ว)~~ ✅ แก้แล้ว 25 ก.ย. (CLAUDE.md 5z6, `getMissingMerchantVerificationItems()`, commit `e39614d`) — ขึ้นเว็บจริงแล้ว
4. ฮับ/ลูกค้ายังเขียนออเดอร์ทั้งก้อน (`syncOrderToCloud`) อาจทับสิ่งที่แม่ค้าเพิ่งบันทึก (CLAUDE.md 5y)
   - ~~`merchant_applications` (มีเลขบัญชีร้าน) ยังเปิดอ่านได้ทุกคน~~ — **ข้อความนี้เป็นข้อมูลเก่า ไม่จริงแล้ว** ตรวจสอบซ้ำ 25 ก.ย. (อ่าน `firebase-rules/hsong-1f342.rules.v6.json` + ยิง GET จริงแบบไม่ล็อกอินไปที่เว็บจริง) พบว่า rules v6 (CLAUDE.md 5z4, ขึ้นเว็บจริงแล้ว) ปิดสิทธิ์อ่าน `merchant_applications` ให้เฉพาะเจ้าของ/ร้านนั้นเองแล้ว และ `custom_market_stalls` ที่เปิดอ่านได้สาธารณะก็ไม่มีเลขบัญชี/เบอร์/ชื่อจริงหลงเหลือ (ผ่าน `sanitizeStallForPublic`) — เรื่องนี้เสร็จสมบูรณ์แล้ว ไม่ต้องทำซ้ำ
5. สงสัยบั๊ก: `aggregateDailyOperations` อาจเขียนทับ `daily_reports/<วัน>` แล้วลบสถานะ "โอนแล้ว" — ยังไม่ยืนยัน
6. ไรเดอร์จริง `RD3562` เคยเห็นสถานะเด้งกลับ "รออนุมัติ" ระหว่างทดสอบ — ยังไม่ได้ตามเรื่องนี้ต่อ
7. รายละเอียดเต็มทุกข้อ + ประวัติฟีเจอร์ทั้งหมด → อ่าน `CLAUDE.md` หัวข้อ 5a-5y และหัวข้อ 6

---

## 5. เครื่องมือและวิธีทดสอบ (ย่อ — ฉบับเต็มอยู่ CLAUDE.md หัวข้อ 5)

| ต้องการ | ใช้ |
|---|---|
| รันชุดทดสอบทั้งหมด (34 ไฟล์) | `for f in firebase-rules/tests/*.test.js; do node "$f" || echo "FAILED: $f"; done` |
| ทดสอบ UI แบบไม่แตะฐานข้อมูลจริงเลย | คัดลอกไฟล์ไปโฟลเดอร์ชั่วคราว แทนที่ host จริงด้วย `127.0.0.1:9` ใน `app.js`/`firebase-config.js`/`index.html` แล้ว `PORT=3001 node server.js` (ดูสูตรเต็มใน CLAUDE.md หัวข้อ 5) |
| ตรวจว่าเว็บจริงขึ้นตรงกับในเครื่องหลัง push | `curl -s https://pisaen666.github.io/hsong/app.js -o /tmp/live.js && diff --strip-trailing-cr /tmp/live.js app.js` |
| ตรวจสถานะ deploy ของ GitHub Pages (ไม่ต้องล็อกอิน) | `curl -s "https://api.github.com/repos/pisaen666/hsong/actions/runs?per_page=1"` ดูฟิลด์ `conclusion` |

---

## 6. กฎเหล็กของโปรเจกต์ (ย่อ) — รายละเอียดใน `CLAUDE.md`
1. `DEFAULT_COMMUNITY_RIDERS` ต้องเป็น `[]` เสมอ ห้ามมีไรเดอร์จำลอง
2. แก้ `app.js` ทุกครั้งต้องรัน `node -c app.js` ก่อนจบงาน
3. ฟังก์ชันที่เรียกจาก HTML ต้องผูก `window.*`
4. ข้อความจากฐานข้อมูล/ผู้ใช้ที่แสดงใน HTML ต้องผ่าน `escapeHtml(x)`; อาร์กิวเมนต์สตริงใน `onclick` ต้องใช้ `jsArg(x)`
5. โค้ดเขียนไรเดอร์/ร้านขึ้นคลาวด์ต้องเขียนทีละรายการ (`syncKeyedToCloud`/`syncListToCloud`) ห้าม `set()`/`remove()` ทั้งโหนด
6. เขียน/ลบ/เผยแพร่กฎบนฐานข้อมูลจริง และ `git push` เข้า main ต้องขออนุญาตเจ้าของทุกครั้ง (ยกเว้นประโยคที่เจ้าของกำหนดไว้ใน CLAUDE.md)
7. `ALL_100_STALLS` กับ `MARKET_DATA` เก็บคนละอ็อบเจกต์กันแม้ `stallId` เดียวกัน — ฟังก์ชันที่แก้ข้อมูลร้านค้าต้องแก้ทั้ง 2 อาเรย์เสมอ
