# 📋 เอกสารส่งต่องานสำหรับ Claude Code (TalatHub / Hsong Hand-off Document)

**โปรเจกต์:** TalatHub / Hsong (ตลาดสดออนไลน์บ้านบึง ตลาดวิศิษฐ์ชัย)  
**คลังซอร์สโค้ด:** https://github.com/pisaen666/hsong  
**อัปเดตล่าสุด:** 20 กันยายน 2026 (Commit: `5d62d78` + updates)  
**ไฟล์คอนฟิกหลักสำหรับ Claude Code:** [`CLAUDE.md`](file:///c:/pisaen/hsong/CLAUDE.md) (Claude Code จะอ่านอัตโนมัติเมื่อเริ่มเซสชัน)

---

## 1. สถานะโปรเจกต์ล่าสุด (Current Snapshot)

ระบบเป็น **Single Page Application (SPA)** ใช้ HTML5, Vanilla JavaScript (`app.js`), Tailwind CSS (CDN) และ Firebase Realtime Database:

| บทบาท (Role) | สถานะ | รายละเอียดการทำงานล่าสุด |
| :--- | :---: | :--- |
| **Role 1 (Customer / ลูกค้า)** | ✅ สมบูรณ์ | สั่งของสดจากแผงค้า, ตะกร้าสินค้า, คำนวณค่าส่ง, GPS ปักหมุด, ติดตามสถานะออเดอร์ |
| **Role 2 (Hub Operations / ศูนย์จัดส่งฮับ)** | ✅ สมบูรณ์ | รวมรายการหยิบของสด (Picking List), บรรจุหีบห่อ, มอบหมายงานไรเดอร์, สรุปยอดโอนรายวัน |
| **Role 3 (Merchant / แผงค้าในตลาด)** | ✅ สมบูรณ์ | เปิด-ปิดร้าน, รายการสินค้าสด, หักค่าบริการ GP 5%, ตั้งค่าบัญชีธนาคาร, เรียกรถด่วน |
| **Role 4 (Rider / ไรเดอร์ส่งของ)** | ✅ สมบูรณ์ & คลีน | **เพิ่งอัปเกรดล่าสุด**: <br>1. ลบ Mock ไรเดอร์ฮาร์ดโค้ดออกหมด (`DEFAULT_COMMUNITY_RIDERS = []`) <br>2. เพิ่ม **Rider Guest & Registration Portal** ในหน้าหลัก มี 2 แท็บ (แท็บลงทะเบียนสมัครใหม่ + แท็บล็อกอิน) <br>3. มีปุ่ม `⚡ เติมข้อมูลตัวอย่าง 1-Click` และ `🚀 สมัคร & ล็อกอินทันที 1-Click Test` <br>4. ระบบรับงาน, แผนที่ GPS, กระเป๋าเงินไรเดอร์, เคลียร์ยอดเงินสด COD |
| **Role 5 (Admin / แอดมินส่วนกลาง)** | ⏳ พร้อมพัฒนาต่อ | อนุมัติแผงค้าและใบสมัครไรเดอร์, สรุปบัญชีการเงินตลาด, ดูภาพรวมทั้งระบบ |

---

## 2. สิ่งที่เพิ่งทำเสร็จในเซสชันล่าสุด (Recent Changes)

1. **Purge Dummy Riders**:
   - ลบรายชื่อไรเดอร์จำลอง (สมชาย, โนอาห์, แซม) ออกจากโค้ดและ Firebase RTDB แบบ 100%
   - ปิดระบบวนลูปสร้างไรเดอร์จำลองซ้ำ เพื่อให้ระบบสะท้อนข้อมูลจริงเท่านั้น
2. **Dedicated Rider Guest & Registration Portal**:
   - เพิ่มหน้าจอใน `index.html` แบ่งเป็น `#rider-guest-view` (เมื่อยังไม่ล็อกอิน) และ `#rider-logged-in-view` (เมื่อล็อกอินแล้ว)
   - หากในระบบยังไม่มีไรเดอร์ (`community_riders.length === 0`) การกดปุ่ม `4. ไรเดอร์` หรือ `สมัครไรเดอร์` จะเปิดแท็บลงทะเบียนให้ทันที
3. **Seamless Navigation & High Z-Index**:
   - แก้ไข `switchRole('rider')` ให้สลับ View มาที่ไรเดอร์เสมอ
   - ปรับ `z-index` ของ Modal ไรเดอร์เป็น `999999` ป้องกันการถูกแถบ Header ซ้อนทับ
4. **Cloud Database Sync**:
   - ฐานข้อมูล Firebase Realtime Database ซิงค์ข้อมูลสะอาดเรียบร้อย ผ่านสคริปต์ `node sync-firebase.js`

---

## 3. ขั้นตอนการเริ่มงานต่อใน Claude Code

### ขั้นตอนที่ 1: เปิด Terminal ในโฟลเดอร์โปรเจกต์
```bash
cd c:\pisaen\hsong
git pull origin main
```

### ขั้นตอนที่ 2: รัน Claude Code
```bash
claude
```
*(Claude Code จะอ่านไฟล์ `CLAUDE.md` ที่อยู่ตรง Root โดยอัตโนมัติ ทำให้จำบริบท กฎเกณฑ์ และสถาปัตยกรรมของระบบได้ทันที)*

---

## 4. ข้อความ Prompt ตัวอย่างสำหรับ Copy-Paste สั่งงาน Claude Code

เลือกข้อความตามเป้าหมายที่คุณต้องการทำต่อ:

### ตัวเลือก A: ตรวจสอบและพัฒนา Role 5 (Admin) ให้สมบูรณ์ครบวงจร (แนะนำ)
> *"สวัสดี Claude โปรดอ่านไฟล์ CLAUDE.md และ HANDOFF_TO_CLAUDE.md เพื่อรับทราบสถานะโปรเจกต์ TalatHub ขณะนี้เราพัฒนา Role 1-4 สมบูรณ์แล้ว และเพิ่งปรับปรุงระบบลงทะเบียนไรเดอร์ใหม่เสร็จสิ้น งานต่อไปคือตรวจสอบและ Audit การทำงานของ Role 5 (Admin / ผู้ดูแลระบบ) ใน index.html และ app.js โดยเฉพาะส่วนการตรวจสอบและอนุมัติใบสมัครไรเดอร์และแผงค้า การดูบันทึกยอดเงิน และความลื่นไหลในการสลับ Role โดยขอให้รัน `node -c app.js` ตรวจสอบไวยากรณ์ทุกครั้งก่อนจบงาน"*

### ตัวเลือก B: ทดสอบระบบ End-to-End ครบ 5 บทบาท
> *"ช่วยตรวจสอบฟังก์ชันการทำงานแบบเชื่อมโยงครบ 5 บทบาท: ลูกค้าสั่งซื้อ (Role 1) -> ฮับรับออเดอร์จัดของ (Role 2) -> ไรเดอร์รับงานและส่งมอบ (Role 4) -> แผงค้ารับยอดเงิน (Role 3) -> แอดมินดูรายงานสรุป (Role 5) ว่ามีจุดสะดุดหรือข้อมูลไม่เชื่อมกันตรงไหนบ้าง แล้วแก้ไขให้ทำงานได้ลื่นไหล 100% พร้อมทดสอบด้วย `node -c app.js`"*

---

## 5. กฎเหล็กที่ Claude Code ต้องปฏิบัติตาม (Strict Rules)
1. **ห้ามสร้าง Dummy Riders คืนมา**: ตัวแปร `DEFAULT_COMMUNITY_RIDERS` ใน `app.js` ต้องเป็น `[]` เสมอ
2. **ทดสอบ Syntax เสมอ**: ต้องรัน `node -c app.js` ก่อนจบงานทุกครั้ง
3. **ผูกฟังก์ชันกับ `window.*` เสมอ**: ทุกฟังก์ชันที่เรียกจาก HTML ต้องมี `window.functionName = functionName;`
4. **รักษา Session ข้าม Role**: การกดสลับระหว่าง 5 บทบาทต้องไม่ทำลาย Session ของบทบาทอื่นที่ล็อกอินค้างไว้
