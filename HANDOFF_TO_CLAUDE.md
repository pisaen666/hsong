# 📋 เอกสารส่งต่องาน (Hand-off) — TalatHub / Hsong

**อัปเดตล่าสุด:** 23 กันยายน 2026 (เว็บจริงมีของถึงคอมมิต `57d8821` แล้ว; มีงานเพิ่มอีก **4 คอมมิตค้างอยู่ในเครื่องนี้ ยังไม่ได้ push** — ทดสอบผ่านบนเว็บทดสอบหมดแล้ว รอเจ้าของพิมพ์ประโยคอนุญาตที่บ้าน)
**คลังโค้ด:** https://github.com/pisaen666/hsong  |  **เว็บจริง:** https://pisaen666.github.io/hsong/  |  **เว็บทดสอบ:** https://hsong-test.web.app
**ไฟล์กติกาของ Claude:** `CLAUDE.md` (อ่านอัตโนมัติ) — ไฟล์นี้บอก "งานค้างและจุดที่ทำถึง" แบบละเอียดทุกฟีเจอร์ (หัวข้อ 5a-5q)

> สำหรับ Claude ที่บ้าน: เจ้าของเป็นมือใหม่ สายตาไม่ดี ให้ตอบเป็นภาษาไทย คำง่าย และ **อธิบายทุกขั้นตอนที่ให้เจ้าของทำแบบละเอียดเป็นข้อ ๆ** (บอกชื่อปุ่มตามหน้าจอ บอกว่าควรเห็นอะไร และถ้าเห็นไม่ตรงให้ทำอย่างไร) เจ้าของขอเรื่องนี้ไว้ชัดเจน (ความจำใน Claude เครื่องออฟฟิศไม่ตามมาที่บ้าน จึงจดไว้ที่นี่และใน CLAUDE.md ข้อ 4)

---

## 0. เริ่มที่บ้านต้องทำอะไรก่อน (สำคัญ)

1. เปิดเทอร์มินัลในโฟลเดอร์โปรเจกต์ แล้วพิมพ์:
   ```bash
   git checkout main
   git pull origin main
   ```
2. เปิด Claude Code แล้ววางข้อความนี้ให้ Claude:
   > *"อ่าน CLAUDE.md กับ HANDOFF_TO_CLAUDE.md แล้วสรุปให้ฟังว่าตอนนี้ทำถึงไหน แล้วรอผมสั่งขั้นตอนต่อไป"*
3. ทดสอบโค้ดก่อนเริ่มงาน (ทุกชุดต้องขึ้น ALL PASSED — ตอนนี้มี 16 ไฟล์ทดสอบแล้ว):
   ```bash
   node -c app.js
   for f in firebase-rules/tests/*.test.js; do node "$f" || echo "FAILED: $f"; done
   ```
4. **งานสำคัญที่สุดตอนนี้:** เครื่องนี้ (ที่ทำงาน) มี **4 คอมมิตแก้เสร็จ ทดสอบบนเว็บทดสอบผ่านหมดแล้ว แต่ยังไม่ได้ push ขึ้นเว็บจริง** (ดูหัวข้อ 1 ด้านล่าง) — ถ้าเจ้าของพร้อมให้ขึ้นเว็บจริงที่บ้าน ให้รอเจ้าของพิมพ์ `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` แล้วทำตามหัวข้อ 4 ของ `CLAUDE.md` (รัน `node -c app.js` → คอมมิตมีอยู่แล้วไม่ต้องคอมมิตซ้ำ → `git push origin main` → ตรวจเว็บจริงด้วย `curl`/`diff` เทียบไฟล์ ไม่ต้องเปิดเว็บจริงในเบราว์เซอร์)

---

## 1. สถานะตอนนี้ (สรุปสั้น)

| เรื่อง | สถานะ |
|---|---|
| เว็บจริง (`main`, commit `57d8821`) | ✅ ใช้งานอยู่ ล่าสุดที่ขึ้นจริงคือ "แก้แผนที่ปักหมุดปลายทางของร้านค้าล้นจอ" (`app.js?v=10.45_merchant_dest_search`) |
| **4 คอมมิตค้าง (ในเครื่องนี้ ยังไม่ push)** | ✅ ทดสอบบนเว็บทดสอบผ่านหมดแล้ว รายละเอียดหัวข้อ 2 ด้านล่าง — คอมมิตล่าสุด `3d0a324`, ไฟล์ `app.js?v=10.49_stall_dual_array_sync_fix` |
| กฎฐานข้อมูลจริง | ✅ v2.2 เผยแพร่แล้ว ไม่มีอะไรเปลี่ยนวันนี้ |
| เว็บทดสอบ https://hsong-test.web.app | ✅ เป็นเวอร์ชันล่าสุด (10.49) รวมงานค้างทั้ง 4 คอมมิตแล้ว |

---

## 2. งานที่ทำวันนี้ (23 ก.ย. 2026) — เรียงตามเวลา พร้อมสถานะ push

### ขึ้นเว็บจริงแล้ววันนี้ (เรียงตามเวลา, ทุกอันเจ้าของพิมพ์ประโยคอนุญาตแล้ว)
1. **แก้บั๊ก "ล้างข้อมูลทดสอบ" ไม่ได้ผลจริง** — ปุ่มล้างข้อมูลไรเดอร์ในหน้าแอดมิน เขียนทับข้อมูลทั้งก้อนซึ่งกฎ v2 ไม่อนุญาต (ต้องลบทีละ id) แก้แล้ว + เพิ่มด่านตรวจสิทธิ์เจ้าของที่หายไป (CLAUDE.md 5m)
2. **ตัดของที่ไม่จำเป็นออกจากหน้าแรกลูกค้า** — แถบ "จัดส่งที่/แตะระบุที่อยู่" ด้านบนสุด และป้าย "Hub ต้นทาง" ตามคำขอเจ้าของ (CLAUDE.md 5n)
3. **ช่องค้นหาปลายทางสำหรับร้านค้าเรียกไรเดอร์** — เดิมมีแค่ปุ่ม GPS (ใช้ไม่ได้เพราะร้านค้าไม่ได้อยู่ที่ปลายทางจริง) เพิ่มช่องค้นหาแบบเดียวกับฝั่งลูกค้า (CLAUDE.md 5o) + แก้บั๊กตามมาที่หน้าต่างนี้ล้นจอมือถือกดปุ่มยืนยันไม่ถึง (CLAUDE.md 5o ต่อท้าย)

### ทดสอบผ่านบนเว็บทดสอบแล้ว **แต่ยังไม่ได้ push ขึ้นเว็บจริง** (4 คอมมิต, เรียงเก่า→ใหม่)
เกิดจากเจ้าของขอให้ตรวจสอบ "ระบบสุ่มหมุนเวียนร้านค้า" บนหน้าแรก แล้วขยายผลต่อเนื่องหลายรอบ (CLAUDE.md 5p, 5q มีรายละเอียดครบทุกจุด):

4. `92d4ebf` — **บั๊กใหญ่ที่เจอจากการตรวจสอบ:** ร้านค้าทุกร้านที่สมัคร/แก้ไขได้หมวดหมู่ "ไก่" (chicken) ติดตัวไปเสมอไม่ว่าขายอะไรจริง เพราะช่องเลือกหมวดหมู่หายไปจากฟอร์มตั้งแต่ฟอร์มถูกปรับปรุงใหม่ (ไม่มีใครสังเกตเพราะมีร้านจริงแค่ 1 ร้านและบังเอิญขายไก่จริง) — เพิ่มช่องกลับเข้าไปแล้ว
5. `139788b` — แก้ไปพร้อมกันตามคำขอเจ้าของ: ปุ่ม "1-Click กรอกข้อมูลตัวอย่าง" ตอนสมัครร้านค้าที่พังอยู่ (อ้าง id ฟอร์มเก่าที่ไม่มีแล้ว)
6. `b338bc0` — เจ้าของสังเกตว่าช่องหมวดหมู่ร้านค้าใหม่มีแค่ 5 ตัวเลือก น้อยกว่าช่องหมวดหมู่ตอนใส่สินค้า (10+1) มาก จึงรวมให้เป็นชุดเดียวกันทั้งระบบ (11 หมวดหมู่)
7. `3d0a324` — ทำ 2 ข้อเสนอแนะที่ค้างจากการตรวจระบบสุ่ม: (ก) ให้แอดมินแก้ไขหมวดหมู่ร้านค้าได้เองจากหน้ารายชื่อ (ข) เปลี่ยนวิธีสุ่มให้ถูกต้องตามหลักสถิติ (Fisher-Yates) — **ระหว่างทำเจอบั๊กจริงอีกจุด:** ปุ่ม "เปิด/ปิดร้าน" ของแอดมิน ดูเหมือนกดสำเร็จบนจอแต่ไม่ได้บันทึกจริง (ข้อมูลร้านค้าเก็บอยู่ 2 ชุดที่ไม่ผูกกัน) แก้ไปพร้อมกัน

**สิ่งที่ต้องทำต่อที่บ้าน:** ถ้าเจ้าของพร้อม ให้ขอประโยคอนุญาต `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` แล้ว `git push origin main` (โค้ดพร้อมแล้ว ทดสอบผ่านหมดแล้ว ไม่ต้องแก้อะไรเพิ่ม)

---

## 3. ทำอะไรไปบ้างในวันที่ 22 ก.ย. 2026 (สรุปย่อ — รายละเอียดเต็มอยู่ CLAUDE.md 5g-5o)

- ตัวอักษรใหญ่สำหรับสายตาไม่ดี, ด่านตรวจก่อนสั่งซื้อ (กันที่อยู่ปลอม), รหัสติดตามออเดอร์ไม่ต้องล็อกอิน + แก้บั๊กข้อมูลลูกค้าคนอื่นรั่วที่กำลังใช้งานจริงบนเว็บจริง (สำคัญ แก้แล้ว)
- ด่านตรวจเอกสารไรเดอร์ก่อนอนุมัติ (กันไรเดอร์ไม่มีเอกสารเข้าไปส่งของถึงบ้านคน)
- แก้ปุ่มแอดมินโดนตัดบน PC, ป้ายแจ้งเตือนสีแดงตำแหน่งผิดที่ (พบ 2 รอบ, บั๊กเดียวกันกระทบทั้งป้ายแอดมินและป้าย "NEW" ของฮับ), ปุ่มไรเดอร์บนมือถือแคบเกิน, ปุ่ม "สมัครไรเดอร์" ตัวหนังสือหายเพราะพิมพ์ผิด breakpoint ที่ไม่มีจริง

---

## 4. งานค้างต่อจากนี้ (เรียงตามที่แนะนำ — ไม่เปลี่ยนจากก่อนหน้า)

1. **ระบบล็อกอินลูกค้า / ป้องกันข้อมูลลูกค้า (ใหญ่ที่สุด ควรทำก่อนมีลูกค้าจริงเยอะ):** `orders` และ `carts` ยังเปิดอ่าน-เขียนได้ทุกคน (มีชื่อ เบอร์ ที่อยู่ลูกค้า) ต้องมีตัวตนลูกค้า (Firebase Auth เช่นเบอร์โทร/OTP) แล้วปรับกฎ — รหัสติดตามออเดอร์ (22 ก.ย.) กันได้แค่ "เปิดเว็บดูของคนอื่น" ไม่ได้กัน REST API ตรง ๆ
2. รหัสผ่านไรเดอร์/แผงค้าตรวจในเบราว์เซอร์เท่านั้น (ไม่ใช่ความปลอดภัยฝั่งเซิร์ฟเวอร์) ทางแก้ระยะยาวคือ Firebase Auth ให้ไรเดอร์/แผงค้า
3. ข้อสงสัยบั๊ก: `aggregateDailyOperations` อาจเขียนทับ `daily_reports/<วัน>` แล้วลบสถานะ "โอนแล้ว" (ยังไม่ยืนยัน ต้องตรวจก่อนพึ่งประวัติการโอนเงิน)
4. หน้า "ตั้งค่า" ของแอดมิน: มีผลจริงแค่ GP ที่เหลือบันทึกแต่ไม่มีที่ใช้ และเก็บในเครื่องเดียว
5. `promptPay` ของไรเดอร์อาจเป็นเลขบัตรประชาชน และเบอร์/LINE ยังเปิดสาธารณะ
6. ค่าส่งไรเดอร์ 40 บาท/เที่ยว ยังฮาร์ดโค้ดในรายงาน
7. เอกสารร้านค้ายังไม่มีคนตรวจก่อนอนุมัติ (ต่างจากไรเดอร์ที่ตรวจแล้ว 22 ก.ย. — ความเสี่ยงน้อยกว่าไรเดอร์ที่เข้าบ้านคน จึงยังไม่เร่งด่วน)
8. ไรเดอร์จริง `RD3562` เคยเห็นย้อนกลับไปสถานะ "รออนุมัติ" ระหว่างทดสอบเมื่อ 22 ก.ย. — ยังไม่ได้ตามเรื่องนี้ต่อ ควรเช็คสถานะปัจจุบันของไรเดอร์นี้ก่อนใช้งานจริง
9. รายชื่อร้านค้าใหม่ที่เพิ่มโค้ดวันนี้ (หมวดหมู่ร้านค้า, ค้นหาปลายทาง ฯลฯ) — เมื่อมีร้านค้าจริงหลายร้านสมัครเข้ามา ควรลองดูว่าระบบสุ่มหมุนเวียนหน้าแรกทำงานสมจริงมากขึ้นหรือไม่ (ตอนนี้ยังทดสอบได้แค่กับข้อมูลจำลอง เพราะมีร้านจริงร้านเดียว)

---

## 5. เครื่องมือและวิธีทดสอบ (ไฟล์ชั่วคราวของเครื่องออฟฟิศไม่ตามมา ใช้สคริปต์ในโปรเจกต์แทน)

| ต้องการ | ใช้ |
|---|---|
| รันชุดทดสอบทั้งหมด (16 ไฟล์) | `for f in firebase-rules/tests/*.test.js; do node "$f" || echo "FAILED: $f"; done` |
| สร้างโฟลเดอร์เว็บทดสอบ (ชี้ hsong-test เท่านั้น) | `node firebase-rules/tools/build-test-site.js <โฟลเดอร์>` แล้วเข้าโฟลเดอร์นั้น `firebase deploy --only hosting --project hsong-test` |
| ส่งกฎไป hsong-test | สร้างโฟลเดอร์ชั่วคราวที่มี `firebase.json` = `{"database":{"rules":"rules.json"}}`, `.firebaserc` ชี้ hsong-test, `rules.json` = ไฟล์ `firebase-rules/hsong-test.rules.v2.json` แล้ว `firebase deploy --only database --project hsong-test` |
| ทดสอบกฎแบบคนแปลกหน้า (55 ข้อ) | ใส่ข้อมูลตั้งต้นก่อน `MSYS_NO_PATHCONV=1 firebase database:update / firebase-rules/tests/rules-anon.seed.json --project hsong-test --force` แล้ว `node firebase-rules/tests/rules-anon.test.js` |
| ตรวจข้อความที่ยังไม่ผ่าน escapeHtml ในโค้ดใหม่ | `firebase-rules/tools/scan-unescaped.js` (ต้องมี `acorn` `acorn-walk` ติดตั้งในโฟลเดอร์ชั่วคราว) |
| ตรวจว่าปุ่ม onclick ทุกปุ่มยังคอมไพล์ได้ | `firebase-rules/tools/check-handlers.js app.js` (ต้องมี `acorn` ติดตั้งในโฟลเดอร์ชั่วคราวเช่นกัน) |
| ตรวจตัวแปรที่ใช้แต่ไม่เคยประกาศ | `eslint --no-eslintrc -c firebase-rules/tools/eslint-undef.json app.js` (ติดตั้ง eslint@8 ในโฟลเดอร์ชั่วคราว) เหลือ 6 รายการที่มี `typeof` กันไว้แล้ว ไม่ต้องแก้ |
| จำลองการโจมตี XSS บน hsong-test | `firebase-rules/tools/seed-poison.js` (ต้องเปิดกฎทดสอบชั่วคราว และคืนกฎเดิมทุกครั้ง ขั้นตอนเต็มใน CLAUDE.md 5e) |
| สร้างกฎใหม่ | แก้ `firebase-rules/build-rules.js` เท่านั้น แล้ว `node firebase-rules/build-rules.js` (ห้ามแก้ไฟล์ .json ตรง ๆ) |

**ข้อควรระวังที่เจอจริง:**
- **ทุกครั้งที่ส่งเว็บทดสอบใหม่ (แม้เป็นการแก้ตามหลังอย่างรวดเร็ว) ต้องเปลี่ยนเลข `app.js?v=` ใน index.html** ไม่งั้นเบราว์เซอร์ใช้ไฟล์เก่าจากแคชแม้ deploy สำเร็จแล้ว — เจอจริงอีกครั้งวันที่ 23 ก.ย. ระหว่างตรวจสอบบั๊ก dual-array (แก้โค้ดแล้ว deploy แล้ว แต่ browser console ยังรันฟังก์ชันเก่าอยู่ จนกว่าจะขยับเลขเวอร์ชันแล้ว deploy ซ้ำ)
- ตรวจว่าไฟล์บนเว็บจริง "ตรงกับที่แก้จริง" ทุกครั้งหลัง push ด้วย `curl` แล้ว `diff` เทียบไฟล์ในเครื่อง (อย่าเชื่อแค่เลขเวอร์ชันตรง เพราะ marker ที่ใช้เช็คอาจเลือกผิดจุดจนเข้าใจผิดว่าขึ้นแล้วทั้งที่ GitHub Pages ยังแคชของเก่าอยู่ ~10 นาที)
- เจ้าของเห็น "ปุ่มกดแล้วไม่ได้ผล" เพราะเบราว์เซอร์จำหน้าเก่า → ให้กด **Ctrl + Shift + R** หรือปิดเปิดแอปใหม่ทั้งหมดบนมือถือ
- ทดสอบล็อกอินไรเดอร์/แผงค้า **ต้องออกจากระบบเจ้าของก่อน** และสมัครเป็นคนทั่วไป ถ้าสมัครตอนล็อกอินเจ้าของ ระบบสร้างไรเดอร์ให้ทันทีโดยไม่มีรหัสผ่าน
- ใส่รหัสผิด 5 ครั้งถูกล็อก 5 นาที (ไรเดอร์และแผงค้าแยกกัน)
- กฎทดสอบเปิดชั่วคราวทุกครั้งต้องคืนเป็น v2.2 แล้วรัน `rules-anon.test.js` ให้ผ่านครบ
- ใน Windows: ใช้ `MSYS_NO_PATHCONV=1` กับคำสั่ง firebase ที่มี `/`, ไฟล์ `CLAUDE.md` และ `index.html` เป็น CRLF (สคริปต์แก้ไฟล์ต้องรักษาบรรทัดใหม่)
- ห้ามเปิดหน้าเว็บจริงในเบราว์เซอร์ของ Claude เพราะเปิดแล้วเชื่อมฐานข้อมูลจริงทันที (ใช้เว็บทดสอบแทน)
- ผมล็อกอินเจ้าของแทนไม่ได้ (ไม่มีรหัสผ่านและไม่ควรมี) ทางที่ได้ผล: ให้เจ้าของล็อกอินเองในหน้าต่างเบราว์เซอร์ของ Claude แล้วให้ Claude กดทดสอบต่อ หรือจำลอง `state.activeMerchant`/`state.activeAdmin` ตรง ๆ ใน console เพื่อทดสอบ UI/ฟังก์ชันเฉย ๆ โดยไม่เขียนขึ้นคลาวด์จริง (ใช้บ่อยวันนี้กับหน้าจอร้านค้า)

---

## 6. กฎเหล็ก (ย่อ ฉบับเต็มอยู่ CLAUDE.md)
1. `DEFAULT_COMMUNITY_RIDERS` ต้องเป็น `[]` เสมอ ห้ามมีไรเดอร์จำลอง
2. แก้ `app.js` ทุกครั้งต้องรัน `node -c app.js`
3. ฟังก์ชันที่เรียกจาก HTML ต้องผูก `window.*`
4. ข้อความจากฐานข้อมูล/ผู้ใช้ที่แสดงใน HTML ต้องผ่าน `escapeHtml(x)`; อาร์กิวเมนต์สตริงใน `onclick` ต้องใช้ `jsArg(x)` (ห้ามเขียน `'${x}'`)
5. โค้ดเขียนไรเดอร์/ร้านขึ้นคลาวด์ต้องเขียนทีละรายการ (`syncKeyedToCloud`/`syncListToCloud`) ห้าม `set()`/`remove()` ทั้งโหนดของ `rider_applications`, `community_riders`, `merchant_applications`, `custom_market_stalls`, `stall_catalog_database`, `rider_documents`, `rider_private` (กฎ v2 ล็อกไว้ระดับลูกเท่านั้น เขียนทั้งก้อนจะโดนปฏิเสธเงียบ ๆ เสมอไม่ว่าใครล็อกอินอยู่ — บทเรียนจาก CLAUDE.md 5m); วัตถุร้านที่สร้างจากใบสมัครต้องผ่าน `stallFromApp(app)`
6. เขียน/ลบ/เผยแพร่กฎบนฐานข้อมูลจริง (`hsong-1f342`) และ `git push` เข้า main ต้องขออนุญาตเจ้าของทุกครั้ง (ยกเว้นประโยคที่เจ้าของกำหนดไว้ใน CLAUDE.md)
7. **`ALL_100_STALLS` กับ `MARKET_DATA` เก็บคนละอ็อบเจกต์กันแม้ stallId เดียวกัน** (บทเรียนจาก CLAUDE.md 5q) — ฟังก์ชันที่แก้ข้อมูลร้านค้าต้องหาและแก้ทั้ง 2 อาเรย์เสมอ ห้ามใช้ `A.find(...) || B.find(...)` แล้วแก้แค่ผลลัพธ์เดียวถ้าเป็นการเขียนข้อมูล (อ่านอย่างเดียวไม่เป็นไร)
