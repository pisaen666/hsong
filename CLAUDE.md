# Project Guidelines: TalatHub / Hsong (ตลาดสดบ้านบึง - ตลาดวิศิษฐ์ชัย)

## 📌 1. Project Overview & Architecture
- **Type**: Single Page Application (SPA) for community fresh market delivery.
- **Frontend**: Vanilla HTML5 (`index.html`), Vanilla JavaScript (`app.js`), Tailwind CSS (CDN).
- **Backend & Database**: 
  - Realtime Database: Firebase RTDB (`https://hsong-1f342-default-rtdb.asia-southeast1.firebasedatabase.app`)
  - Local Storage Fallback: LocalStorage with keys prefixed by `talathub_*` or `hsong_*`.
  - Local Server: `node server.js` serving `localhost:3000` (and LAN IP e.g. `192.168.1.34:3000`) with `Cache-Control: no-store`.
  - Sync Script: `node sync-firebase.js` (downloads live RTDB state to `firebase-rtdb-export.json`).

## 👥 2. The 5 Core Roles & Current Status
1. **Role 1 (Customer / ลูกค้า)**: [✅ Complete] Market catalog, multi-stall cart, GPS destination picker, checkout, order tracking.
2. **Role 2 (Hub Operations / ศูนย์จัดส่งฮับ)**: [✅ Complete] Consolidated picking list, packing verification, rider assignment/dispatch, end-of-day settlement.
3. **Role 3 (Merchant / แผงค้าตลาด)**: [✅ Complete] Stall open/close status, product availability, bank setup, GP 10% fee calculation (owner decision 2026-09-21), express call.
4. **Role 4 (Rider / ไรเดอร์)**: [✅ Complete & Purged]
   - **Guest Portal (`#rider-guest-view`)**: Shown when not logged in.
     - Tab 1: 📝 Registration form (`#onpage-rider-reg-form`) with `1-Click Sample Data Fill` and `1-Click Instant Register & Login`.
     - Tab 2: 🔑 Login tab with registered rider list (`#onpage-registered-riders-list`) and PIN/phone input.
     - Auto-opens Registration tab if 0 riders exist in database!
   - **Active Workspace (`#rider-logged-in-view`)**: Active delivery order, map route, cash collection (COD), PromptPay payout, wallet balance.
5. **Role 5 (Admin / ผู้ดูแลระบบ)**: [⏳ Active Development / Audit]
   - Approvals for merchant stalls and rider applications.
   - Financial settlement logs, rider rosters, system-wide configuration.

## ⚠️ 3. Strict Development Rules & Constraints
1. **NO DUMMY / MOCK RIDERS**:
   - `DEFAULT_COMMUNITY_RIDERS` in `app.js` must remain `[]`. Never hardcode dummy riders (e.g. Somchai, Noah, Sam).
   - Real riders must be created through user registration or the explicit `1-Click Test Register` button.
2. **MANDATORY SYNTAX VALIDATION**:
   - Every time `app.js` is modified, immediately run: `node -c app.js`.
   - Never finalize a task if syntax check fails.
3. **GLOBAL WINDOW BINDINGS**:
   - All interactive handlers invoked via HTML `onclick="..."` or `onsubmit="..."` MUST be attached to `window.*` (e.g. `window.switchRole = switchRole;`).
4. **SESSION INTEGRITY**:
   - Multi-role switching must preserve logged-in sessions across roles (`state.activeRider`, `state.activeMerchant`, `state.activeAdmin`).
5. **MODAL Z-INDEX**:
   - All primary modals (`rider-register-modal`, `rider-login-modal`, etc.) must maintain `z-[999999]` and explicit `display: flex / none` management so they never hide behind sticky headers.
6. **GIT WORKFLOW**:
   - Test in the sandbox (see section 5) or read-only on `http://localhost:3000`. Never test write flows against the live Firebase.
   - Commit with descriptive commit messages (`fix(...)`, `feat(...)`).
   - Push to `origin main` only when the owner asks (phrase in section 4). `main` is the live website (GitHub Pages). Never force-push.
   - When `app.js` changes and is pushed, bump the `app.js?v=...` tag in `index.html` (GitHub Pages caches for 10 minutes).

## 🤖 4. Claude Working Scope (ขอบเขตการทำงานของ Claude)
Owner is a solo beginner with low vision: reply in Thai, short sentences, plain words, explain jargon. **Whenever you ask the owner to test, try or do anything, explain it in detail, step by step (owner request 2026-09-21): numbered steps, one action each, exact button names as shown on screen, what they should see after each step, what to do if it looks different, what NOT to do, and why it matters in one short sentence.** Owner works from two computers (home / office) synced only through GitHub; see `คู่มือสื่อสารกับ_Claude.md` and `HANDOFF_TO_CLAUDE.md`.

**Do without asking (ทำเองได้เลย):**
- Read, search and edit project files; run `node -c app.js`; run local tests.
- Start throwaway servers on ports other than 3000 (e.g. 3001) from a sandbox copy (section 5) and stop them afterwards. Never stop the owner's server on port 3000.
- Read-only Firebase access: HTTP GET and `node sync-firebase.js` (downloads only).
- `git status / diff / log / pull`, and `git add` + `git commit` for finished work.
- Anything at all on the test Firebase project `hsong-test` (created 2026-09-20; Spark plan, Realtime DB in asia-southeast1, Email/Password auth with 2 owner accounts). Its web config is not secret; owner UIDs are listed in `firebase-rules/hsong-test.rules.json`. Never type or ask for the owner's passwords.

**Ask first, every time (ต้องถามก่อนเสมอ):**
- Any write or delete on the production Firebase project `hsong-1f342` (PUT / POST / PATCH / DELETE, `.set()`, `.remove()`, deploys).
- Changing or publishing production Firebase rules.
- `git push` to `main`, except on the owner's phrase below or an explicit request. No force-push, no history rewrites.
- Anything that costs money (e.g. Firebase Blaze plan) and deleting owner data or files Claude did not create.
- Claude never creates accounts or types passwords for the owner; the owner does that.

**Owner phrase `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ`** = run `node -c app.js`, bump the `app.js?v=` tag, commit, push to `main`, then verify the live site (https://pisaen666.github.io/hsong/) by fetching it and comparing files with local. Do not open the live page in a browser pane: page start-up syncs to the live Firebase.

## 🧪 5. Safe Testing Recipe (สำคัญ)
The production database is shared and currently open to everyone. Lesson from 2026-09-20: rider test data leaked into the live DB because the page was reloaded while seeded test data sat in localStorage (app start-up pushes riders from localStorage to the cloud).
- Sandbox: copy `index.html app.js styles.css firebase-config.js server.js images/` and the `.png` files to a temp folder; replace the host `hsong-1f342-default-rtdb.asia-southeast1.firebasedatabase.app` with `127.0.0.1:9` in `app.js`, `firebase-config.js` and `index.html`; run `PORT=3001 node server.js`; open `http://localhost:3001` (it has its own localStorage). Confirm no real host is left with `grep -c firebasedatabase.app`.
- In the sandbox the Firebase SDK never connects but `isFirebaseReady()` is still true, so `await db.ref().set()` hangs. Any awaited Firebase write needs a timeout (see `_withTimeout` in `app.js`).
- After anything that could touch production, confirm with a GET that no test data exists (`community_riders`, `rider_applications`, `rider_documents`).

## 🔐 5b. Owner Sign-in & Private Data (LIVE on main since 2026-09-20)
- Admin (Role 5) and Hub (Role 2) now use ONE Firebase Auth email/password owner account instead of PINs (`admin6305`, `hb6305`, quick-login buttons and the hub PIN setting were removed). Code: `initOwnerAuth`, `applyOwnerSession`, `verifyOwnerPassword` in `app.js`; `auth` and `OWNER_UIDS` in `firebase-config.js`.
- Production owner accounts (primary + backup) exist in `hsong-1f342` Authentication; their UIDs are in `firebase-config.js` (`OWNER_UIDS`) and in `firebase-rules/hsong-1f342.rules.json` (published in the Firebase Console). Sign-up is disabled. If the owner ever loses both accounts, Admin/Hub cannot be opened: create a new user in the Console and add its UID to BOTH `OWNER_UIDS` and the rules.
- Deploy order that worked: publish rules first (old code still works with them), then merge/push code. Keep `firebase-config.js?v=` and `app.js?v=` tags bumped in `index.html`.
- Rider personal data (ID number, address, emergency contact, bank accounts, driving-license number) lives ONLY in `rider_private/<riderId>` (owner read; riders create-only). `stripRiderPrivate*` removes it from every write to localStorage, `rider_applications` and `community_riders`; `_riderPrivateCache` (memory only) is filled after owner sign-in and merged by `loadRiderApplications()`. Any NEW code that writes rider/application objects to storage or Firebase must strip private fields. Known leftovers: `promptPay` (may be a national ID), phone and LINE id are still public; merchant applications (bank accounts), orders and carts are still open.
- Test environment with the REAL Auth SDK: copy the app to a temp folder, in `app.js` replace the host `hsong-1f342-default-rtdb...` with `hsong-test-default-rtdb.asia-southeast1.firebasedatabase.app`, set the `hsong-test` web config and test UIDs in `firebase-config.js`, run `PORT=3001 node server.js`. Claude cannot log in for the owner (no passwords); the owner tests the real login by hand. Logic can be tested with a mock `auth` object in a sandbox.

## 🛡️ 5c. Rules v2 & per-item cloud writes (PUBLISHED TO PRODUCTION 2026-09-21 with the owner's OK)
Goal: anyone may APPLY, only the owner may APPROVE / create real riders & stalls / delete. Files: `firebase-rules/build-rules.js` generates `hsong-test.rules.v2.json` and `hsong-1f342.rules.v2.json` (edit the template, never the JSON). PRODUCTION now runs `hsong-1f342.rules.v2.json` (deployed with the Firebase CLI from a temp folder, `--project hsong-1f342`). The old `*.rules.json` files are the previous version, kept for rollback.
- **Rollback (only useful before 2026-11-02, the old expiry):** copy `firebase-rules/hsong-1f342.rules.json` to `rules.json` in a temp folder that has `firebase.json` = `{"database":{"rules":"rules.json"}}` and `.firebaserc` default `hsong-1f342`, then `firebase deploy --only database --project hsong-1f342` (needs the owner's OK). The CLI has no command to download the published rules; git commit `322184a` holds the previous file.
- Verified after publishing (denial-only probes, nothing written): self-approve, direct create of riders/stalls, `daily_reports` write, unknown node and delete all return 401; public reads of the application nodes return 200; `rider_private`, `rider_documents` and the DB root return 401.
- Client: `syncListToCloud` / `syncKeyedToCloud` write ONLY changed items, one id at a time (`node/<id>`), instead of `set()` on the whole node. Used for `rider_applications`, `community_riders`, `merchant_applications`, `custom_market_stalls`, `stall_catalog_database`. Readers use `cloudValToList` (accepts old array layout AND id-keyed objects; id-keyed wins). Never add a whole-node `set()`/REST PUT for these nodes again.
- Owner sign-in runs `migrateLegacyCloudLists()` once: rewrites the old array layout (keys 0,1,2...) to id keys. Production data is still in the old layout (checked read-only 2026-09-21).
- Rules: non-owner may CREATE an application only with `status: "pending"` (merchant: no `accessCode`; rider: no `loginHash`/`loginSalt`); may UPDATE existing items only if `status`/`accessCode`/`id` (riders: `id`/`accessCode`/`phone`/`loginHash`/`loginSalt`; rider applications also `loginHash`/`loginSalt`; stalls: `stallId`/`accessCode`) are unchanged; may NOT delete. `community_riders`, `custom_market_stalls` and new `stall_catalog_database` entries: owner-only create. `daily_reports`: owner-only write. `orders carts riders rider_status rider_locations active_rider` stay open (no expiry).
- Rider registration (3 flows) now produces `pending` for visitors; the rider is created only when the owner approves (`approveRiderApplication`) or when the owner is signed in. `requireOwnerAction()` guards approve/reject/reconsider/delete. `approveAndLoginRider` logs an already-approved rider in without owner rights, but never approves for a visitor. The 1-Click test register button works only for the owner.
- Removed: the "one-time purge" blocks that ran on every NEW browser and deleted `orders`, `daily_reports`, `riders` and the whole rider database from the cloud. `cleanRiderDatabase()` is now owner-only and manual.
- Tests in `firebase-rules/tests/` (repeat after any change): `rules-anon.test.js` = 46 anonymous REST cases against `hsong-test` (what a stranger can / cannot do; setup steps are in its header, seed file included), `owner-logic.test.js` = migration/delete/change-detection with an in-memory DB, `admin-gp-revoke.test.js` = GP 10% and rider revoke logic. Run with `node firebase-rules/tests/<name>.test.js`. Helper scripts (build test site, XSS scan, handler check, attack seeder) are in `firebase-rules/tools/`; the office-machine temp folders are not in the repo, see `HANDOFF_TO_CLAUDE.md` section 5. Owner-side rules could not be run live (Claude has no owner password, no Java for the emulator): the owner must sign in on the test copy and try approve/reject once.
- **Production rollout order used (done 2026-09-21 in this order):** (1) push the code (works with the old open rules; per-item writes and old layout coexist), (2) owner signs in once so the data migrates (verify with a shallow GET that keys are ids, not 0,1,2), (3) only then publish `hsong-1f342.rules.v2.json` (Console or `firebase deploy --only database --project hsong-1f342` from a temp folder, with the owner's OK). Pages cached before step 1 (≤10 min) write whole nodes and will be rejected after step 3.
- Firebase CLI default project in this repo is PRODUCTION (`.firebaserc`). Always pass `--project hsong-test` explicitly and deploy rules from a temp folder that has its own `firebase.json`/`.firebaserc`.

## 🔑 5d. Rider secret-password login (LIVE 2026-09-21: rules v2.1 + code `app.js?v=10.28_rider_secret_login`)
- Login = **rider number (RDxxxx, public) + secret password (8 chars, e.g. K7MQ-2XTA)**. Code: `generateRiderSecret`, `makeRiderLoginCredential`, `hashRiderSecret` (PBKDF2-SHA256, 150k iterations), `riderSecretLogin`, `submitRiderSecretLogin`, `resetRiderLoginSecret` in `app.js`. Tests: `firebase-rules/tests/rider-login.test.js`.
- The secret is created when the OWNER approves (`approveRiderApplication`, now async) or presses "รหัสเข้าระบบ" / "สร้างรหัสผ่านใหม่" on an approved rider. It is shown ONCE in the SMS/LINE modal for the owner to send to the rider. Only `loginSalt` + `loginHash` are stored (on the application and on `community_riders/<id>`); the plain secret is never stored and cannot be recovered - reset it instead.
- Rules (v2.1): non-owner cannot set/change/remove `loginHash`/`loginSalt` on existing riders or applications, and cannot create an application that carries them. WITHOUT this an attacker could overwrite the hash with their own and log in as anyone.
- Every login path that skipped the password is now owner-only: `loginRiderById`, `quickLoginRider`, `handleRiderLoginSubmit`, `approveAndLoginRider` for approved riders, and the public rider list / pending list in the login screens (hidden from visitors). Visitors after applying only get their application number (pending notice box); the owner sends the password after approval.
- Lockout: 5 wrong tries within 10 minutes locks that browser for 5 minutes (`talathub_rider_login_fail`).
- Limits (be honest with the owner): the password is checked in the browser, so this stops impersonation by anyone who only knows a number/phone/name, but it is NOT server-side security: `orders`, `rider_status`, `rider_locations` stay open. Old riders (e.g. `RD3562`) have no password until the owner presses the reset button. `crypto.subtle` needs https or localhost (plain http on a LAN IP shows a "cannot verify" message).
- **Rollout order (do not swap):** (1) DONE 2026-09-21: `hsong-1f342.rules.v2.json` (v2.1, adds the `loginHash`/`loginSalt` locks) published to production with the owner's OK and verified with denial-only probes (rollback file = git `0910f19:firebase-rules/hsong-1f342.rules.v2.json`), (2) DONE: code pushed (`d0f35fd`), (3) DONE for `RD3562` (owner created its password on the real site; production shows loginHash 64 chars / loginSalt 32 chars on both nodes). For any future real rider: owner signs in on the real site and presses "รหัสเข้าระบบ" for each real rider and sends them the password. Test copy of this version: https://hsong-test.web.app (talks to `hsong-test`, title starts with "[ทดสอบ]").

## 🧱 5e. Stored-XSS defence (LIVE on main 2026-09-21, `app.js?v=10.31_login_error_msg`; includes the rider-login red error box under the button)
Problem found 2026-09-21: anyone can write text into the shared database (applications, orders, carts, and edit text of existing riders/stalls), and `app.js` put that text into `innerHTML` templates unescaped (only 46 of ~700 places used `escapeHtml`). Proof on `hsong-test` with the OLD code: seeded attack strings ran code in 10 places just by loading the customer home page / admin / hub views. After the fix: 0 places.
- **Layer 1 (broad net, one gate):** "UNTRUSTED DATA GUARD" block right after `jsArg` in `app.js`. `neutralizeUntrustedString/Deep` replace `< > " ' \ backtick` with look-alike characters (`＜ ＞ ” ’ ˋ ＼`), turn typed `&#..`/`&quot;` into `＆`, and prefix `javascript:`/`vbscript:`/`data:text/html` with `blocked-`. Installed on `DataSnapshot.prototype.val/exportVal`, on every `fetch(...).json()` and on `localStorage.getItem` (JSON values only, and only when suspicious characters exist). Normal data (Thai text, URLs with `&`, base64, numbers) is unchanged character for character. Effect: an apostrophe in a name shows as `’`.
- **Layer 2 (at the sink):** ~700 template spots were rewritten by an AST script (kept out of the repo; it used acorn): `'${x}'` inside `onclick="fn('${x}')"` became `${jsArg(x)}`, and user-text fields (`name`, `stallName`, `phone`, `address`, `orderId`, `id`, ...) inside HTML templates became `${escapeHtml(...)}`. SMS-modal message, print-window `<title>` and category `<option>` lists were fixed by hand.
- **Rules for new code:** any new template that shows text from the database/user must use `escapeHtml(x)`; string arguments in inline handlers must use `jsArg(x)` (never `'${x}'`); never read Firebase/REST data through a path that skips `snapshot.val()` / `fetch().json()` (no XMLHttpRequest, `.text()`, WebSocket, EventSource; the test fails if they appear).
- **Tests:** `node firebase-rules/tests/untrusted-guard.test.js` (59 checks: attack strings, benign data unchanged, 20,000 random strings, SDK/fetch/localStorage wrapping, no bypass APIs). Live proof recipe on the TEST project only: temporarily deploy open rules to `hsong-test`, PUT attack records (payload sets `__xssHits`), load https://hsong-test.web.app, check `window.__xssHits` is empty and payload appears only as harmless text; then delete the records and redeploy `hsong-test.rules.v2.json`, re-seed with `rules-anon.seed.json` and run `rules-anon.test.js` (46 pass).
- **Limits (be honest):** the guard changes the stored look of names with quotes (they are neutralised on read and written back that way). It does not protect other origins or things typed only by the local user. The old rider-application list on production may still hold browsers' old localStorage caches: the localStorage guard cleans them on read.

## 🏪 5f. Merchant (stall) secret-password login (code `app.js?v=10.32b_merchant_secret_login` pushed to main 2026-09-22 on the owner's phrase; rules v2.2 PUBLISHED to production 2026-09-21 with the owner's OK, verified with denial-only probes; rollback = `hsong-1f342.rules.v2.json` of git commit 286ccbf)
- Login = **shop code (`stallId`, e.g. APP-SHOP-6758, public) + secret password (8 chars, same format/hash as riders)**. Core: `merchantSecretLogin`, `stallFromApp`, `normalizeShopCode` (block after `riderSecretLogin`), UI `handleMerchantCodeLoginSubmit` (two inputs `merchant-shop-login-input` + `merchant-code-login-input`, red error box `merchant-login-modal-error`). Phone, stall number, the old 6-digit `accessCode` and the shop code alone no longer log anyone in.
- Password is created by the OWNER in `approveMerchantApplication` (now async) or with the 🔑 button (`resetMerchantLoginSecret(stallId)`) on the application card / detail modal / roster table; shown once in the SMS/LINE modal. Only `loginSalt` + `loginHash` are stored on `merchant_applications/<id>` AND `custom_market_stalls/<stallId>` (stall objects built from an application must go through `stallFromApp(app)` so they carry the hash; otherwise the merchant's own saves are rejected by the rules).
- Rules v2.2 (`build-rules.js`): non-owner cannot set/change/remove `loginHash`/`loginSalt` on existing `merchant_applications` or `custom_market_stalls`, and cannot create an application that carries `loginHash`/`loginSalt`/`accessCode`. `accessCode` stays as a public reference id (new approvals set it to the app id).
- `loginAsMerchantStall` is owner-only now; the merchant's own "จัดการ" button uses `reopenMyMerchantStall` (only re-enters the stall already logged in). Internal entry = `_enterMerchantStall`. The old owner shortcut typed as `ADMIN6305` in the merchant login box was removed (owner uses the per-stall "สลับเข้าร้าน" buttons).
- The public status-check modal and printed application no longer show any login code (they show the shop code only).
- Tests: `firebase-rules/tests/merchant-login.test.js` (36 checks), 9 new merchant/stall cases in `rules-anon.test.js` (now 55; seed adds `ZZSEEDM1`, `ZZSEEDS1`). Verified end-to-end on https://hsong-test.web.app (approve -> hashes on both nodes -> visitor login -> wrong password red box -> merchant edits allowed -> tampering with the hash rejected).
- **Rollout order (do not swap):** (1) DONE 2026-09-21: publish `hsong-1f342.rules.v2.json` (v2.2; compatible with the live code because no hash exists yet) with the owner's OK, (2) DONE 2026-09-22: push the code (owner phrase), (3) owner signs in on the real site and presses 🔑 for each REAL stall and sends the password. On 2026-09-22 the owner confirmed `APP-SHOP-6758` (ร้าน ส.ไก่สด) is HIS OWN TEST stall, but decided to KEEP it (too much work to re-enter its data). He pressed 🔑 on the real site, got a password and logged in as that stall successfully (production shows loginHash 64 / loginSalt 32 on both `merchant_applications` and `custom_market_stalls`; only this one stall exists now). Its old public 6-digit `accessCode` (AN4675) is still stored but no longer a credential. Do NOT delete this stall without asking. Until step 3 real merchants cannot log in (old codes stop working by design). Rollback file for rules = git `HEAD~` version of `hsong-1f342.rules.v2.json` (v2.1).

## 🔠 5g. Text size for low vision (code `app.js?v=10.33_text_size`, `styles.css?v=10.03_text_size`; on hsong-test, NOT pushed yet)
- Owner (low vision) found the text too small: ~1,300 uses of `text-[9px]`/`text-[10px]`/`text-[11px]`/`text-xs`. Header button "ตัวอักษร: ใหญ่" cycles ใหญ่ (default) -> ใหญ่มาก -> ปกติ; saved in localStorage `talathub_text_size`; class `text-large` / `text-xlarge` on `<html>` (set by an inline script in `<head>` before first paint). Code: `cycleTextSize`, `applyTextSizeMode`, `getTextSizeMode` in `app.js`.
- CSS is GENERATED: `node firebase-rules/tools/build-text-size-css.js` rewrites the block between `TEXT-SIZE:BEGIN/END` in `styles.css` from the font-size classes actually used in `app.js` + `index.html` (also `sm:` `md:` `lg:` `xl:` variants). Re-run it (and bump `styles.css?v=`) whenever new `text-[..px]` / `text-xs..` classes are added. Never edit the block by hand.
- Measured on hsong-test at phone width: text under 12px 90 of 174 elements (min 8px) -> 0 (min 13px) in ใหญ่; no horizontal overflow on home / market / checkout / tracking; header buttons fit (market-name pill hidden below `sm`).
- Known leftovers: 27 small icon-only buttons are still under 30px tall; "สมัครเป็นไรเดอร์" wraps mid-word in the rider promo card at large text (layout of that card should be redone when the home page is reorganised).

## 🛒 5h. Customer checkout gate + home page (code `app.js?v=10.34d_checkout_gate`; on hsong-test, NOT pushed yet)
- Old behaviour (bad): `processOrderCheckout` silently invented a customer ("คุณลูกค้าทั่วไป", phone 089-123-4567) and a fake address ("บ้านเลขที่ 12/3 ซอยเทศบาล 1...") when the customer had not logged in / set a location, so riders were sent to a made-up place. The fake address could also stay in customers' localStorage.
- Now: `validateOrderPrerequisites` / `enforceOrderPrerequisites` (called at the start of `processOrderCheckout` AND `simulatePaymentSuccess`) require: items in cart, no closed stall in the cart (`isClosed`), customer logged in, a 9-10 digit phone (`getCustomerContactPhone`; LINE-ID logins must type a phone in `#checkout-contact-phone-input`), and a real delivery location with lat/lng. Failures show a red box `#checkout-error-box` above the pay button and open the login / map. `isFakeDefaultDeliveryLocation` discards the old fake address (also in `loadSavedLocation`). The checkout field `#checkout-quick-house-no` (was never read) is now added to the order address.
- Tests: `firebase-rules/tests/checkout-gate.test.js` (23 checks, also runs `processOrderCheckout`; it would have caught a `selectedPayment is not defined` slip made while editing).
- Real bugs found on the way and fixed: `payMethodText` was undefined in `renderTrackingScreen` (customer order tracking threw an error whenever an order existed - broken since commit 2f7f872); `finalStallId/finalStallName` undefined in `openVendorSlipViewerModal` (vendor slip viewer broke); unguarded `RIDER_DATABASE`; COD orders showed random satang and "money received". COD now shows the real total and "เก็บเงินสดปลายทาง" (`payAmountExact` = total for COD).
- Undefined-variable scan (no-undef) of `app.js`: `firebase-rules/tools/eslint-undef.json` (install `eslint@8` in a temp folder, then `eslint --no-eslintrc -c firebase-rules/tools/eslint-undef.json app.js`). The 6 findings left are guarded by `typeof` and harmless. Run it before every push.
- Home page: the rider-recruitment banner moved to the bottom (before the floating cart); two big shortcut buttons `#home-quick-actions` ("ระบุที่อยู่จัดส่ง", "เลือกสินค้าเลย") call `scrollToHomeSection`; rider banner buttons no longer break words.
- **Deploy gotcha:** browsers cache `app.js?v=TAG`. Bump the tag on EVERY redeploy (even to the test site) or the old file is served.
- **Mobile-Frame gotcha:** this app has a PC/Mobile screen-mode toggle (`toggleScreenMode`, `state.screenMode`) that keeps `#main-app-container` at a fixed 430px width even when the real browser window is desktop-wide. Tailwind's `sm:`/`md:` classes react to the WINDOW width, not that 430px card, so anything using them inside that card can overflow/clip on a wide window in "mobile frame" mode (found 2026-09-22 in the rider-recruitment banner). Fix pattern: do NOT use Tailwind container-query classes (`@container`/`@[..]:`) for this — this project's Tailwind CDN build did not apply them reliably when classes are set dynamically via `element.className =`. Instead add a plain override in `styles.css` scoped by `#main-app-container.frame-mobile #id-of-the-element` (see the existing grid-column rules right above the rider-banner rule, and `#rider-recruit-banner-card`). Bumping `app.js?v=` is not enough for a `styles.css`-only change — bump `styles.css?v=` too, or the browser serves its cached copy.

## 🔗 5i. Order tracking code + fix for a live cross-customer data leak (code `app.js?v=10.35b_order_track_code`; on hsong-test, NOT pushed yet)
- **Owner-chosen approach for customer "login":** a short 6-char code per order (`generateOrderTrackCode`, alphabet `ABCDEFGHJKMNPQRSTUVWXYZ23456789`), NOT a persistent account. Created once when `simulatePaymentSuccess` builds the order (`order.trackCode`). Every tracking link built anywhere (rider/hub notifications, LINE messages, QR codes) now goes through `buildOrderTrackingUrl(order)` so it always carries `&code=...`. The customer's own tracking screen shows the code and a "คัดลอกลิงก์" button (`copyTrackingLink`, box `#tracking-share-box`).
- `handleTrackingDeepLink` (the ONLY place that opens someone else's order from a URL) now calls `resolveOrderTrackingAccess(order, code)`: wrong/missing code -> order is discarded, red toast, no data shown. Orders created before this feature (no `trackCode`) are let through (`legacy-no-code`) since they cannot be retro-fitted with a code - a known, accepted gap.
- **Honest limit (told to the owner):** this only gates what the WEBSITE shows. `orders` in the database rules is still a fully open node (no Firebase Auth for customers/riders/merchants), so someone querying the Firebase REST API directly with a guessed order id can still read it. Real server-side protection needs the bigger customer-auth project (still on the backlog). This closes the "guess/know the order id -> open it in a browser" path, which was the easy one.
- **Serious PRE-EXISTING bug found and fixed while testing this (not something added today):** three cloud-sync functions (`syncLatestOrderFromCloud`, `syncAdminOrdersFromCloud`, `listenToFirebaseOrdersForAdmin`) and the `orders` `child_added` realtime listener ran for EVERY visitor on EVERY page load / every ~30s window (not gated by role or owner sign-in despite the names), and set `state.activeOrder` to "whichever order anyone placed most recently that isn't delivered yet" - so a random visitor's tracking screen could silently fill with a stranger's name/phone/address just from having the site open. Reproduced live on `hsong-test` (confirmed the same code path exists unpatched on production `main` right now). Fixed with a shared helper `pickMyOwnActiveOrder(list)`: only adopts an order if it's already the one being tracked (by `orderId`) or its `customerPhone` matches the signed-in customer's own phone; otherwise returns `null` and leaves `state.activeOrder` untouched (admin/hub still get every order via `window._cachedFirebaseOrders` / their own list renders, unaffected).
- Tests: `firebase-rules/tests/order-track-code.test.js` (20), `order-broadcast-leak.test.js` (9, replays the exact `child_added` callback body to prove strangers' orders are no longer adopted while the customer's own phone-matched order and hub/admin notifications still work), `pick-my-order.test.js` (8). Verified live on hsong-test in a clean tab (cleared localStorage + reload): a stranger's order placed via direct REST PUT no longer appears; the same customer's own order (matching phone) still live-updates.
- **Recommend pushing this soon independent of the rest** - the cross-customer leak is live on production today and affects everyone, not just people using the new track-code links.

## 📋 5j. Verify rider documents before approval (code `app.js?v=10.36_rider_doc_verify`; on hsong-test, NOT pushed yet)
- **Problem:** `approveRiderApplication` never checked `app.docFlags` at all - the owner could click "อนุมัติ" straight from the roster list without ever opening the application or looking at the uploaded ID/license/registration photos. A rider could be approved (and start taking delivery jobs into people's homes) with zero human check that the documents even exist, let alone match the applicant.
- **Hard block:** `getMissingRequiredRiderDocs(app)` checks the 3 documents marked `required: true` in `RIDER_DOC_SLOTS` (selfie, license, registration - `vehicle` stays optional). If any is missing, `approveRiderApplication` refuses outright (no `confirm()`, no credential created), shows which document(s) are missing, and opens `viewRiderAppDetail` so the owner sees it immediately. A red "⚠️ เอกสารไม่ครบ" badge also shows on the roster card and the small pending-applications dashboard widget before the owner even clicks anything.
- **Explicit confirmation:** once all 3 required docs are present, the FIRST approval of that application shows a `confirm()` dialog naming the applicant and listing exactly what to check (opened the 3 photos, face matches the name, numbers/expiry are readable) before proceeding. Cancelling aborts with no side effects (no credential, no status change). On confirming, `app.docsVerifiedAt` / `app.docsVerifiedBy` are recorded and shown in the detail modal; re-approving later (e.g. after `reconsiderRiderApplication` moved it back to pending) does not ask again, since it was already verified once.
- `approveAndLoginRider` calls `approveRiderApplication` internally, so both approval paths are covered by one gate.
- Tests: `firebase-rules/tests/rider-doc-verify.test.js` (18 checks, including that `confirm()` is never even called when docs are missing, and that cancelling leaves no trace). Verified live on hsong-test: missing-docs card shows the badge and blocks approval without a dialog; full-docs approval (checked with `isFirebaseReady=()=>false` to avoid the known "reverts to pending" artifact of testing owner-only cloud writes without a real Firebase Auth sign-in) completes and records who/when verified.
- **Scope note:** only riders were changed (this was the specific risk named: a stranger physically entering someone's home to deliver). Merchant applications still approve without a similar documents review - a reasonable follow-up if wanted, since a bad merchant is lower physical risk than a bad rider.

## 🧭 5k. Header role-selector bar cut off on PC (code `index.html?` no version tag - bump `app.js?v=10.37b_role_bar_scroll` alongside it; on hsong-test, NOT pushed yet)
- **Bug (owner-reported with screenshots 2026-09-22):** on a real desktop browser (confirmed at 1536px and 1024px window widths, not the "Mobile Frame" simulator), the "5. แอดมิน" role button was clipped at the page's right edge, unreachable even after maximizing the window. Caused by the header's outer row (`<header><div class="max-w-7xl mx-auto flex flex-col md:flex-row ...">`) switching to a SIDE-BY-SIDE layout at `md:` (768px), cramming the logo + 2 auth buttons + all 5 role buttons into one row inside a content area capped at `max-w-7xl` (1280px) - too tight once "ตัวอักษร: ใหญ่" (default since section 5g) made every button bigger. Raising the breakpoint to `xl:` (1280px) was NOT enough (still failed at exactly 1536px, since the capped 1280px content width is the real constraint, not the window width).
- **Fix:** removed the side-by-side breakpoint entirely - the logo/auth row and the 5-button role bar are now ALWAYS stacked on their own lines at any screen width (matches how it already behaved on mobile, which never had this problem). The role bar itself keeps `overflow-x-auto scrollbar-none` (already present) as a safety net, plus `setupDragScroll('main-role-selector-bar')` for convenient click-drag scrolling with a mouse if it ever still overflows.
- **Known remaining rough edge:** on a REAL narrow phone (375px) with large-text mode, the 5 role buttons can still slightly overflow their own row; a phone's native touch-swipe can reveal the rest (unlike the PC bug, which had no way to reach it at all), so it is a much smaller issue - not fixed in this pass since the owner's report was specifically about PC.
- Verified visually (screenshots) at 1536px, 1024px and 375px after the fix: all 5 buttons visible and clickable on desktop widths.

## 🏷️ 5l. Admin badge mispositioned + role bar still tight on real phones + rider button label hidden + hub badge same bug (code `app.js?v=10.42_hub_badge_pos_fix`; on hsong-test, NOT pushed yet)
- **Bug 1 (owner screenshot, real phone):** the red "!" notification dot (`#role-admin-badge`, `position:absolute`) was rendering on top of the "4. ไรเดอร์" button instead of "5. แอดมิน". Root cause: `setActiveRoleView()` rewrites each role button's `className` WHOLESALE on every single role switch, and BOTH admin templates (active + inactive) were missing the `relative` class (a pre-existing gap, not something introduced in section 5k) - without it, the badge's positioning parent fell back to the whole scrollable bar (`#main-role-selector-bar`), so it rendered at a fixed offset from the BAR's edge, landing wherever that happened to coincide with another button. Fixed by adding `relative` back to both templates. Proven via `offsetParent`/`getBoundingClientRect()`: before the fix the badge's offsetParent was the bar; after, it's `role-btn-admin` itself, positioned correctly at that button's own corner.
- **Bug 1 was only half-fixed:** the owner tested again and still saw the red dot look wrong, floating right at the edge next to "ไรเดอร์". Root cause found by measuring on a real-width viewport: `#role-admin-badge` anchors to the admin button's TOP-RIGHT corner (`-top-1 -right-1`), but the admin button is the RIGHTMOST of the 5 buttons - on any narrow phone where the bar overflows, the button's right edge (and therefore the badge sitting on that corner) is exactly the part that gets scrolled off-screen, so the badge appeared to hover at the visible cut edge near the neighbouring "ไรเดอร์" button even though it was correctly parented to the admin button the whole time. **Fixed by moving the badge to the TOP-LEFT corner** (`-left-1` in `index.html`) - the admin button's left edge is always the first part to become visible, so the badge is now reliably on-screen.
- **Bug 2 (owner screenshot, real phone), and its own follow-up:** the 5-button role bar overflowed on narrow phones (measured 152px, later 43-59px depending on which button is active - the ACTIVE button is visually bolder/wider than inactive ones). Shrinking padding/gaps/hiding number prefixes (done earlier) only reduces the overflow, it can't fully eliminate it without hurting the large-text readability the owner (low vision) relies on. **Real fix: `setActiveRoleView()` now auto-scrolls the bar** (`roleBar.scrollLeft`, inside a `requestAnimationFrame`) so that whichever button is ACTIVE is always scrolled fully into view after every role switch - both the button and the badge sitting on its corner. Native touch-swipe/drag-scroll still work for browsing the other buttons.
- **Bug 3 (owner-reported, real phone):** the "สมัครไรเดอร์" button in the top header row (next to "เปิดร้าน") showed only its icon (a small blue circle), never its label, on any phone. Root cause: its label span used `class="hidden xs:inline sm:inline"` - **`xs:` is not a real Tailwind breakpoint** in this project's config, so the whole rule collapsed to `hidden sm:inline`, meaning the label was invisible below 640px width - i.e. on every real phone, always. Present in two places (`index.html`'s static button AND the `renderAuthHeaderButtons()` template in `app.js` that rebuilds this button on every load/login-state change). Fixed by removing the broken responsive classes entirely - the label now always shows, matching how the "เปิดร้าน"/"ลงทะเบียนเปิดร้าน" merchant button already worked correctly (it had a proper mobile-vs-desktop short/long label pair, this one just had a typo'd breakpoint). Since showing the full label makes this top row slightly overflow on the narrowest phones (~17px measured at 375px), also made `#top-auth-buttons-container` scrollable (`overflow-x-auto scrollbar-none`, `min-w-0` on it and its parent so it can actually shrink instead of pushing the row wider) plus `setupDragScroll('top-auth-buttons-container')` for mouse drag - same safety-net pattern already used for the role bar.
- **Bug 4 (found while re-investigating Bug 1, NOT separately reported by the owner but same root cause):** the amber "NEW" pill on the "2. จัดส่ง" (Hub) button (`#hub-badge-count`) had the *exact same* missing-`relative` bug as Bug 1, just in the OTHER two `setActiveRoleView()` templates (the ones shared by จัดส่ง/แผงค้า/ไรเดอร์, not the admin-specific ones). Every role switch stripped `relative` from `#role-btn-hub`, so the "NEW" badge fell back to anchoring on the whole bar and floated near whichever button happened to be at that fixed offset from the bar's edge (often near ไรเดอร์/แอดมิน) - this is almost certainly what the owner was actually pointing at when asking "ทำไมต้องแสดงจุดแดงตรงตำแหน่งไรเดอร์" even after Bug 1's fix, since this second badge was never touched by that fix. Fixed the same way: added `relative` back to both templates. Proven live: `hub-badge-count.offsetParent.id` stays `"role-btn-hub"` across repeated `switchRole()` calls now (was `"main-role-selector-bar"` before).
- Test: `firebase-rules/tests/admin-badge-position.test.js` (13 checks, static - greps all 4 non-customer className templates in `setActiveRoleView` for `relative`/`shrink-0`, checks both badge spans in `index.html` use the right corner classes, checks `setActiveRoleView` contains the `scrollLeft` auto-scroll code).
- Verified live on hsong-test at 360-375px width: switched to "ไรเดอร์" (rightmost overflow case) - both the rider button and the admin button+badge are fully within the bar's visible rectangle (`getBoundingClientRect` checked programmatically, not just eyeballed), bar auto-scrolled from 0 to its max scroll position; switched back to "ลูกค้า" (leftmost) - bar scrolled back to the start correctly; "สมัครไรเดอร์" label renders in full and is reachable by scrolling the header row; "NEW" pill now visually sits directly on the "จัดส่ง" button, not floating near ไรเดอร์/แอดมิน.

## 🧹 5m. "ล้างข้อมูลทดสอบ" (rider fleet) silently did nothing on the cloud, even signed in as owner (code `app.js?v=10.44_clear_rider_data_per_item`; PUSHED to main 2026-09-23 on the owner's phrase, owner has not yet confirmed a real test with real owner sign-in)
- **Bug (owner-reported):** clicking "ล้างข้อมูลทดสอบ" on the rider fleet admin page (`clearFleetTestData()`), while genuinely signed in as the real owner (Super Admin, confirmed via the header showing "เฮียส่ง (Super Admin)" + "Realtime Cloud DB"), cleared nothing from the cloud - a GET on production confirmed `rider_applications/RD3562` was still there afterward, on both PC and mobile.
- **Root cause:** `clearFleetTestData()` called `db.ref("rider_applications").set([])` and `db.ref("community_riders").set([])` - a **whole-node** write/delete at the PARENT path. Rules v2 (`build-rules.js`) only defines `.write` at the **child `$id` level** for these two nodes (`rider_applications`, `community_riders`), not at the parent. Firebase does not "look inside" child-level `.write` rules to authorize a write/delete made AT the parent path - if the parent itself (and none of its ancestors) grants `.write: true`, a whole-node `set()`/`remove()` there is rejected outright, **no matter who is authenticated**. This is exactly why the rest of the codebase already writes these nodes per-item (`syncKeyedToCloud`/`syncListToCloud`, see section 5c: "Never add a whole-node set()/REST PUT for these nodes again") - `clearFleetTestData()` was simply never updated to that model when rules v2 shipped. The failure was invisible because the rejected promise was swallowed by `.catch(console.warn)`.
- **Same bug, second place:** `cleanRiderDatabase()` (the bigger "purge everything" owner-only function, reachable from a settings button) had the identical mistake for `rider_documents`, `rider_private`, `community_riders`, `rider_applications` - plus a companion block of anonymous `fetch(...).json(), {method:"DELETE"}` REST calls with **no auth token at all**, which could only ever have worked on the already-open nodes (`riders`, `active_rider`, `rider_locations`, `rider_status`) and was dead code for the owner-gated ones - removed as redundant since `db.ref(...).remove()` already covers the open nodes.
- **Fix (both functions):** capture the current rider/application ids from `loadCommunityRiders()`/`loadRiderApplications()` *before* clearing local storage, then delete each one individually (`db.ref("community_riders/" + id).remove()`, etc.) - matching the per-item write pattern already used everywhere else. Also added a `requireOwnerAction()` gate to `clearFleetTestData()` (it had none before; relied only on the rules rejecting it, which is exactly what was silently failing) so a non-owner now gets an immediate, honest "owner only" toast instead of a no-op cloud call. Nodes that are genuinely open at the parent level (`active_rider`, `rider_locations`, `rider_status`, `riders`) are untouched - whole-node `.remove()` on those was never the problem.
- Test: `firebase-rules/tests/clear-rider-data-per-item.test.js` (18 checks, static - asserts neither function contains a whole-node `.set()`/`.remove()` on the four restricted nodes, and both delete per-id instead).
- **Could not verify live with real owner auth** (Claude does not have and does not ask for the owner's password, per section 4) - the fix is grounded in Firebase's documented write-rule evaluation (a write is authorized only by a `.write` rule at the exact path written or an ancestor of it, never a descendant) and mirrors the pattern already proven to work for every other per-item sync function in this codebase. Owner should verify on hsong-test by seeding a throwaway pending application there, signing in as the test project's owner, and confirming "ล้างข้อมูลทดสอบ" actually removes it from the cloud (a GET on `hsong-test`'s `rider_applications` afterward should come back empty). Owner gave the push phrase before this live verification happened - pushed to production on the strength of the code-level diagnosis alone.

## 🧽 5n. Home page decluttered: removed 2 non-interactive header blocks (code: `index.html` only, no `app.js` change; PUSHED to main 2026-09-23 on the owner's phrase)
- **Owner's request (2026-09-23), with screenshots:** two blocks near the top of the customer home page felt cluttered on mobile and led to no action: (1) the green "จัดส่งที่ ▾ / แตะระบุที่อยู่จัดส่ง / รอบ HH:MM น." header bar right above the banner carousel, and (2) the dark "ศูนย์กระจายตลาดวิศิษฐ์ชัย อ.บ้านบึง / Hub ต้นทาง / พิกัดต้นทาง / ส่งตรงถึงหน้าบ้านคุณ" card inside the in-page location section.
- **Checked before removing:** block (2) has no `onclick` and no id read by `app.js` - pure decoration, zero functional risk. Block (1) DOES have a real function (`onclick="openLocationModal()"`) and live-updates two ids (`selected-address-preview`, `top-delivery-slot-text`) from `updateDeliveryLocationUI()` in `app.js` - but both reads are `if (el) ...`-guarded, so removing the elements is safe (no crash, just a no-op update). Its function is not lost: the "ระบุที่อยู่จัดส่ง" home-quick-action button (section 5h) and the "เปลี่ยนพิกัด" button inside the in-page location card both call the same `openLocationModal()`.
- **Owner chose:** remove BOTH blocks entirely (was offered a middle option - shrink block 1 instead of removing it - and declined; accepted the tradeoff that the address-prompt is no longer the very first thing seen on page load, relying on the quick-action button lower down instead).
- Both blocks replaced with an HTML comment noting what was removed and why, not deleted silently, so a future revert is a one-line lookup.
- Verified: `node -c app.js` still passes (file untouched), full local test suite still passes (none of it depended on these DOM nodes), live on hsong-test at 375px both blocks are gone, the home page still functions, and the "ระบุที่อยู่จัดส่ง"/"เลือกสินค้าเลย" shortcut row and the delivery-slot countdown card render normally right where the removed blocks used to be above them.

## 🔍 5o. Merchant express-dispatch destination map got the same place-search box as the customer's (code `app.js?v=10.45_merchant_dest_search`; PUSHED to main 2026-09-23 on the owner's phrase)
- **Owner's question (2026-09-23):** when a merchant calls a rider to deliver somewhere OTHER than the stall itself (`openMerchantDestinationMap()` / `#merchant-map-modal`), pinning that destination felt harder than the customer flow. Checked why: the merchant map modal only had a GPS button (useless here - the merchant isn't physically at the destination) and tap/drag-to-pin on a small map, with **no search box at all** - not even the "OK, decent" free-text search the customer flow already has. So this really was harder, not just a feeling.
- **Fix:** reused the customer's entire place-search system (Nominatim text search + curated `BANBUENG_LANDMARKS` list + Google-Maps-link/Plus-Code paste, all already built) and wired it to the merchant's map instead of duplicating ~150 lines of search/shortlink-resolution logic. Mechanism: a module-level `_activeLocationSearchTarget` ("customer" | "merchant") plus `_lsId(key)`/`_lsEl(key)` helpers that resolve to the right DOM ids (`LOCATION_SEARCH_IDS.customer.*` vs `.merchant.*`) - every shared search function (`handleLocationSearchInput`, `fetchOnlineLocationSearch`, `renderLocationSearchResults`, `renderGoogleMapsShortlinkHelper`, `pasteFromClipboardToSearch`, `clearLocationSearch`, `hideLocationSearchDropdown`, `executeLocationSearchNow`, `openGoogleMapsSearchHelper`, the clipboard-paste watcher) now reads/writes through these helpers instead of hardcoded `location-search-*` ids, so the exact same code path serves either map depending on which one is "active." `selectLocationSearchResult(item)` branches at the top: if target is `"merchant"` it delegates to a new `_applyLocationResultToMerchantMap(item)` that reuses the EXISTING merchant map objects/functions (`merchantPickerMap`, `merchantPickerMarker`, `_onMerchantMapPinMoved`) instead of creating a second map.
- Five thin wrapper functions (`handleMerchantLocationSearchInput`, `executeMerchantLocationSearchNow`, `pasteFromClipboardToMerchantSearch`, `clearMerchantLocationSearch`, `openGoogleMapsSearchHelperMerchant`) just set `_activeLocationSearchTarget = "merchant"` then call the shared function - these are what the new merchant search box's `onclick`/`oninput` attributes call.
- Target resets automatically: `openMerchantDestinationMap()` sets it to `"merchant"` on open, `closeMerchantMapModal()` resets it to `"customer"` on close, and `openLocationModal()` (customer) also defensively resets it to `"customer"` - so the two pickers can never cross-contaminate even if a merchant is also a logged-in customer testing both in one session.
- `index.html`: added a compact version of the customer's search box (input + paste/clear buttons + results dropdown + "open Google Maps" banner) inside `#merchant-map-modal`, between the GPS button and the map, with `merchant-` prefixed ids.
- Test: `firebase-rules/tests/merchant-destination-search.test.js` (20 checks, static - all 5 wrapper functions exist and are `window`-bound, open/close set/reset the target variable, `selectLocationSearchResult` branches to the merchant path, `_applyLocationResultToMerchantMap` reuses the existing merchant map/pin-moved functions, and all 6 new ids exist in `index.html`).
- Verified live on hsong-test: opened the merchant destination map (simulated `state.activeMerchant` in the console, no cloud write involved), typed "โรงพยาบาลบ้านบึง" - got both the curated local landmark and an online Nominatim result, selected one - map recentered, address field auto-filled, distance/fee calculated (~0.61 กม., ฿20), confirm button activated; closed the modal and confirmed the CUSTOMER's own home-page search (typed "ตลาดวิศิษฐ์ชัย") still works exactly as before, unaffected.
- **Follow-up bug (owner-reported with screenshot, real phone, right after this shipped):** on a real phone, once a destination was pinned, the page could not scroll down far enough to reach the confirm/pay button - stuck. Root cause: `#merchant-map-modal`'s inner panel was a `flex flex-col` with a hard `max-height:100dvh` and every child (header, GPS button, the NEW search box, the map, distance/fee, address input, confirm button) was `shrink-0` except the map (`flex-1`) - adding the search box pushed the total fixed-height content past the screen height on shorter phones, and the OUTER overlay had `style="touch-action:none"` (originally meant to stop the page behind it scrolling) which also blocks touch-scrolling inside the modal itself, so there was no way to reach the overflow. **Fixed** by wrapping everything except the header and the confirm button in one `<div class="flex-1 min-h-0 overflow-y-auto">` (GPS button, search box, map, distance/fee, address input all scroll together now), moving the Confirm Button OUTSIDE that wrapper so it's pinned at the bottom and always visible without scrolling, giving the map a plain fixed `height:260px` (was `flex-1`/`min-height`, meaningless once it's inside a scrolling block instead of a flex row), and removing the outer `touch-action:none` (background-scroll lock already comes from `document.body.style.overflow = "hidden"`, set in `openMerchantDestinationMap()` - a widely-used-enough pattern elsewhere in this app that the extra `touch-action:none` wasn't needed and was actively harmful here). Verified live at a short 419×600 viewport (matching the real-phone report): scroll area now genuinely scrolls (`scrollHeight` 613px inside a 455px `clientHeight`), and the confirm button sits fixed at the bottom, reachable and clickable, without any scrolling needed at all.

## 🏷️ 5p. Stall category was silently stuck at "chicken" for every merchant - found while auditing the random stall-rotation system (code `app.js?v=10.47_unified_stall_category`; on hsong-test, NOT pushed yet)
- **Owner's request (2026-09-23):** audit the random/rotating stall-display system (`selectRandomStallBatch`, `startStallRotationTimer`, the "🎲 หมุนเวียนอัตโนมัติ" bar on the home page) for correctness and summarize how it works, with recommendations.
- **How it actually works (confirmed by reading the code, no behavior changed by the audit itself):** every 30s (or on manual shuffle), `selectRandomStallBatch()` (1) always puts newly-approved/self-edited stalls first, (2) tries to pick one stall from each of 5 stall-type categories (`chicken`/`pork`/`veggie`/`curry`/`seafood`) for variety, (3) fills any remaining slots with a random pick from the rest, (4) narrows to the active category if the customer has one selected, (5) always pins the logged-in merchant's own stall to the front. `renderCatalog()` then renders whichever stall ids `selectRandomStallBatch()` chose.
- **Bug found:** step (2)'s category diversity can never actually work, because **every stall's `category` field is permanently stuck at `"chicken"`**, regardless of what it really sells. Root cause: the registration/edit form's `document.getElementById("m-stall-category")?.value || "chicken"` (used both when a merchant submits a new application and when they edit their profile) has been reading from an element that **doesn't exist** - the merchant form was simplified at some point (removed stall number/zone/category inputs entirely, restructured contacts into `contact1`/`contact2`) and this line was never updated, so it always falls through to the `"chicken"` fallback. There was also no way to fix it afterward - no owner/admin-side control for a stall's category existed anywhere either.
- **Why nobody noticed yet:** the rotation bar hides itself whenever there's ≤1 real stall with products, and production currently has exactly one real stall (`APP-SHOP-6758`, ร้าน ส.ไก่สด) which happens to genuinely sell chicken - so the wrong default looked correct by coincidence and the feature has effectively never been exercised with real, diverse data.
- **Confirmed NOT affected:** the customer's actual product search/category browsing (the emoji-labeled Thai tabs like "🥩 เนื้อสัตว์และสัตว์ปีก") uses a completely separate, correctly-working taxonomy stored per-PRODUCT, not this simple 5-value stall-level tag - so this bug never hid products from customers. It only broke the rotation's own diversity goal and mislabeled `category` wherever it's shown standalone (the favorite-stalls-bar color coding, the admin roster's "หมวดหมู่" column, printed sheets).
- **Fix, round 1 (2026-09-23 morning):** added back `<select id="m-stall-category">` to the registration/edit form (right under the shop name field), initially with the 5 legacy stall-only options (`chicken`/`pork`/`veggie`/`curry`/`seafood`) to match what the rotation system/color-coding/admin roster expected at the time.
- **Related, separate bug found while checking this - owner asked to fix together before testing (2026-09-23), so fixed in the same pass:** the "1-Click กรอกข้อมูลตัวอย่าง" (`fillSampleMerchantRegistration()`) test-fill button for merchant registration was broken - it called `document.getElementById(...)` on several ids (`m-stall-number`, `m-stall-zone`, `m-owner-name`, `m-phone`, `m-highlight`, `m-desc`) that no longer exist in the simplified form (same root cause: form restructured to `contact1`/`contact2` + owner nicknames, this helper never updated), throwing partway through instead of filling anything. Also had a second latent bug: it referenced `MERCHANT_PRESET_IMAGES.stall.beef`, a key that has never existed (only `chicken`/`veggie`/`pork`/`curry`/`seafood`), which would have set the 3rd stall photo to the literal string `"undefined"`. **Fixed** by rewriting it against the current real field ids (`m-owner1-nickname`, `m-contact1-name/phone/line`, `m-bank-name`/`m-bank-account-no`/`m-bank-account-name`, etc.) through a small `setVal(id, val)` helper that checks the element exists before writing - so a future form change degrades gracefully (skips the missing field) instead of throwing and aborting the whole fill; also swapped `.beef` for the valid `.curry` preset image.
- **Owner noticed (2026-09-23, with screenshots) the new stall-category dropdown had only 5 options while the per-product category dropdown (used when adding items to the catalog) has 10 (+1 "อื่นๆ") - asked why, then asked to unify them.** Root cause: the codebase had always had TWO separate, unrelated category vocabularies - the rich 10-category Thai/emoji taxonomy (`CATEGORY_TAXONOMY_3TIER` / `getMainCategories()`) used for products and the customer's own category-browsing tabs, and a much smaller legacy 5-value English-keyword set (`chicken`/`pork`/`veggie`/`curry`/`seafood`) used only for the stall-level tag this section is about. There was no reason for these to differ - `normalizeMainCategoryName()` already treats `"chicken"`/`"pork"`/`"seafood"`/`"vegetable"` as synonyms that map into the rich taxonomy, confirming the two were always meant to line up.
- **Fix, round 2 (2026-09-23 afternoon, unify the two systems):**
  - `<select id="m-stall-category">` now lists the same 11 categories as `CATEGORY_TAXONOMY_3TIER` (`getMainCategories()`), with option **values** matching those Thai/emoji strings character-for-character (not the old short English keywords).
  - New constant `const DEFAULT_STALL_CATEGORY = getMainCategories()[0];` (first category, "🥩 เนื้อสัตว์และสัตว์ปีก") replaces every `|| "chicken"` fallback across the 5 read/write sites (registration, edit-prefill, live preview, and `fillSampleMerchantRegistration()`'s own fill value).
  - `selectRandomStallBatch()`'s diversity-picking category list (`const categories = [...]`) now calls `getMainCategories()` instead of the hardcoded 5-value array, so the rotation genuinely spreads across all 10 real categories once enough diverse stalls exist.
  - New `getStallCategoryColorClass(rawCategory)` replaces the old 5-way `if/else` chain for the favorite-stalls-bar color coding; it runs the value through `normalizeMainCategoryName()` first, so it still colors OLD stalls correctly even though they carry a legacy value like `"chicken"` (backward compatible, no data migration needed) and falls back to a neutral color for anything unrecognized.
  - `previewMerchantLiveStore()`'s dead `categoryNames` lookup table (kept a completely different 9-key vocabulary - `pork`/`chicken`/`beef`/`seafood`/`vegetable`/`egg`/`frozen`/`dryfood`/`all` - that never matched the stall-category values anyway) was removed; the preview badge now just displays `category` directly, since it's already a human-readable Thai label.
  - `normalizeMainCategoryName()` gained two missing legacy synonyms (`"veggie"` → ผักสด, `"curry"` → อาหารปรุงสุก) for completeness/robustness, even though no real stall currently carries those exact old values.
- Test: `firebase-rules/tests/merchant-stall-category.test.js` (33 checks now - reads the real 11 categories straight out of `CATEGORY_TAXONOMY_3TIER` and asserts the stall-category `<select>` has exactly those same 11 values with no legacy short keyword left over, `DEFAULT_STALL_CATEGORY` and its use everywhere replacing the old `"chicken"` fallback, `selectRandomStallBatch` and `getStallCategoryColorClass` use the unified system, plus everything from round 1 about `fillSampleMerchantRegistration()`).
- Verified live on hsong-test: registration form's category dropdown now shows all 11 real categories with `DEFAULT_STALL_CATEGORY` ("🥩 เนื้อสัตว์และสัตว์ปีก") pre-selected; `getStallCategoryColorClass('chicken')` (an old real stall's stored value) still returns the correct color with no crash, `getStallCategoryColorClass('🦐 อาหารทะเลสดและแปรรูป')` (a new-style value) returns a distinct color, and an unrecognized value returns the neutral fallback; `selectRandomStallBatch()` + `renderCatalog()` run cleanly against the test project's several seeded stalls with no errors.

## 🎲 5q. Admin can now edit a stall's category directly + real Fisher-Yates shuffle + a genuine dual-array save bug found along the way (code `app.js?v=10.49_stall_dual_array_sync_fix`; on hsong-test, NOT pushed yet)
- **Owner's request (2026-09-23):** implement the 2 remaining recommendations from the stall-rotation audit (section 5p): (2) let the admin edit a stall's category themselves from the stall-management page, (3) switch the rotation's shuffling to a proper Fisher-Yates algorithm (the `.sort(() => 0.5 - Math.random())` pattern is well known to not shuffle uniformly).
- **Item 2:** added an inline `<select>` (built from `getMainCategories()`, the same unified 11-category list from 5p) directly in the admin stall roster table (`renderAdminStalls()`), next to each stall's name - changing it calls a new `adminUpdateStallCategory(stallId, newCategory)`. Pre-selects the right option even for a stall still carrying an old legacy value (e.g. `"chicken"`) via `normalizeMainCategoryName()`.
- **Item 3:** new `shuffleArray(arr)` helper (proper Fisher-Yates) replaces all 3 `.sort(() => 0.5 - Math.random())` call sites inside `selectRandomStallBatch()`.
- **Real bug found while building item 2 (not something introduced today - pre-existing):** `ALL_100_STALLS` and `MARKET_DATA` hold **separate object copies** for the same `stallId`, not the same reference, despite being kept "in sync" by most code paths. `adminUpdateStallCategory`'s first draft (and the pre-existing `toggleStallOpenStatusByAdmin`, which has the exact same lookup pattern: `ALL_100_STALLS.find(...) || MARKET_DATA.find(...)`) only mutated whichever object was found FIRST (`ALL_100_STALLS`, since it's checked first) - the admin roster table also *displays* from `ALL_100_STALLS`, so on screen it looked like "เปิด/ปิดร้าน" (open/close) worked instantly, but `saveMarketDataToStorage()` only ever persists from `MARKET_DATA`, which was never touched - so the change was **silently never saved**, and would have reverted the next time the page reloaded or re-synced from the cloud. Confirmed live with a direct object-identity check (`ALL_100_STALLS.find(...) === MARKET_DATA.find(...)` → `false`) and by toggling and re-reading both arrays.
- **Fix:** both `adminUpdateStallCategory` and `toggleStallOpenStatusByAdmin` now look up the stall in BOTH arrays separately and write the new value to whichever one(s) exist, instead of stopping at the first match. Checked `deleteStallByAdmin` for the same class of bug - it already correctly removes the stall from both arrays, no fix needed there. Also checked the two remaining read-only `ALL_100_STALLS.find(...) || MARKET_DATA.find(...)` sites (`toggleFavoriteStall`, a checkout-summary label lookup) - neither mutates the result, so they were never affected.
- Test: `firebase-rules/tests/admin-stall-category-edit.test.js` (12 checks - the admin category select exists and is wired to `getMainCategories()`, both admin-mutation functions write to both arrays, `deleteStallByAdmin` is confirmed clean, `shuffleArray` exists and is used in all 3 of `selectRandomStallBatch`'s former shuffle sites, and no `.sort(() => 0.5 - Math.random())` remains in real code).
- **Deploy note for future reference:** hit the documented "browsers cache `app.js?v=TAG`" gotcha firsthand while verifying this - fixed the dual-array bug, redeployed to hsong-test, and initial verification still showed the OLD (buggy) function body running in the browser console because the version tag hadn't been bumped for that specific redeploy. Bumping the tag and redeploying again fixed it. A reminder that every deploy needs a fresh tag, even a quick follow-up fix minutes after the previous one.
- Verified live on hsong-test (after the correct redeploy): `adminUpdateStallCategory` and `toggleStallOpenStatusByAdmin` both now write matching values into `ALL_100_STALLS` and `MARKET_DATA` for the same stall (checked directly by reading both arrays after calling each function).

## 🗂️ 5r. Removed "zone" (โซน A/B/C/D/E) for stalls entirely, replaced with the unified 11-category system (code `app.js?v=10.50_stall_zone_removed`; on hsong-test/sandbox only, NOT pushed yet)
- **Owner's decision (2026-09-23):** the market isn't big enough to need a physical zone layout, and the old `zone` field was self-declared by each merchant at registration (not assigned/enforced by the market) - it never tracked what a stall actually sells, especially once a stall changes hands (section 5p already found the category field itself had the same kind of drift). Owner: locals already know where each stall is by name/number, so drop zone entirely rather than fix it.
- **Scope: only the STALL "zone" concept.** The codebase also has an unrelated `zone`/`โซน` field for RIDERS (their preferred delivery coverage area, e.g. "ตำบลบ้านหนองชาก") - `onpage-rider-zone`, `rider-form-zone`, `app-edit-zone`, `reg-rider-zone` and all rider read/write sites were deliberately left untouched.
- **Replaced with category everywhere it was shown/filtered:** customer stall-directory tabs (`#directory-zone-tabs` → `#directory-category-tabs`, `filterDirectoryZone`→`filterDirectoryCategory`, now 12 buttons: ทั้งหมด + the same 11 categories as product browsing/registration), the admin stall roster's zone filter buttons (the screenshot that started this - `filterAdminStallsByZone`→`filterAdminStallsByCategory`, `_adminStallZoneFilter`→`_adminStallCategoryFilter`), the admin dashboard's "ผังโซน 100 แผงค้า" mini-grid (now groups by category), the merchant self-service header/portal badges, the stall detail modal, the admin's merchant-application edit form (`merchant-edit-zone` select → `merchant-edit-category`, previously had NO category field at all), and the merchant directory A4 print (โซน column → หมวดหมู่ column using `s.category`).
- **Removed outright (no replacement) where it was purely a location label, not a filter:** vendor settlement report/CSV/print rows and headers, print packing slips, the rider "ร้านอยู่โซน X แผง Y" quick-message button (now just "ร้านอยู่แผง Y"), the merchant express-order rider-facing origin display. These were location text only, not something to filter by, and the owner said the market is small enough that riders/hub staff don't need it.
- **Dead code found along the way (not something this change caused):** several `zone` reads already had no matching form field to read from (`m-stall-zone` doesn't exist in the merchant registration/edit form - same root cause as section 5p's category bug: the form was simplified and this line never got cleaned up), so they were already silently producing empty/placeholder values in production before this change. Also found and removed a duplicate zone display sitting right next to an already-shown category value in 3 places (stall detail modal, admin roster table, merchant live-preview banner) - the zone text there was pure redundant clutter even before this change.
- Filtering matches old legacy category values too via `normalizeMainCategoryName()` (same helper from section 5p), so stalls that still carry an old value like `"chicken"` still show up correctly under the new category tabs - no data migration needed. The stored `zone` field itself is left alone on old records (harmless unused leftover), only new code stops reading/writing it.
- Test: `firebase-rules/tests/merchant-stall-zone-removed.test.js` (asserts every old zone identifier is gone from both `app.js` and `index.html`, every new category identifier exists, the customer directory tab bar has exactly 12 buttons, the admin edit-form category select has exactly 11 options, both new filters compare through `normalizeMainCategoryName`, and all 4 rider-zone ids are still present untouched).
- Verified in an offline sandbox (fake `127.0.0.1:9` host, no cloud writes): opened the customer stall directory - category tabs render and filter correctly (`state.currentDirectoryCategory` updates, active button styling moves); opened a stall's catalog modal - header badge shows the category instead of a zone; logged in as a mock admin and opened "จัดการร้านค้า & แผงค้า" → "ทำเนียบ & ใบสมัคร" - the exact table from the owner's screenshot now shows category filter tabs and the "เลขแผง" column with no zone text, matching the already-existing per-row category dropdown from section 5q. Not yet verified with real data on hsong-test (the local sandbox has no seeded stalls, so a single injected test stall was used).

## 🙈 5s. Merchant no longer sees the "จัดส่ง" (Hub) / "ไรเดอร์" tabs while logged in (code `app.js?v=10.51_merchant_hide_hub_rider_tabs`; on hsong-test/sandbox only, NOT pushed yet)
- **Owner's decision (2026-09-23, prompted by a screenshot of a logged-in merchant's header):** switching role never logs anyone out (`state.activeMerchant` stays in memory regardless of which role view is on screen) and Hub/Admin are gated behind the owner's own Firebase Auth login while Rider needs its own separate secret password - so a real merchant tapping "จัดส่ง" or "ไรเดอร์" only ever hits a login wall that has nothing to do with them. Owner agreed this is just confusing clutter for non-technical vendors and asked to hide both. "ลูกค้า" stays visible on purpose - the merchant portal already has a "ดูหน้าร้านในตลาด" button that deliberately switches to that same view to preview the store or place a personal order.
- **Two separate places had to change** (found while implementing, not obvious from the screenshot alone): (1) the main 5-button role bar at the top of every screen (`#main-role-selector-bar`, `role-btn-hub`/`role-btn-rider`), toggled by a new shared helper `updateHubRiderTabsVisibility()` (checks `state.currentRole === "merchant"`); and (2) a second, separate "สลับบทบาท:" mini bar that only exists inside the merchant portal's own header (`index.html`, "Cross-Role Quick Navigation Bar for Merchant") - this one always had exactly 3 static buttons (ลูกค้า/จัดส่ง/ไรเดอร์) with no admin button at all, so its Hub/Rider buttons were just deleted from the markup directly (no conditional needed, since this block only ever renders while already in the merchant view).
- `updateHubRiderTabsVisibility()` is called from both places that already had to re-assert `role-btn-admin`'s visibility for the same reason (`setActiveRoleView()`, called on every role switch, and `renderAuthHeaderButtons()`, called after login/logout actions) - the main role bar's buttons get their whole `className` overwritten on every switch (see section 5l), so anything that hides a button has to be re-applied every time, not just once.
- Test: `firebase-rules/tests/merchant-hide-hub-rider-tabs.test.js` (11 checks - the helper exists, is window-bound, checks `state.currentRole`, touches only the hub/rider buttons and never the customer button, is called from both of the two functions above, and the merchant portal's own mini nav bar keeps its customer button but has no `switchRole('hub')`/`switchRole('rider')` left in its markup).
- Verified live in an offline sandbox: simulated a logged-in merchant session and called `switchRole('merchant')` - both `role-btn-hub` and `role-btn-rider` gained the `hidden` class (confirmed via `classList.contains`), `role-btn-customer` did not; the merchant portal's own mini bar showed only one "ดูหน้าลูกค้า" button; switching back to `switchRole('customer')` removed `hidden` from both buttons again, confirming the main bar returns to all 5 for every other role.

## 🎨 5t. Merchant registration form UI/UX pass (code `app.js?v=10.52_merchant_reg_form_uiux`; on hsong-test/sandbox only, NOT pushed yet)
- **Owner's request (2026-09-23, from a screenshot of the "ลงทะเบียนแผงค้าใหม่" form):** owner gave 4 specific observations - (1) the form felt like one undifferentiated wall of text/boxes with no visual distinction between "just FYI" content and "you must fill this in", (2) grouping needed clearer borders/section titles, (3) the "1./2." tab switcher at the top didn't read as tabs because the active one barely looked different from the inactive one, (4) example text (placeholder) inside inputs added clutter when it just repeated the label, but is fine when it's a genuinely useful format example. Asked me to review and recommend against real UI/UX principles, then explicitly approved all of it plus 2 of my own follow-on suggestions before I touched any code, with the added instruction to optimize everything for **making the registrant understand exactly what they need to do**.
- **Fix 1 (tabs):** `#btn-tab-info` / `#btn-tab-products` and the JS toggle in `switchMerchantPortalTab()` now use the same solid-color active/inactive scheme already established elsewhere in the app (e.g. the category filter tabs from section 5r) - active = `bg-emerald-700 text-white shadow-sm`, inactive = `bg-white text-slate-500 border border-slate-200` - instead of the old "white vs. transparent on the same light-gray strip" pair that had almost no contrast.
- **Fix 2 (grouping):** the "ชื่อร้านค้า / แผงค้า" and "ร้านค้าขายอะไรเป็นหลัก" fields used to float directly under the LINE banner with no box or heading at all, unlike every other section below them (photos, contacts, payment). Wrapped them in the same white-bordered-box-with-icon-header pattern as the rest, titled "ข้อมูลร้านค้าเบื้องต้น".
- **Fix 3 (labeling optional sections consistently):** "ผู้ติดต่อคนที่ 2" header didn't say it was optional even though it visually looks identical in weight to the required "ผู้ติดต่อคนที่ 1" - added the same `(ไม่บังคับ)` tag already used next to "บัญชีสำรองที่ 2" and the second owner-photo slot, so all three "optional, second of two" sections now signal it the same way.
- **Fix 4 (placeholders):** removed placeholders that only repeated the label with no new information (`m-stall-name`: "กรอกชื่อร้าน" → a real example "เช่น ร้านผักสดป้าแดง"; both bank account-number/name fields: "กรอกเลขที่บัญชี"/"กรอกชื่อบัญชี" → "เช่น 123-4-56789-0" / "ชื่อ-นามสกุลเจ้าของบัญชี"). Also found and fixed a related gap while doing this: `m-contact1-phone`/`m-contact2-phone`/`-name`/`-line` had **empty** placeholders (`placeholder=""`) - added real format examples (`08x-xxx-xxxx`, `เช่น mama_veggie88`, "ชื่อ-นามสกุล หรือชื่อเล่น") since these are exactly the "genuinely useful example" case the owner said to keep.
- **Fix 5 (my suggestion, approved):** the LINE-admin-help banner used to be the very first thing after the tab bar, before any actual input field - competing for attention on a screen most people don't need help with. Moved it to sit right before the "ถัดไป: ใส่สินค้าและราคาที่ต้องการจำหน่าย" button at the end of the tab, so it's there for whoever gets stuck at the end without being the first thing everyone sees.
- **Fix 6 (my suggestion, approved):** condensed the green "กรอกข้อมูลพื้นฐานของแผงค้า" instructional box (icon + bold heading + full paragraph) down to a single compact line, since its message was generic and didn't need that much visual weight competing with the actual form.
- (Originally out of scope, done in section 5u below) tab 2 ("สินค้าและราคาที่ต้องการจำหน่าย") was not touched in this first pass - the owner's screenshot and feedback were specifically about tab 1's layout.
- **Follow-up (same day, after the owner previewed it live):** owner asked to darken the border on the 5 outer "container" boxes specifically (ข้อมูลร้านค้าเบื้องต้น, the shared photo-upload box, ผู้ติดต่อคนที่ 1/2, ช่องทางการรับชำระเงิน) so the outer/inner nesting reads more clearly - `border border-slate-200` → `border-2 border-slate-300` (amber box: `border-amber-200/80` → `border-2 border-amber-300`). Deliberately left every INNER sub-box (each photo slot, the owner-photo-1/2 cards, bank account 1/2 cards) at their original thinner border - darkening only the outer layer is what creates the hierarchy the owner asked for; darkening everything would have cancelled the effect out.
- Test: `firebase-rules/tests/merchant-registration-form-uiux.test.js` (21 checks - the 5 fixes above, plus this border follow-up: all 5 outer boxes confirmed at `border-2`/darker, and 2 representative inner boxes confirmed still at their original thin border so the nesting contrast actually holds).
- Verified live in an offline sandbox: opened the registration form fresh - tab 1 now shows solid emerald-green/white, tab 2 plain white/gray; stall-name/category fields sit inside a titled box matching the photo/contact/payment sections; contact 1 and 2 fields show the new example placeholders; scrolled to the bottom - LINE banner now sits directly above the "ถัดไป" button, not at the top; clicked tab 2 and confirmed via `className` inspection that the active/inactive colors swap correctly between both tabs.

## 🛵 5u. Extended the same UI/UX principles to merchant tab 2 and both rider registration forms (code `app.js?v=10.53_reg_forms_uiux_consistency`; on hsong-test/sandbox only, NOT pushed yet)
- **Owner's request (2026-09-23):** after approving the tab-1/border work in 5t, asked to (a) apply it to tab 2 ("สินค้าและราคาที่ต้องการจำหน่าย") of the same merchant form, and (b) apply the same principles to "the rider registration form" for consistency across the app, then push straight to production once done.
- **Merchant tab 2:** condensed the "สินค้า Highlight 10 รายการ" and "เพิ่มสินค้าอื่นๆ" instructional boxes down to one line each (same treatment as tab 1's banners in 5t). Darkened each of the 10 highlight-product cards and every additional-catalog-item row to `border-2 border-slate-300` (`renderMerchantTop6ProductsForm` / `renderMerchantCatalogTable` in `app.js`) - these cards are the direct equivalent of tab 1's "outer boxes" since nothing sits above them. Fixed two placeholders that were blank (`m-p-name-N`, `m-p-price-N` had `placeholder=""`) with real examples ("เช่น คอหมูย่าง, ไก่ทอด", "เช่น 50"), and made the additional-catalog row's matching fields consistent with the same examples instead of restating their labels ("ชื่อสินค้า", "ราคา").
- **Discovered while doing the rider forms: there are TWO separate, independent rider-registration UIs** that duplicate almost the same fields (found no shared static markup between them) - `#onpage-rider-reg-form` (the "1. ลงทะเบียนสมัครไรเดอร์ใหม่" tab on the Role-4 guest page, reached from the header's "สมัครไรเดอร์" button via `goToRiderRegisterScreen()` - this is the one CLAUDE.md section 2 documents as the main flow) and `#rider-register-modal` (a separate popup, reached via `openRiderRegisterModal()` from the rider login modal's "ยังไม่มีบัญชี" link). Both call the SAME shared JS function for their bottom half (`mountRiderRegExtras` → `renderRiderRegExtras`/`renderRiderAccountRows` for identity/emergency-contact/documents/bank-accounts/consent), but their TOP half (personal info / vehicle / zone-and-shifts) is separately hand-coded HTML in each, so both needed fixing independently for full consistency.
- **`renderRiderRegExtras` (app.js, shared by both forms):** the 4 previously-unboxed sections (identity, emergency contact, documents, bank accounts) now get the same `p-3.5 bg-white border-2 border-slate-300 rounded-2xl` treatment; the already-boxed consent section went from `border-amber-200` to `border-2 border-amber-300` to match; the shared `head()` header helper's internal divider was lightened from `border-slate-200` to `border-slate-100` so it reads as an inner divider, not competing with the new outer box border. Also swapped the emergency-contact-name placeholder from the vague "ชื่อผู้ที่ติดต่อได้" to a concrete example, "เช่น นางสมศรี ใจดี".
- **`#onpage-rider-reg-form` (index.html):** this one was structured differently from the merchant form - ONE big white bordered card wrapped 3 un-boxed sections (just a header + thin divider each, no box). Removed the outer card's own border/background/shadow (now a plain spacing wrapper, matching how the merchant modal's tab-content area is also unstyled) and gave each of the 3 sections (ข้อมูลส่วนตัว, ยานพาหนะ, พื้นที่วิ่งส่ง) its own `border-2 border-slate-300` box instead - this makes it structurally identical to the merchant form (unstyled container → titled dark-bordered boxes directly) and to how the shared extras sections now render, instead of mixing two different nesting depths.
- **`#rider-register-modal` (index.html):** already had each of its 4 sections in its own light box (`bg-slate-50 border border-slate-200`, no outer double-wrapper) - simpler fix, just darkened all 4 to `border-2 border-slate-300` and lightened their internal header dividers to `border-slate-100` to match. Also moved its LINE-help banner from the top of the modal (before any field) to just above the "ส่งใบสมัครให้แอดมินอนุมัติ" button - it was competing with the very first field exactly like the merchant form's banner did in 5t.
- Test: `firebase-rules/tests/rider-form-uiux-consistency.test.js` (18 checks covering all of the above across both rider forms and merchant tab 2).
- Verified live in an offline sandbox: merchant tab 2 shows the condensed banners and darker product-card borders with real placeholder examples; switched to the rider guest page (`switchRole('rider')`) - the onpage form's 3 sections and the shared extras sections (scrolled to bottom: bank accounts, consent) all show the same darker box treatment with no double-boxing; opened the separate rider modal (`openRiderRegisterModal()`) - same darker boxes, and the LINE banner now sits directly above its submit button, not at the top.
- **Pushed to production same day** on the owner's instruction (`อัปขึ้นเว็บจริงได้เลย` given up front, before this work started) - see git log for the push commit hash.

## 📖 5v. Rider guide page for applicants (`guide-rider.html` + `guide.css`, code `app.js?v=10.54_rider_guide`; PUSHED to main 2026-09-24 on the owner's request, live files verified identical to local)
- **Owner's request (2026-09-24):** a guide for people applying as riders (and later merchants) with 2 parts: (1) how to register successfully with complete data, (2) how to use the role after logging in; placed where people register. Owner chose: a WEB PAGE (not PDF), WITH screenshots, rider guide first, merchant guide later in the same style.
- **Files:** `guide-rider.html` (static page, NO `<script>`, no Firebase - safe to open anywhere), `guide.css` (shared style meant for the future merchant guide too: 19px base text, dark-mode tokens, print CSS), screenshots in `images/guide/rider/*.jpg` (~1 MB, 20 images). Contact = the existing public LINE OA `https://lin.ee/61O4EyH` + `images/line_oa_qr.png` (owner did not give another channel).
- **Content:** overview flow (prepare -> apply -> owner checks -> password by SMS/LINE -> log in), a checklist of what to prepare (3 required photos from `RIDER_DOC_SLOTS`), each form section 1-8 with a picture, a "message you see -> what to do" table using the REAL validation texts, the pending notice + password rules, login + lockout, the 3 workspace tabs (job board, current job incl. COD / change envelope / arrival buttons, wallet + hand COD cash to the hub the same evening), FAQ (forgot password, privacy: what only the owner sees vs what customers/merchants see), contact. Deliberately NOT included: the "rain bonus" / "10-trip bonus" promised in the rider modal's benefits card - no code implements them.
- **Links in the app (all open a NEW TAB so typed form data is not lost):** `#rider-guide-link-onpage` (top of the on-page form -> `#prepare`), `#rider-guide-link-modal` (top of `#rider-register-modal`), `#rider-guide-link-login` (login box -> `#part2`), `#rider-guide-link-workspace` (own slim row UNDER the 3 workspace tabs - putting it inside the tab bar squeezed the tabs and got cut off at 390px), plus a link in `renderRiderApplicationNotice()` (`#after`, or `#part2` when approved).
- **Screenshots are generated, not hand-made:** taken with `puppeteer-core` + the installed Edge, headless, 390x844 @2x, from the OFFLINE sandbox (fake host `127.0.0.1:9`), fake sample people/orders only (e.g. RD4821, "คุณมาลี (ตัวอย่าง)"). The script lives in the session scratchpad, not the repo; recipe: open sandbox, `switchRole('rider')`, `fillOnPageRiderSampleData()`, element screenshots of each `#onpage-rider-reg-form > div.p-3\.5` / `#onpage-rider-extras > div`, notice via `sessionStorage talathub_rider_app_notice`, workspace via `loginRiderWithProfile(fake)` + a fake `state.activeOrder` / `state.merchantExpressOrders`; inject CSS to make `.sticky` static and hide `#toast-message`. Re-take them whenever these screens change.
- **Rule for future work:** changing the rider registration form, rider login, or rider workspace means updating `guide-rider.html` (and screenshots if the look changed). Test `firebase-rules/tests/guide-rider.test.js` (83 checks) fails when a button name quoted in the guide (`<span class="btn-name">`) or a quoted message no longer exists in `index.html`/`app.js`, an image file is missing, an app link points at a missing anchor or is not `target="_blank"`, or a number in the guide (3 required docs, 15 MB, 3 accounts, 8-char password without O/I/L/0/1, 5 tries / 5 min lock, ฿40 per trip) no longer matches the code.
- **Fixed the same day on the owner's decision:** the on-page submit button said "ยืนยันการลงทะเบียน & เปิดระบบรับงานทันที 🚀" although applicants must wait for approval -> now "ส่งใบสมัคร (รอเจ้าของอนุมัติ)". Removed the rider-facing bonus promises that no code implements ("มีโบนัสพิเศษช่วงฝนตก", "โบนัสสะสมครบ 10 เที่ยว" in `#rider-register-modal`, "โบนัสฝนตก" in the home rider banner); replaced with true facts (payout to PromptPay/bank, choose your own shifts). Owner-side bonus fields (`#rider-payout-modal`, admin rate settings) were left alone.
- `firebase-rules/tools/build-test-site.js` now also copies `guide.css` and `guide-rider.html`. Deployed to https://hsong-test.web.app on 2026-09-24 (verified: guide, css and images return 200; new button label and 4 guide links present; bonus promises gone).

## 🏪 5w. Merchant guide page + shop-total bug fix (`guide-merchant.html`, code `app.js?v=10.55_merchant_guide`; PUSHED to main 2026-09-24, live files verified identical)
- **Owner's request (2026-09-24):** same kind of guide as 5v, for merchants. Same style (`guide.css`), same rules (static page, no script, screenshots from the offline sandbox with fake data only: "ร้านผักตัวอย่าง", APP-SHOP-0001, "ป้าแดง (ตัวอย่าง)"), 17 images in `images/guide/merchant/`.
- **Content:** overview, what to prepare, form page 1 (basic info, stall + owner photos, contact 1/2, bank) and page 2 (10 highlight products, up to 50 more, preview + submit), after submitting (APP-SHOP code = shop code, status check by phone, password by SMS/LINE), login via the "แผงค้า" role button (opens `#merchant-login-modal` when not logged in), open/pause toggle, order cards ("แจ้งหมด", "✓ รับออเดอร์ & เตรียมของเสร็จแล้ว", 80mm slip), express rider call (prepay delivery fee, no COD), settlement (GP 10%, "✏️ แก้ไขบัญชี"), FAQ, LINE contact. Deliberately NO privacy promise about bank accounts: `merchant_applications` is still publicly readable (known gap, see 5b).
- **Registration-window screenshots need a tall viewport** (390x4200): the modal scrolls on its own, and element screenshots inside a scrolled modal capture the page behind it.
- **Links (new tab):** `#merchant-guide-link-register` (top of form page 1 -> `#prepare`), `#merchant-guide-link-success` (success step -> `#after`), `#merchant-guide-link-login` (login modal -> `#part2`), `#merchant-guide-link-workspace` (slim row under the 3 merchant tabs -> `#part2`). `build-test-site.js` copies `guide-merchant.html`.
- **Real bug found and fixed while making the screenshots:** the per-shop total on the merchant's order card ("ยอดสินค้าร้านนี้", which also feeds "ยอดจำหน่ายรวมของร้าน") and on the 80mm slip summed the UNIT price and ignored quantity (2 bunches at ฿20 counted as ฿20), while the settlement page correctly used price x qty - so the two screens disagreed. New shared helper `merchantStallItemsTotal(items)` (price or `actualPrice` x qty, skips `outOfStock`) is now used by all three. Open question for the owner, NOT changed: whether the hub's `actualPrice` (weighed price, `updateHubItemWeight`) means per unit or per line - all three places treat it as per unit, as settlement already did.
- **Also fixed (leftovers of 5r, stall numbers removed):** the success step said "รับรหัส 6 หลัก" (now "รับรหัสผ่าน 8 ตัว"), "ปุ่ม 3. แผงค้า" (now "กดปุ่ม แผงค้า"), "หมายเลขแผง & หมวดหมู่" (now "หมวดหมู่ร้าน", shows the category only); a lone "-" (new stalls store `stallNumber: "-"`) no longer shows in the merchant header pill, the express sender badge/name, the "แผงค้าของฉัน" line, the status box, or the header "จัดการ" pill (shows the shop name instead).
- Test `firebase-rules/tests/guide-merchant.test.js` (85 checks): quoted buttons/messages exist, images exist, links hit real anchors in a new tab, numbers match the code (10 highlights, 50 more, 8-char password, 5 tries / 5 min lock also for merchants, GP example), the old "6 หลัก" text is gone, and `merchantStallItemsTotal` gives 90 for 20x2+25+25, skips out-of-stock items, uses `actualPrice`, and is used in all 3 places.

## 💰 5x. Owner decisions 2026-09-24: price x qty everywhere, bank account required, no rider button (code `app.js?v=10.56_bank_required_qty_money`; PUSHED to main 2026-09-24, live files verified identical)
- **(1) Owner: "unit price must follow the weighed weight".** Investigation first: the hub's weigh box in `renderHubPickingList` only shows when `item.hasScale` is true, and NOTHING ever sets `hasScale` - so the weighing feature has never appeared for real orders. The weight-based feature itself is NOT built yet (needs owner decisions on over/under weight, see the hand-off). What WAS fixed now, because it is plainly wrong money: many places used the unit price and forgot the quantity. New helper `orderItemLineTotal(it)` = (`actualPrice` if set, else `price`) x `qty`; `merchantStallItemsTotal` uses it. Now used by: hub vendor payout in `renderHubSettlement` (shops were UNDERPAID when qty > 1 - it is the "โอนเคลียร์เงิน (PromptPay)" amount), out-of-stock refunds in `goToRiderTrackingScreen`, `renderRiderScreen` (rider's change envelope), `renderTrackingScreen` (+ the stall "หมด ... คืน ฿" label), `renderHubPickingList`, `toggleHubItemOutOfStock` toast, `sendOutOfStockLineNotice`, the customer receipt subtotal (`openReceiptModal`), and the customer's tracking item list (now shows "xN" and the line total).
- **(2) Bank account required for merchants:** `merchantBankAccountProblem(bankName, no, name)` (same number rules as riders: bank 10-15 digits, PromptPay 10/13/15) runs in `saveMerchantStallData` right after the name/phone check, for BOTH new applications and edits; on failure it alerts, switches to page 1, focuses the field and gives it a red border. Consequence: an existing stall without a valid main account (e.g. the owner's test stall `APP-SHOP-6758` if it has none) cannot save edits until the account is filled - intended. The merchant guide says the account is required and lists the new messages.
- **(3)** Removed the "🛵 ไรเดอร์ (Role 4)" button from the merchant success step.
- Test: `firebase-rules/tests/order-money-qty.test.js` (45 checks: line totals, every money function above uses the helper and no longer adds a bare unit price, bank rules incl. focus ids and that the check runs before saving, rider button gone). Verified in the offline sandbox: submitting with an empty account shows the message, saves nothing, returns to page 1 with the field focused/red, and re-enables the submit button.

## ⚖️ 5y. Weighed items + merchant order buttons that really save (code `app.js?v=10.57_weighed_items`; PUSHED to main 2026-09-24 on the owner's request, commit fec6ef1, live files verified identical)
- **Owner decisions 2026-09-24:** (1ก) the MERCHANT types the weighed amount on the order card; (2) weighed LESS than ordered = charge the real weight, refund the difference in the cash envelope; (3ก) weighed MORE = no extra charge (customer pays what was ordered).
- **Helpers** (next to `orderItemLineTotal`): `WEIGHT_UNITS` = กก./ขีด/กรัม, `orderItemUnit(it)` (`it.unit`, or parsed from the name suffix "(กก.)" for old orders; checkout now also stores `unit` on each order item), `isWeighedOrderItem`, `orderItemWeighedQty` (`it.weighedQty`, same unit as ordered), `orderItemBilledQty` = min(weighed, ordered), `orderItemLineTotal` (weighed lines rounded to whole baht), `orderItemRefund(it)` (out of stock = full ordered amount; under-weight = ordered - line), `orderRefundTotal(order)`. Every refund place now uses them: `renderTrackingScreen`, `renderHubPickingList` (envelope list also names under-weight items), `toggleHubItemOutOfStock`, `goToRiderTrackingScreen`, `renderRiderScreen` (always recomputed, no stale stored value), `sendOutOfStockLineNotice`. Shop totals/payout (`merchantStallItemsTotal`) follow the billed weight automatically.
- **Real bug fixed at the same time:** the merchant's "แจ้งหมด" and "✓ รับออเดอร์ & เตรียมของเสร็จแล้ว" only worked when that order happened to be `state.activeOrder` on the merchant's OWN device (normally the merchant's own shopping order), so on a real merchant phone they showed a success toast and saved nothing. Now `mutateOrderEverywhere(orderId, fn)` updates every local copy (active order, `talathub_order_history`, `window._cachedFirebaseOrders`) and `patchOrderInCloud(orderId, updates)` sends ONLY the changed paths (e.g. `stalls/1/items/0/weighedQty`, `refundCashTotal`, `finalPaidTotal`) via `db.ref("orders/<key>").update()` with a REST PATCH fallback (`RIDER_DB_BASE_URL`) - never the whole order, so rider/hub status written meanwhile is not overwritten. If the order is not found locally the merchant gets an honest "not found, press refresh" toast. New `merchantSetItemWeight(orderId, stallId, itemIndex)` (input id from `merchantWeighInputId`).
- **Known limitation (pre-existing design):** other roles (hub/customer) still write whole orders with `syncOrderToCloud`; if their copy is older than a merchant's patch, they can overwrite it. Moving everyone to per-field updates is a bigger job.
- The hub's old "ชั่งจริง: [ ] บาท" box (`item.hasScale`, stores `actualPrice`) never appears because nothing sets `hasScale`; left in place, unused.
- Test: `firebase-rules/tests/weighed-items.test.js` (30 checks, incl. an in-memory run of the three merchant buttons on an order that is NOT the device's own: correct local update, only-changed-paths PATCH, other stalls untouched, wrong input rejected). Verified in the offline sandbox: typing 0.9 for pork ฿150/kg shows ฿135, "คืนลูกค้า ฿15 ใส่ซอง", shop total ฿155. Merchant guide step 4 now explains weighing (image `m23c-weigh.jpg`).

## 🎫 5z. "บัตรผ่าน" (Firebase Anonymous Auth) + orders/carts closed to strangers (code `app.js?v=10.58_order_access_pass`, rules `*.rules.v3.json`; LIVE on production 2026-09-25: code pushed + rules v3 published)
- **Problem (owner-approved fix 2026-09-25):** `orders` and `carts` were fully open: anyone could list every order (name, phone, address, GPS), edit or delete them; cart keys were customers' phone numbers (visible even in a shallow listing — confirmed read-only on production).
- **Design:** every browser signs in anonymously (`initOwnerAuth` → `_signInAnonymouslyOnce`; `getAuthUid`, `ensureAuthUser`). If the project has anonymous sign-in off, `_anonAuthFailed` = compat mode (old behaviour, works with the old open rules). Owner email sign-in replaces the anonymous user; sign-out gets a new anonymous uid.
- **Orders:** new orders carry `customerUid` (`stampOrderOwner`, called in `saveActiveOrderToStorage` and `_syncOrderToCloudNow`; copies the existing uid from `_cachedFirebaseOrders` so staff saves never change it). `syncOrderToCloud` waits for auth before writing. Track code is also written to `order_codes/<key>` (owner-read only). Deep links write `order_viewers/<key>/<uid>` = code before reading (`handleTrackingDeepLink`); the rules accept it only if it matches `order_codes`.
- **Who lists all orders:** owner, and riders/merchants with a DB-verified session. `canListAllOrders()` gates `syncAdminOrdersFromCloud`, `listenToFirebaseOrdersForAdmin`, `syncLatestOrderFromCloud`, `attachOrderChildListeners` (child_added/child_changed moved here from DOMContentLoaded), `_executeClearDailyOrdersAndReport`. Customers follow only their own order with `watchActiveOrderInCloud` (per-order `value` listener; `applyActiveOrderCloudUpdate`). Customer login now uses `restoreMyActiveOrderFromCloud` (own uid from local history) instead of listing 15 orders and matching names. `refreshOrderAccess()` re-runs all of this whenever access changes (auth ready, owner sign-in, staff login). Listener cancel callbacks reset their flags; a staff device without rights gets one toast "ออกจากระบบแล้วเข้าใหม่".
- **Staff proof (password checked by the database, not only the browser):** `makeRiderLoginCredential` also returns `staffProof` = PBKDF2(secret, salt + "proof"); owner stores it in `staff_keys/<rider|merchant>/<id>` (`saveStaffKey` in approve/reset for riders and merchants; `removeStaffKey` on revoke/reject/delete/reconsider). Riders are keyed by rider number (`accessCode`, NOT `community_riders.id` which can be `RIDER-xxxx`); merchants by `stallId`. On login (`submitRiderSecretLogin`, `handleMerchantCodeLoginSubmit`) the browser writes `staff_sessions/<uid>` = {role,id,proof}; rules accept it only if proof === staff_keys value. New password = old sessions lose rights immediately. Logout removes the session (`closeStaffSession`).
- **Carts:** `carts/<uid>` (no name/phone), only that uid reads/writes.
- **REST fallbacks** to `orders` append `?auth=<idToken>` (`authQueryParam`).
- **Rules v3** (`build-rules.js` now writes `*.rules.v3.json`; `*.rules.v2.json` untouched = current production / rollback). Deployed to hsong-test 2026-09-25 (syntax valid; `rules-anon.test.js` 60/60 pass with the new "no pass = no orders/carts" cases).
- **Tests:** `order-access.test.js` (57 offline checks). `rules-v3-live.test.js` = ~40 live cases on hsong-test with 3 real anonymous users (customer / stranger / rider), seeds `staff_keys` with the CLI, cleans up and deletes the test users; runs only with `LIVE=1` and needs anonymous sign-in ON in hsong-test. Four older tests got stubs for the new helpers; `order-broadcast-leak` now checks uid (phone match alone no longer counts as "my order").
- **Sandbox warning:** the offline sandbox recipe (section 5) keeps the PRODUCTION apiKey, and anonymous sign-in talks to googleapis, not the DB host — it would create anonymous users in production once production enables anonymous sign-in. Use the hsong-test build (`build-test-site.js`) or stub `auth` for sandbox work.
- **hsong-test status 2026-09-25:** owner enabled Anonymous + sign-up there. `LIVE=1 rules-v3-live.test.js` 40/40 pass. Checked on https://hsong-test.web.app in the browser pane: page gets an anonymous uid, an order saved through `saveActiveOrderToStorage` carries `customerUid`, the customer reads it, listing `orders` / reading `order_codes` is denied; a fresh device (new uid) opens `?track=..&code=RIGHT` and sees the order, `&code=WRONG` shows nothing. Test data removed. Owner tested by hand the same day: hub and admin (real owner sign-in) still see every order/stall; pressed 🔑 on stall `APP-SHOP-8952` (hsong-test) -> `staff_keys/merchant/APP-SHOP-8952` (64 chars) written; in an Incognito window a wrong password showed the red box, the right one entered the shop and wrote `staff_sessions/<uid>` {merchant, APP-SHOP-8952}; "ออก" removed it (verified with CLI GETs). Rider login not tried by hand (same code path, covered by the live test). All "ZZ*" junk from old test runs was deleted from hsong-test (211 paths); one came back from the owner's Chrome localStorage until he used Site settings > Delete data for hsong-test.web.app — remember this when cleaning test data.
- **Enabling it (done for hsong-test, needed later for production):** in the Console (1) Authentication > Sign-in method > Anonymous, (2) Authentication > Settings > User actions > "Enable create (sign-up)" (this is what blocked it: `ADMIN_ONLY_OPERATION`; rules only trust OWNER_UIDS and verified staff, so an email account made by a stranger gets customer-level rights only). Then run `LIVE=1 node firebase-rules/tests/rules-v3-live.test.js` and test the site by hand.
- **Production rollout order (do not swap):** (1) DONE 2026-09-25: owner enabled Anonymous + sign-up in hsong-1f342, (2) DONE 2026-09-25: code pushed by the owner (`199f55e`, live files verified identical; the auto-mode classifier blocks Claude's own `git push`), (3) DONE 2026-09-25 ~07:20: owner saw "(anonymous)" users in Console, then HE ran the deploy (Claude's `firebase deploy` to production is blocked by the auto-mode classifier; in the owner's PowerShell use `firebase.cmd`, `firebase` fails on the execution policy) with `--config <temp>/rules-prod/firebase.json`. Verified with read-only GETs: `orders`, `carts`, `staff_keys`, `order_codes`, `staff_sessions`, `rider_private`, root = 401; `rider_applications`, `custom_market_stalls`, `daily_reports` = 200. Rollback = `firebase-rules/hsong-1f342.rules.v2.json`, (4) DONE 2026-09-25: owner pressed 🔑 for `APP-SHOP-6758` and `RD3562` on the real site; production `staff_keys/merchant/APP-SHOP-6758` and `staff_keys/rider/RD3562` exist (64 chars, checked with a CLI read). Any FUTURE real rider/stall gets its proof automatically when the owner approves it. Step 4 must come AFTER the rules: the v2 rules have no `staff_keys` node, so the owner's proof write is rejected under v2 (root `.write` is false). Until step 4 riders/merchants cannot see orders. Known losses: orders made before the change (no `customerUid`) are visible to owner/staff only; a customer who clears browser data or changes device can only reopen an order through the link with the code; verified riders/merchants can still read ALL orders (narrowing to their own jobs is later work). Still open: `riders`, `rider_status`, `rider_locations`, `active_rider`, `merchant_applications` (bank accounts).

## 🛵 5z2. Riders/merchants see only their own jobs (code `app.js?v=10.59_own_jobs_only`, rules `*.rules.v4.json`; LIVE on production 2026-09-25)
- **Owner decisions 2026-09-25:** riders take jobs themselves AND the hub can assign; hub alert when a job has no rider for 10 minutes (`UNCLAIMED_JOB_ALERT_MIN`); riders can hand a job back before leaving; merchants see only the items to prepare (no customer name/phone/address).
- **Data:** `rider_jobs/<orderKey>` = short card {orderId, orderType, pickup, area, distanceKm, fee, createdAt, status open|claimed|done, claimedBy, claimedAt, assignedBy} (rules whitelist fields; no customer data). `stall_orders/<stallKey>/<orderKey>` = {orderId, savedAt, stallIndex, stall, status}. Both created once by the ordering device (`saveOrderSideCopiesToCloud`, triggered by `_newOrderIds` from `stampOrderOwner`). Order gets `assignedRiderId` (= rider number / `staff_keys` id) when taken.
- **Rules v4:** listing `orders` = owner only. Rider reads/writes an order only when `assignedRiderId` = own session id, or takes an unassigned one (claim = one multi-path update of `orders/<k>/assignedRiderId`+`rider_jobs/<k>/claimedBy`; a second rider is rejected). Merchant writes only `orders/<k>/stalls/<i>` whose `stallId` = own shop (cannot read the full order) and its `stall_orders` copy. `refundCashTotal`/`finalPaidTotal` are no longer written by merchants (everything recomputes with `orderRefundTotal`).
- **Client:** `canListAllOrders()` = owner only. Riders: `listenRiderJobs`, `renderRiderJobPool` (short card), `claimOrderForRider(orderKey)`, `releaseRiderJob` (button `#btn-rider-release-job`), `restoreMyRiderJob` (also picks up hub assignments). Hub: `#hub-rider-jobs-panel`, `renderHubRiderJobsPanel`, `hubAssignJobToRider`, `checkUnclaimedJobAlerts` (30 s timer). Merchants: `listenStallOrders`, `patchMerchantStall`, `watchMyExpressOrders`; merchant card no longer shows customer info. `syncOrderSideStatus` (called by `updateOrderStatusInFirebase`) updates job/copy status. Session role/id cached in `_myStaffSession` (`loadMyStaffSession`).
- **Removed made-up data:** fake riders "สมศักดิ์ / วินัย / ปรีชา" in the assign modal and `assignExpressOrderToRider`, the button label "จ่ายงานให้ไรเดอร์สมศักดิ์", the fake order in `claimOrderForRider` ("คุณลูกค้า (งานด่วน)") and in `dispatchOrderToRider` ("คุณสมหมาย แสนสุข"). Approving a merchant-express slip no longer auto-assigns a rider (job goes to the board).
- **Tests:** `rules-v4-live.test.js` 41/41 on hsong-test (LIVE=1); `rules-anon` 60/60 on v4; `order-access` + `weighed-items` updated. `rules-v3-live` is for v3 only now. Cleanup tool: `node firebase-rules/tools/clean-zz-test-data.js` (hsong-test only).
- **Browser end-to-end on hsong-test (Claude, 2026-09-25):** customer order -> job card + shop copy without personal data; test rider `ZZRD01` (password AAAA-BBBB) saw only the short card, took the job, saw full details, handed it back (lost access); test shop `ZZSHOP01` (CCCC-DDDD) saw the order without customer data, saved a weight (reached both the order and the copy). Owner then tested the hub by hand: the red "🛵 งานที่ยังไม่มีไรเดอร์รับ" card showed `#TH-265177` (waiting > 10 min), he assigned it to the test rider and the card disappeared; DB showed `assignedBy: hub`, `claimedBy: ZZRD01`, and the test rider logging in on a fresh device got the job automatically (`restoreMyRiderJob`). All ZZ test data and the test order were deleted afterwards (owner cleared Chrome site data first).
- **Production rollout (DONE 2026-09-25):** (1) owner pushed code (`ed36795`, live files verified identical), (2) waited 10 min, (3) owner published `hsong-1f342.rules.v4.json` with `firebase.cmd deploy --only database --project hsong-1f342 --config <scratchpad>/rules-prod/firebase.json`; confirmed with a CLI read of `/.settings/rules` (has `rider_jobs`, `stall_orders`) and stranger GETs (orders, rider_jobs, stall_orders, carts, staff_keys = 401). Rollback = `hsong-1f342.rules.v3.json`. Orders created before step 1 have no job card/shop copy: riders/merchants will not see them (hub assigns by hand).

## 🙈 5z3. Rider personal data hidden from strangers + 4 unused open nodes closed (code `app.js?v=10.60_rider_privacy`, rules `*.rules.v5.json`; LIVE on production 2026-09-25)
- **Finding (told to the owner):** the app never stored real rider GPS. `riders`, `rider_status`, `rider_locations`, `active_rider` were unused open nodes (anyone could write junk); rider icons on maps are simulated (position along the route by status; random numbers in admin). Real exposure was `community_riders` + `rider_applications` (public read: phone, PromptPay, LINE, loginHash that allows offline guessing).
- **Rules v5:** the 4 old nodes -> owner only. `community_riders` / `rider_applications` read = owner; a logged-in rider reads/updates only its own `community_riders/<key>` (match `accessCode` = session id; phone/id/accessCode/login fields locked). New `rider_public/<code>` = {status, loginSalt, profileKey} (per-key public read, list denied, visitors may only create `{status:'pending'}` once). New `rider_phone_status/<sha256("hsong-rider-phone|"+digits)>` = {code, status} for the phone status check.
- **Login:** `riderSecretLogin` first tries `_riderLoginViaServer`: read `rider_public/<code>` -> compute proof -> write `staff_sessions/<uid>`; the rules accept only the right password. No hash needed on the client. Falls back to the old local-list check when `rider_public/<code>` does not exist (keeps working under v4 during rollout). Wrong tries still count for the 5-try lock.
- **Owner side:** `initRiderRealtimeSync` runs only for the owner (called from `refreshOrderAccess`), listeners call `publishAllRiderPublic()` (memoised, debounced) so approve/reset/reject/reconsider automatically refresh `rider_public` + `rider_phone_status`; `deleteRiderApplication` -> `removeRiderPublic`. Visitors: `purgeRiderListCachesForVisitors` deletes old `talathub_community_riders`/`talathub_rider_applications` copies; `saveRiderApplications` -> `createRiderPublicPending` for new applicants; notice box and "check status by phone" read the public nodes. `loadSavedRider` no longer logs riders out when the local list is empty. Riders update their own status with `setMyRiderStatus`.
- **Tests:** `rider-privacy.test.js` (offline), `rules-v5-live.test.js` 32/32, `rules-v4-live` 41/41 and `rules-anon` 69/69 on v5 (hsong-test). Browser check on hsong-test: old cached rider list removed on a visitor's device; wrong password / pending / right password messages; rider stays logged in after reload with job board working; own status update allowed, listing riders denied; phone status check found a pending application and "not found" for an unknown phone; applicant pending entries created. Test data cleaned (`clean-zz-test-data.js` now also covers `rider_public`; `rider_phone_status` keys are hashes and must be removed by hash).
- **Production rollout (DONE 2026-09-25, in this order):** (1) owner pushed code `943d930` (live files verified identical), (2) waited 10 min, (3) owner published `hsong-1f342.rules.v5.json` (`/.settings/rules` shows `rider_public`, `rider_phone_status`), (4) owner signed in and opened แอดมิน > ไรเดอร์: public GET `rider_public/RD3562` = {status: approved, loginSalt (32), profileKey: RD3562}; stranger GETs of community_riders, rider_applications, rider_public list, rider_locations, active_rider, orders = 401. Original plan text: (4) owner signs in on the real site and opens แอดมิน > ไรเดอร์ once -> `rider_public` is written; until step 4 riders cannot log in (the old fallback list is private under v5). Verify with a public GET of `rider_public/RD3562` (status + loginSalt only). Rollback = v4 file.
- **Regression found 2026-09-25 (~1 h after v5 went live) and fixed in `app.js?v=10.61_blind_app_save`:** visitors' new rider applications were silently NOT saved: `saveRiderApplications` -> `syncKeyedToCloud` first reads the whole node as a baseline, v5 denies that read to non-owners, so it returned `skipped`. Checked production read-only: no application was lost (only `RD3562` exists, no stray `rider_public` pending entry). Fix: for nodes in `_BLIND_WRITE_NODES` (`rider_applications`, `merchant_applications`) a non-owner whose baseline read fails writes items one by one with `_blindWriteItems` — only `status: "pending"` items (or, for merchants later, the logged-in shop's own application), so stale local copies cannot overwrite other people's records. Verified on hsong-test with the real on-page form (application, `rider_public` pending, `rider_private`, `rider_documents` all saved) and that a stale approved record was skipped. Lesson: after closing reads on a node, test every NON-owner WRITE path that goes through `syncKeyedToCloud`.
- Not done yet (same problem, merchants): `merchant_applications` (bank accounts, phones) and `custom_market_stalls` login hashes are still public.

## 🏪 5z4. Shop personal data hidden + strangers can no longer edit shops (code `app.js?v=10.62_merchant_privacy`, rules `*.rules.v6.json`; on hsong-test, NOT pushed, production still v5)
- **Owner decisions 2026-09-25:** the shop's main phone (contact 1) is visible ONLY to logged-in riders (option ค, chosen after discussing: riders need it for express pickups; the real risk was strangers scraping every vendor's personal mobile); the customer card shows the owner photo + NICKNAME only (full real name hidden).
- **Found:** every public shop record (`custom_market_stalls` and `merchant_applications`) carried 2 bank accounts, contact phones/names, LINE id, the full owner name (also inside `stallTag`) and the login hash; and ANYONE could edit any existing application/stall/catalog as long as status/codes were unchanged (e.g. swap a shop's bank account number -> payout fraud).
- **Rules v6:** `merchant_applications` read = owner + that shop after login; non-owner update = only that shop (status/accessCode/id/login locked); create pending stays open. `custom_market_stalls` public read, write = owner or that shop (`stallId` locked; the old `accessCode`/hash locks were dropped — accessCode is only a reference now and differed between copies, which made shop saves fail), `.validate` forbids every field in `STALL_PRIVATE_FIELDS` (same list in `app.js` and `build-rules.js`, checked by a test). `stall_catalog_database` write = owner or that shop. New: `merchant_public/<code>` {status, loginSalt}, `merchant_phone_status/<sha256("hsong-merchant-phone|"+digits)>` {code, status}, `stall_contacts/<stallId>` {phone, name} (owner + logged-in riders + that shop).
- **Client:** `sanitizeStallForPublic` (also strips the owner name out of `stallTag`) is the `_CLOUD_COMPARE_MAPPERS.custom_market_stalls` mapper, so every stall write is sanitized. `stallOwnerDisplayName` (nickname) on the customer card and stall modal; the empty-shop "call to order" line with the phone / fake `081-234-5678` was replaced. Shop login: `_merchantLoginViaServer` (merchant_public -> proof -> staff_sessions, then `loadMyMerchantApplication` puts the shop's own full data locally), falls back to the old hash check while `merchant_public` does not exist. Owner: `initMerchantRealtimeSync` owner-only (called from `refreshOrderAccess`) -> `publishAllMerchantPublic`; `sanitizePublicStallsInCloud` rewrites old public records once per owner session; `removeMerchantPublic` on delete. Visitors: `createMerchantPublicPending` on apply; status check by phone via hash; `purgeRiderListCachesForVisitors` also drops other shops' applications and sanitizes cached stalls. Shops: own edits also update `stall_contacts` (`saveMyStallContact`); merchant applications use the same blind-write path as riders (`_blindWriteItems` allows the logged-in shop's own application). Riders: `callMerchantFromRiderUI` / rider screen fetch the phone with `fetchStallContactPhone` when the order has none. Removed fake shop phones `081-444-5555`, `081-999-8888` fallbacks in express orders and `contactExpressStall`.
- **Tests:** `merchant-privacy.test.js` (offline), `rules-v6-live.test.js` 41/41, `rules-v5-live` 32/32 + `rules-v4-live` 41/41 on v6, `rules-anon` 76/76 (strangers can no longer edit shops/catalogs/applications). Browser on hsong-test: customer sees nickname, no phone/bank/real name, application and contact reads denied; shop login (wrong password red box, right one enters with its own full data), edit form prefilled with bank/phone, save updated the application, `stall_contacts` and the public record (no private fields, real name gone from `stallTag`); logged-in rider got the new shop phone for an express job while a visitor could not; visitor shop application saved (blind write) with pending status, status check by phone worked on a fresh device, pending shop login shows the pending message. Test data removed (`clean-zz-test-data.js` now also covers `merchant_public`, `stall_contacts`; phone-status keys are hashes).
- **NOT testable by Claude:** owner-side `publishAllMerchantPublic` / `sanitizePublicStallsInCloud` (needs owner sign-in) — check after rollout step 3 with public GETs.
- **Production rollout order (do not swap):** (1) push code, (2) wait 10 min, then publish `hsong-1f342.rules.v6.json` quickly (between (1) and (3) a shop's own saves to its public record are rejected because the cloud copy still has the hash), (3) owner signs in and opens แอดมิน > ร้านค้า once -> `merchant_public`, `stall_contacts`, phone status written and public records cleaned. Until (3) shop login keeps working through the old hash still stored in the public record. Verify: GET `merchant_public/APP-SHOP-6758` (status + salt only) and `custom_market_stalls/APP-SHOP-6758` without phone/bank/ownerName/loginHash. Rollback = v5 file.
- Still open: many other hard-coded placeholder numbers shown to users (e.g. hub "089-123-4567", PromptPay "081-999-8888" on slips/receipts) — ask the owner for the real numbers.

## ⚠️ 6. Known Constraints (update when resolved)
- (Resolved) Personal data (ID numbers, addresses, driver-license photos in `rider_documents/<riderId>`, `rider_private`) is owner-read only since the owner-auth release.
- (Resolved 2026-09-20) Admin/hub PINs and the admin "test mode" quick login were removed; see section 5b.
- (Resolved 2026-09-22, section 5j; on hsong-test, NOT pushed to production yet) Both rider registration forms auto-approve; documents are collected but nobody must verify them before a rider can take jobs.
- GP rate is 10% (owner decision 2026-09-21). Single source: `hubSettings.merchantGP` (localStorage `hsong_hub_settings`); `loadMarketStallSettings().gpRate` reads from it and both settings forms reject values <= 0 (fall back to 10). Rider fee is still hard-coded to 40 THB per trip in the report.
- Admin "Settings" tab: only GP (`merchantGP`) is read by app logic. The other keys (minOrder, baseDeliveryFee, cutoffs, rider fares...) are saved but nothing uses them, and all settings live in one browser's localStorage (not synced to Firebase).
- Approving a rider adds them to `community_riders`; rejecting or moving an approved application back to pending now calls `revokeRiderAccessForApplication` to remove them again (2026-09-21).
- (Resolved 2026-09-21) The old open rules that expired 2026-11-02 were replaced in production by rules v2 (section 5c): no expiry, approvals/creation of riders and stalls are owner-only. Still open by design: `orders`, `carts`, `riders`, `rider_status`, `rider_locations`, `active_rider` (no customer/rider Firebase Auth yet).
- Not yet tried live on PRODUCTION as owner: approve/reject on the real site after the rules change (worked on hsong-test). Pending real applications: `RD1987` (test, delete) and `RD3562` (was moved back to pending during testing).
- Suspected pre-existing bug (not verified): `aggregateDailyOperations` does `set()` on the whole `daily_reports/<date>` node without `settledRiders`/`settledVendors`, which may erase the cloud copy of the settled flags each time a report is generated. Check before relying on settlement history.
- (Resolved 2026-09-23, section 5p) `fillSampleMerchantRegistration()` (the "1-Click กรอกข้อมูลตัวอย่าง" test-fill button on merchant registration) used to throw partway through - fixed alongside the stall-category bug, on the owner's request to fix both together before testing.
- (Resolved 2026-09-21, see section 5d) Rider login now needs rider number + secret password; phone and name no longer log anyone in.
- (Resolved 2026-09-21, section 5e, LIVE) Stored XSS: attacker text in database records could run code in customer / admin / hub browsers. Browsers may show a cached old page for ~10 min after a push (owner hit this on the test site: Ctrl+Shift+R fixes it).
- (Resolved 2026-09-25, section 5z, LIVE) `orders` / `carts` open to everyone: closed with anonymous auth + rules v3.
- (Fixed in code 2026-09-22, section 5i; PRODUCTION still has the bug until pushed) Any visitor's tracking screen could silently show a random stranger's name/phone/address, just from having the site open (cross-customer order leak in 3 cloud-sync functions + the child_added listener).
