# 📄 คู่มือขั้นตอนการนำโปรเจกต์ TalatHub / Hsong ไปพัฒนาต่อด้วย Claude Code

**โปรเจกต์:** ตลาดสดออนไลน์บ้านบึง (TalatHub / Hsong)  
**คลังซอร์สโค้ด (GitHub):** https://github.com/pisaen666/hsong  
**วันที่บันทึกเอกสาร:** 9 กันยายน 2026  

---

## 📌 1. สรุปภาพรวมและสถานะโปรเจกต์ปัจจุบัน

ระบบถูกออกแบบเป็น **Single Page Application (SPA)** โดยใช้ HTML5, Vanilla JavaScript (`app.js`) และ Tailwind CSS ประกอบด้วย 5 บทบาท (Roles) หลัก:

| บทบาท (Role) | สถานะความพร้อม | ฟังก์ชันหลักที่พัฒนาเสร็จสิ้น |
| :--- | :---: | :--- |
| **Role 1 (Customer / ลูกค้า)** | ✅ สมบูรณ์ | เลือกของสด ตะกร้าสินค้า ชำระเงิน ติดตามพิกัด GPS เรียลไทม์ |
| **Role 2 (Hub Operations / ฮับ)** | ✅ สมบูรณ์ | ใบจัดของสด รวมรอบพัสดุ จ่ายงานไรเดอร์ สรุปยอดโอนประจำวัน |
| **Role 3 (Merchant / แผงค้า)** | ✅ สมบูรณ์ | เปิด-ปิดร้าน สรุปค่างวด หัก GP 5% ตั้งค่าบัญชีธนาคาร เรียกรถด่วน |
| **Role 4 (Rider / ไรเดอร์)** | ✅ สมบูรณ์ | รับงานด่วน สัญญาณพิกัด 2 ขั้น อัปโหลดรูปส่งมอบ สรุปเงินกระเป๋า COD |
| **Role 5 (Admin / ผู้ดูแลระบบ)** | ⏳ รอปรับแต่งเพิ่ม | อนุมัติแผงค้า เรดาร์กองยาน ล็อกระบบภาพรวม |

*หมายเหตุ: โค้ดทั้งหมดผ่านการทดสอบไวยากรณ์ (`node -c app.js`) 100% ไร้ข้อผิดพลาด และ Push ขึ้น GitHub เรียบร้อยแล้ว*

---

## 🚀 2. ขั้นตอนปฏิบัติ 4 ขั้นตอนเพื่อพัฒนาต่อด้วย Claude Code

### 🔹 ขั้นตอนที่ 1: เตรียมสภาพแวดล้อมและดึงโค้ดล่าสุด (Environment Setup)
1. เปิดโปรแกรม **Terminal** (PowerShell หรือ Command Prompt) ในเครื่องของคุณ
2. เข้าไปยังโฟลเดอร์โปรเจกต์:
   ```bash
   cd c:\pisaen\hsong
   ```
3. ดึงซอร์สโค้ดและไฟล์บริบท [`CLAUDE.md`](file:///c:/pisaen/hsong/CLAUDE.md) ล่าสุดลงมาจาก GitHub:
   ```bash
   git pull origin main
   ```

---

### 🔹 ขั้นตอนที่ 2: เริ่มต้นเรียกใช้งาน Claude Code
1. พิมพ์คำสั่งรัน Claude Code CLI:
   ```bash
   claude
   ```
2. ระบบ Claude Code จะทำการอ่านและโหลดไฟล์บริบท [`CLAUDE.md`](file:///c:/pisaen/hsong/CLAUDE.md) ที่อยู่รากของโปรเจกต์โดยอัตโนมัติ ทำให้ปัญญาประดิษฐ์เข้าใจโครงสร้าง 5 Role ทันทีโดยไม่ต้องเสียเวลาอธิบายใหม่

---

### 🔹 ขั้นตอนที่ 3: ข้อความสั่งงานแรก (Initial Prompt) สำหรับคัดลอกวาง
คัดลอกข้อความด้านล่างนี้ไปวางใน Claude Code เพื่อให้สั่งงานได้อย่างต่อเนื่อง:

> *"ช่วยอ่านไฟล์ CLAUDE.md และสแกนดูไฟล์ index.html กับ app.js เพื่อรับทราบโครงสร้างโปรเจกต์ ขณะนี้เราพัฒนา Role 1 (ลูกค้า), Role 2 (ฮับ), Role 3 (แผงค้า), และ Role 4 (ไรเดอร์) เสร็จสมบูรณ์แล้ว โปรดตรวจสอบ Role 5 (Admin / ผู้ดูแลระบบ) และทำ Audit ระบบเพื่อพัฒนาปุ่มหรือขั้นตอนส่วนที่เหลือให้เชื่อมต่อกันครบทุก Role 100% พร้อมทดสอบด้วย `node -c app.js` ก่อนจบงาน"*

---

### 🔹 ขั้นตอนที่ 4: กฎเหล็ก 4 ข้อในการควบคุม Claude Code (Strict Rules)

1. **บังคับทดสอบ Syntax ทุกครั้ง:**  
   ระบุในคำสั่งเสมอว่า *"โปรดรัน `node -c app.js` เพื่อตรวจสอบความถูกต้องของไวยากรณ์ก่อนสรุปงาน"*
2. **สั่งงานเจาะจงทีละส่วน (Iterative Tasks):**  
   เน้นสั่งงานทีละหน้า หรือทีละบทบาท เช่น *"ช่วยปรับปรุงเฉพาะส่วนอนุมัติแผงค้าใน Role 5 Admin"* เพื่อป้องกันไม่ให้กระทบโค้ดส่วนอื่น
3. **รักษาการเชื่อมต่อฟังก์ชัน Global (`window.*`):**  
   ย้ำกับระบบว่า *"ห้ามลบการผูกฟังก์ชันเข้ากับ `window.*` ที่ด้านล่างไฟล์ app.js"* เพื่อให้ปุ่ม HTML ทำงานได้ไม่สะดุด
4. **บันทึกงานเป็นระยะ (Git Commit & Push):**  
   เมื่อพัฒนาแต่ละฟีเจอร์เสร็จสิ้น ให้สั่ง *"ช่วย git commit และ push ขึ้น origin main"* เพื่อสำรองงานไว้บน GitHub

---

## ⏰ 3. ตารางกำหนดเวลาการรีเฟรชโควต้า (Quota Refresh Schedule)

หากต้องการกลับมาร่วมงานกับระบบนี้ใหม่อีกครั้ง เวลาเปิดโควต้าเต็ม 100% จะเป็นดังนี้:

* **โควต้าโมเดล Claude / GPT (100%):** รีเฟรชใน **1 วัน 1 ชั่วโมง**
* **โควต้าโมเดล Gemini (100%):** รีเฟรชใน **1 วัน 11 ชั่วโมง**

---
*เอกสารนี้สามารถนำไปเปิด อ่าน หรือนำเข้า (Import) ใน Google Docs ได้ทันที*
