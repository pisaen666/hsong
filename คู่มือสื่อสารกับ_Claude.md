# คู่มือสื่อสารกับ Claude (ทำงานสลับคอมฯ บ้าน / ที่ทำงาน)

ประโยคคำสั่งไม่ต้องพิมพ์ตรงตัวเป๊ะ Claude เข้าใจถ้าคำใกล้เคียงกัน
แต่ใช้ประโยคเดิมทุกครั้งจะช่วยให้เข้าใจตรงกัน

## 1) ตอนเริ่มงาน (ทุกเครื่อง)

| สถานการณ์ | พิมพ์ว่า | Claude จะทำอะไร |
|---|---|---|
| **เริ่มงานทุกครั้ง** | `ดึงโค้ดล่าสุดจาก GitHub แล้วอ่าน CLAUDE.md กับ HANDOFF_TO_CLAUDE.md แล้วสรุปสถานะให้ฟัง` | ดึงโค้ดใหม่ อ่านเอกสาร ตรวจไวยากรณ์ และสรุปว่างานอยู่ตรงไหน |
| อยากได้ข้อมูล Firebase ล่าสุดในเครื่อง | `ซิงค์ Firebase ด้วย` | ดาวน์โหลดข้อมูลลงเครื่อง (อ่านอย่างเดียว) |
| **เมื่อวานเก็บงานค้างไว้ใน branch แยก** | `ดึง branch wip มาทำงานต่อ` | ดึงงานที่ค้างไว้มาให้ทำต่อ |
| เครื่องใหม่ ครั้งแรกเท่านั้น | พิมพ์เองในเทอร์มินัล: `git clone https://github.com/pisaen666/hsong.git` | (ต้องมีโฟลเดอร์โปรเจกต์ก่อนเปิด Claude Code; เครื่องต้องมี Node.js และ Git) |
| ไม่แน่ใจว่าค้างอะไรอยู่ | `ตอนนี้งานค้างอะไรอยู่บ้าง มีอะไรที่ยังไม่ได้ commit หรือ push ไหม` | ตรวจสถานะแล้วบอกตรง ๆ |

## 2) ระหว่างทำงาน

| สถานการณ์ | พิมพ์ว่า | Claude จะทำอะไร |
|---|---|---|
| **แจ้งปัญหาจากการใช้แอป** | `ที่หน้า... (บทบาท...) เมื่อกด... แล้วเกิด... แต่ควรจะเป็น...` แนบภาพหน้าจอหรือข้อความ error | หาสาเหตุ แก้ ตรวจไวยากรณ์ และสรุป |
| **ต้องการทดสอบบนเว็บจริง** | `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` | เปลี่ยนเลขเวอร์ชัน commit push แล้วตรวจว่าเว็บเสิร์ฟโค้ดใหม่ และบอกเมื่อทดสอบได้ |
| ทดสอบเสร็จ อยากล้างข้อมูลทดสอบ | `ตรวจข้อมูลทดสอบใน Firebase แล้วบอกว่าจะลบอะไรบ้าง` | ดูข้อมูลแล้วเสนอรายการ ไม่ลบจนกว่าจะได้รับคำตอบ |

## 3) ตอนเลิกงาน

| สถานการณ์ | พิมพ์ว่า | Claude จะทำอะไร |
|---|---|---|
| **งานเสร็จ ใช้ได้แล้ว** | `แก้เสร็จแล้ว อัปขึ้นเว็บให้ทดสอบ` | ส่งขึ้น `main` (เว็บจริงเปลี่ยนตาม) |
| **งานยังไม่เสร็จ** | `เก็บงานที่ทำค้างไว้ขึ้น GitHub ใน branch แยก` | ส่งขึ้น branch `wip` เว็บจริงไม่เปลี่ยน |
| **ทุกคืนก่อนย้ายเครื่อง** | `อัปเดต HANDOFF_TO_CLAUDE.md สรุปงานส่งต่อ` | เขียนสิ่งที่ทำ เรื่องที่รอตัดสินใจ และข้อควรระวัง แล้วเก็บขึ้น GitHub |
| งานที่เก็บไว้ใน `wip` เสร็จแล้ว | `รวม branch wip เข้า main แล้วอัปขึ้นเว็บให้ทดสอบ` | รวมงานเข้า `main` และตรวจเว็บจริง |

## กฎ 4 ข้อที่ต้องจำ

1. **เริ่มงานต้องสั่งดึงก่อนเสมอ** ไม่งั้นโค้ดของสองเครื่องจะชนกัน
2. **เลิกงานต้องสั่งส่งขึ้น GitHub ก่อนปิดเครื่อง** จะขึ้น `main` หรือ `wip` ก็ได้ ขอแค่ไม่ปล่อยค้างไว้ในเครื่อง
3. **Claude จำการคุยข้ามเครื่องไม่ได้** รู้แค่สิ่งที่อยู่ในไฟล์ CLAUDE.md และ HANDOFF_TO_CLAUDE.md จึงต้องสั่งอัปเดตทุกคืน
4. **Claude ถามก่อนเสมอ** ถ้าจะเขียนหรือลบข้อมูล Firebase จริง

## สิ่งที่ไม่ตามไปกับ GitHub

- ประวัติการคุยกับ Claude และความจำที่ Claude จดไว้ (อยู่เฉพาะเครื่องนั้น)
- `database.rules.json` และ `firebase-rtdb-export.json` (อยู่ใน `.gitignore`)
- งานที่ยังไม่ได้ commit

## ข้อควรรู้เรื่องเว็บทดสอบ

- เว็บ GitHub Pages (https://pisaen666.github.io/hsong/) ใช้ **ฐานข้อมูล Firebase ตัวเดียวกับของจริง** ข้อมูลที่ทดสอบจะเข้า DB จริงทันที
- หลัง push ใช้เวลาประมาณ 1-2 นาทีกว่าเว็บจะอัปเดต แล้วกด `Ctrl + F5` เพื่อโหลดใหม่
- Firebase rules ปัจจุบันจะหมดอายุ **3 ต.ค. 2026** หลังวันนั้นเว็บจะอ่านเขียน DB ไม่ได้
